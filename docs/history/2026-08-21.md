# 2026-08-21

### 2026-08-21 — The gains section on a phone, where it had nothing to show

The ten rows under *Co získáte?* carry a card of the application's own markup,
built on 20 August. Below 900 px it read:

```css
@media (max-width: 900px) { .row-reveal { display: none; } }
```

So every phone and every tablet held upright got the sentences and no proof at
all — the argument the section is built on, gone at the width where most of a
link's traffic reads it. Three more faults came out of measuring the same
widths, and one of them made the page scroll sideways.

#### The card comes into the flow rather than being taken away

The reason it was an overlay is that a card which unfolds moves everything
under it, and that reason is a two-column reason. In one column the row has the
width to itself and there is nothing to push. So below 900 px the fragment is
`position: static`, in `grid-column: 2` beside the icon, 92 px tall, with no
transition and nothing hidden: the proof stands under the sentence that makes
the claim.

The room turned out to be there already. The fragments were drawn for the
526 px column of the two-column grid, and one column is **820 px at 899 and
565 px at 620** — wider than what they were built in, all the way down to the
phone breakpoint.

Two fragments needed the width taken seriously:

- **The header bar** carries the mark, three marks for where sound comes from,
  and the running recorder pill. At 299 px they overlapped. The mark goes below
  620 px — the page wears it in its own header two screens up, and the card is
  about the other two — and the pill takes what is left instead of its fixed
  290 px. The application folds that pill at exactly 900 px, which is right in a
  380 px window and wrong in a picture of a 1280 px one, so the fragment keeps
  its waves and its name.
- **The eight format sheets** are 465 px of fixed widths. They are a share of
  the card now, capped at the 52 px they are drawn at.

#### `min-height: 128px` outlived the card it was for

Every row that can open has a floor of 128 px so the card clears the hairline
evenly. That rule was never turned off with the card, so below 900 px each row
kept 48 px of empty space under text that measures 79. Ten rows, 480 px of
nothing, and the section stood at 2016 px where its writing needs about 1540.

#### The loop cards never started

Three cards animate — the corrected word, the three file names arriving, the
subtitle sweep — and the loop is started by `pointerenter` or `focusin`, since
until now the card was only ever seen while hovered. On a phone neither ever
fires, so *Sledovaná složka* stood empty: its file rows begin at `opacity: 0`
and nothing was there to bring them in.

Below 900 px an `IntersectionObserver` starts each one as it arrives on screen,
once and then never again — a picture that replays every time it scrolls past is
a flicker rather than a proof. The wordmark in the header bar is drawn the same
way; the two paths share one `startRow`, so hover and scroll cannot both draw it.

#### The page scrolled sideways, and the number was in the stylesheet

`.carousel-viewport` takes 48 px of horizontal padding for the window's shadow
and gives it straight back as `margin: -48px`, which puts the box 48 px past its
column on each side. The page's gutter is 32. So **every window narrower than
1276 px scrolled sideways by exactly 16 px**, and below 620 px, where the gutter
is 20, by 28.

The amount is the gutter now, at both breakpoints. It is not a number of its
own and never was — the negative margin makes it the overhang, and the overhang
has to be something the page can afford.

At 320 px there was a second one: the mark and the three places to go want
346 px of a 305 px line. They wrap now, rather than one of them being taken
away.

#### Nineteen tab stops behind an invisible card

The ten cards hold 19 buttons of the application's own furniture — a time mark,
a rename, a stop. `.row-reveal` is `opacity: 0` and `pointer-events: none` when
shut, neither of which takes anything out of the tab order, so a keyboard was
walking through window furniture nobody could see on the way to *Stáhnout*.
Every card is `inert`.

What it costs: `:focus-within` was the keyboard's way of opening a card, and
with the contents inert nothing inside can hold focus, so on a desktop the
cards are now a pointer affordance only. That is the right trade — the sentence
carries the claim and the card illustrates it — and below 900 px the question
does not arise, because there the card is simply open.

#### Measured, at fifteen widths

`document.scrollWidth` against `clientWidth` from 320 to 1440 px: **no sideways
scroll at any width**, where before there was 16 px from 621 to 1276, 28 px
below 620 and 41 px at 320. No card clips its own frame at any width except
320 px, where two cut a hairline (10 px and 2 px) inside their own 92 px face.

The desktop is untouched by all of it: the section measures 1131 px at 1280 and
1440 px, the same as before the change, and the cards still arrive on hover.

Files: `docs/site/index.html` — the stylesheet's 900 px and 620 px blocks, a new
420 px block, `.carousel-viewport`, `.save-formats`/`.fmt`, `inert` on the ten
cards, and the boot script's row-staging.

### 2026-08-21 — 92 px was the height, and on a phone it had to be a floor

The owner, from the screen: the note card is cut off at the bottom on a phone.
He is right, and it was not only that one.

The card is 92 px because it was an overlay. An overlay that grew would have
covered the row above it, so the frame had to hold and the fragment had to fit
inside it — which it does at the 526 px it was drawn for. At 299 px the note's
sentence wraps onto a second line and the row of controls under it goes past
the frame. Measured, `scrollHeight` against `clientHeight`:

| card | 390 px | 360 px | 320 px |
|---|---|---|---|
| Poznámky drží slovo | +18 | +18 | +50 |
| Složky na úložky | — | +2 | +18 |
| Odhalíte nejistá místa | — | — | +14 |

In the flow there is nothing under the card to push, so the reason for the
fixed height is gone with the overlay. Below 900 px the 92 px is a
`min-height` on `.row-reveal` and `.row-face` both, and the fragment takes the
room it needs. Nothing is cut at any width from 320 to 1440 now.

One fragment could not be helped by height, because a file name is one line by
nature: the three names in *Sledovaná složka* were 2 px wider than the card at
320 px. `justify-items: start` had the rows hugging their text; they take the
list's width now and the name is cut with an ellipsis, which is what the
archive itself does with a name too long for its row. That needed
`min-width: 0` on the row as well — `min-width: auto` on a grid item is its own
min-content, and the min-content of an unbreakable name is the whole name, so
the row stayed 211 px wide inside a 189 px list until it was said.

The desktop is untouched: the section still measures 1131 px at 1280 and
1440 px, where the card is still an overlay of exactly 92 px.

#### Parked, and worth writing down

The owner's other reading of the same screen: with all ten cards standing open
the section takes an endless amount of room — 3603 px at 390 px against 1131 px
on a desktop — and he would rather the card opened on a tap. His words, and his
call: *zatím to nechme tak, jak to je*. So this is not a decision made, it is a
decision deferred, and the argument on both sides is here for whoever picks it
up. Open on a tap is one more thing to discover on a page whose whole job is to
answer *should I install this?* before the reader loses interest; standing open
is ten pictures nobody asked to see. Nothing in this repository knows which is
right, and the thing that would is somebody scrolling it on a telephone.

Files: `docs/site/index.html` — the 900 px block.

### 2026-08-21 — *Inteligence po ruce* becomes *AI funkce po ruce*

The owner, in a comment on the published page, anchored to the third lead card
under *Co získáte?*: change it to **AI funkce po ruce.** Done, and it is the
only change — the sentence under it is untouched.

Worth the line because this is the third name that card has had. It was
*Shrnete i přeložíte u sebe* until 20 August, when the owner renamed all three
leads to two-word titles of equal weight; the entry from that day records what
the rename cost and that he was told at the time. This one moves in the other
direction: *Inteligence* was the vaguest of the three — a category with no
edges — and *AI funkce* names the thing a reader is actually looking for on a
page about transcription. The sentence beneath still says what those functions
are, which is why the title can afford to be flat.

Files: `docs/site/index.html`.

### 2026-08-21 — The numerals come back, on the third try, as a display

*Třikrát a dost* has three blocks and, since 20 August, nothing to number them
with. Two attempts had already been taken off — an 11 px label, then for an
hour a 92 px figure in the page's own background colour lifted by nothing but
its shadow, which *looked good and said nothing*. The owner asked for a third:
a large numeral, segmented and dotted like a display, that flicks through
something before it settles.

Three variants went up for approval and he took **A**: a **5 × 7 dot-matrix
panel** that rolls through other digits and stops on its own.

#### What is drawn, and where the number lives

The whole cell is in the markup — all thirty-five dots, lit and unlit, as
`<circle class="dot">` — which is what makes it a panel rather than a figure
made of dots. Unlit takes `--line`, the quietest colour the page has.

**The number is in the HTML, not in the script.** The panels are written out
already showing 1, 2 and 3; the script only overwrites them for half a second.
A browser with no JavaScript, a blocked script, an observer that never fires —
all of them show the number, because the animation is a thing that happens to a
numeral that is already correct.

43 px, and not an arbitrary smaller number than 92: it is the height of the
title and its sentence together, so the panel ends where the writing ends.

#### The roll is on the clock, not on a chain of ticks

Nine digits and then its own, 55 ms a frame, the three columns a beat apart so
the section reads as three panels waking rather than one animation in
triplicate. The step is three digits, not one — consecutive frames then share
few enough lit dots to read as a roll instead of a flicker.

`requestAnimationFrame` against `performance.now()`, not `setInterval`. A
background tab freezes the first and throttles the second to one tick a second,
so with ticks a reader who scrolled past in a background tab and came back
would find a panel still shuffling. Here the frame that arrives on return is
simply past the end, and the branch for past-the-end paints the settled number.

#### Two faults found by checking rather than by looking

**The bitmap table disagreed with the markup, for the digit 3.** The panels are
generated from a table of seven five-character rows; the script carries the same
digits flattened to one string each, and the flattening of 3 was wrong by hand.
Nothing would have shown it — the panel starts correct and only the last frame
of the roll would have differed, for one digit, for 55 ms. What found it was
comparing the two representations against each other rather than viewing the
page: the markup's lit dots read back out of the HTML, against the script's
strings. Both are now generated from the same seven-row source.

**A brace had been standing orphaned in the stylesheet since 20 August.**
Removing the counter rule that numbered these blocks took its declarations and
left its `}` behind. A stray closing brace at the top level is ignored, so the
page never looked wrong; it is gone with the rule it used to close.

#### Verified

Rendered at 1280, 900, 620 and 390 px: no sideways scroll at any of them, no
panel wider than its row, all three settling on the right count of lit dots
(10, 14, 14 — which is 1, 2 and 3), and no JavaScript error on the page.

Files: `docs/site/index.html` — the `.rows.narrow` block in the stylesheet,
three panels in the markup, and the roll in the boot script.

### 2026-08-22 — *Sledovaná složka* says the thing it does

The owner's line, in his words:

> Volocal automaticky sleduje, přepisuje a přidává obsah vybrané složky.

It replaces *V nastavení zvolíte složku. Volocal ji sleduje, přepisuje a
přidává obsah do archivu sám.*

Two sentences become one, and what goes is the half that described a setting
rather than a gain: where the folder is chosen is a thing the reader finds out
once they have the program, and *Co získáte?* is read by somebody who does not.
The word **automaticky** now stands where *sám* stood at the end — the same
claim, made before the reader has stopped paying attention rather than after.

Nothing moved for it. The row keeps its 128 px floor on the desktop, so the
sentence dropping from two lines to one changes no height at all: the section
measures 1131 px before and after. Checked at 1280, 899, 620, 390 and 320 px —
no sideways scroll, no card cut, and the sentence takes one line down to 620,
two at 390 and three at 320.

Files: `docs/site/index.html`.

### 2026-08-25 — *Na čem to běží* becomes *Jak a na čem běží*

The owner, pointing at that heading from a comment on the published page. The
section had always answered two questions and named one of them: Rust, Tauri,
whisper.cpp and Silero are *what it runs on*, and *on your own machine, not on
somebody's server* is **how** — which is the half a reader of this page came
for, the whole argument above it having been about privacy.

The heading is linked from nowhere; the only nav anchor on the page is
`#firmy`, so nothing else had to move.

His comment on the artifact read *Našem běží* and his line here read *Jak a na
čem běží*; the second is what went in, being the whole phrase. If the shorter
*Na čem běží* was the intention, this is the entry that gets corrected.

Files: `docs/site/index.html`.
