# Change log — 2026-08-11

Yesterday, and why anything here is the way it is:
[README.md](README.md).

### 2026-08-11 — CI could not build the installer any more, and the first release goes out unsigned

- Dispatched the installer job on `main` before tagging anything, because the
  configuration had changed under it and nothing had built since. It failed:
  *A public key has been found, but no private key. Make sure to set
  `TAURI_SIGNING_PRIVATE_KEY`.* With a public key in the config the bundler
  demands the matching private key at bundle time, and that key must not be on
  a runner — anybody who can push a workflow could then sign a release this
  application accepts.
- Fixed by building CI's installer with `createUpdaterArtifacts` turned off,
  through a partial config the CLI merges. What comes out of CI is a build that
  proves the code bundles and the result starts; a release anybody installs is
  built and signed by hand.
- The path was wrong on the first attempt — `--config tauri.ci.conf.json`
  resolves against the working directory, not the Tauri folder, so it has to be
  `src-tauri/tauri.ci.conf.json`. Found in thirty seconds locally rather than
  in twenty minutes on a runner, and worth the habit: the failure is immediate
  and unmistakable, so it is cheap to provoke on purpose.
- Timing worth recording: this was found **before** the first tag existed. A
  tag is created the moment a draft release is published, so the failure would
  otherwise have appeared on the release itself.
- **The first release goes out unsigned, and that is a decision rather than an
  omission.** There is no code-signing certificate on the machine; `signtool`
  is installed and has nothing to sign with. The reason not to buy one today is
  that it would not help: OV certificates and the cheap cloud signing services
  start with no SmartScreen reputation and earn it by being downloaded, which
  is the thing that has not happened. Only EV is trusted on sight, at hundreds
  of dollars a year and a hardware token.
- So `release.ps1` grew `-Unsigned`. It has to be asked for — the guard exists
  to stop an unsigned release happening by accident — and it prints what every
  reader will see. Both paths tested: without the switch it stops, with it the
  run continues.
- Also cleared a warning the release build printed: `App.tsx` loaded the
  updater plugin behind `await import(...)`, which looked like it kept the
  module out of the bundle until somebody turned the check on. It did not — the
  About page imports the same module directly, so it was in the main chunk
  anyway, and the bundler said so. Now imported plainly, renamed to
  `checkForUpdate` because `check` is already the tool-availability state in
  that component.
- Files: `.github/workflows/check.yml`, `src-tauri/tauri.ci.conf.json`,
  `scripts/release.ps1`, `src/App.tsx`, `README.cs.md`.
- Verified: the config override loads (build reaches `beforeBuildCommand`);
  both `-Unsigned` paths; `npm run build` with the warning gone; `npm run test`
  (65).

### 2026-08-11 — Volocal 1.0.0, and a signing key that is worth something

- The version goes 0.9.0 to 1.0.0. Not a milestone awarded for effort: the
  number is a message to whoever is about to install it, and `0.9` was telling
  them to wait for something that had already arrived. The application is in
  daily use by its author on real work, and the hardening plan finished the day
  before.
- What 1.0.0 does not claim: that the installer is signed by anyone Windows
  recognises, or that an update has ever been installed by anything. Those are
  release-process gaps and the version makes no promise about them.
- The updater signing key was replaced first, with one that has a password.
  Today that costs nothing — nothing has been released, so no installation has
  the old public half. After the first release it costs everything: the public
  key is baked into every copy, and rotating it means none of them will ever
  accept an update again. It was the last cheap moment.
- The old key was generated without a password because the tool defaulted that
  way and it was convenient; anyone who could read the owner's profile could
  have signed an update the application would have accepted. Checked rather
  than assumed that the new one is different: signing with an empty password
  now fails with *Wrong password for that key*. The key id in the config
  changed from `8B3B0235CC530786` to `A2ECEAED4E1E4E1D`, and matches the public
  half on disk byte for byte.
- Files: `src-tauri/tauri.conf.json` (public key and version), `Cargo.toml`,
  `package.json`, `Cargo.lock`.
- Verified: `npm run build`; `npm run test` (65); `cargo test` (150);
  `i18n check --strict` exits 0.

### 2026-08-11 — "You have the newest version" belongs in the notice bar

- The owner's note after the first release: the answer to a check that found
  nothing should appear where the application says everything else, not under
  the button that asked.
- So the rule is now about whether anything can be done with the answer. A
  finished check with no follow-up — nothing newer, or it could not be reached
  — goes to the notice bar. What stays in the panel is what carries an action
  or a wait: the version that was found, beside the button that installs it,
  and the progress of a download where the download was asked for.
- The `current` state disappeared from the component entirely, because a state
  that renders nothing is not a state.
- Corrected a comment while there, which had quietly become false: the file
  still said the check happens "on a button press and nowhere else". The switch
  added the same afternoon means that is no longer true, and a comment
  describing a promise the code stopped keeping is worse than none.
- Files: `src/settings/updates.tsx`, `src/Settings.tsx`, `src/App.tsx`.
- Verified: `npm run build`; `npm run test` (65).

### 2026-08-11 — Volocal 1.0.1, released to find out whether updating works

- Same application as 1.0.0. The point of it is the one thing no amount of
  reading proves: that an installed copy fetches the feed, accepts the
  signature, downloads the installer and comes back running. Until a second
  release exists there is nothing to update *to*, so the loop cannot be
  exercised at all.
- What 1.0.0 already proved, by being pressed: the feed is fetched, parsed and
  compared. That much works.
- The build no longer needs the signing key. The bundler's own `.sig` was
  always thrown away — it is made over bytes Authenticode has not touched yet,
  and the signature that ships is made in the second pass over the file as it
  will be downloaded. So the twenty-minute step now builds with the updater
  artifact turned off, exactly as CI does, and asks for no password. Only
  publishing needs the key.
- Files: version in all four, `scripts/release.ps1`.


### 2026-08-11 — A release went out carrying the previous version's installer

- Reported from the running application: the update downloads, the application
  closes, and nothing installs. Everything about that behaviour was correct;
  the contents were not. `v1.0.1` held `Volocal_1.0.0_x64-setup.exe`, and
  `latest.json` pointed at it while claiming version 1.0.1. The application
  downloaded itself and the installer had nothing to do.
- How it got there. The 1.0.1 build failed on `npm run tauri build --
  --config …` — **npm claims `--config` for itself even after `--`**, the same
  trap as `-w` earlier the same night, and reports `Unknown cli flag`. The
  failure left the previous installer in the bundle folder, and the publishing
  pass picks the newest `.exe` there, which was then the 1.0.0 one.
- Both halves fixed. The build calls `node_modules\.bin\tauri.cmd` directly
  rather than through npm, which is what does the flag-grabbing — and the same
  line in CI was corrected, where it had additionally kept a path that does not
  resolve. And the publishing pass now refuses an installer whose name does not
  carry the version being released: *that is the previous build, still in the
  folder because this one did not get as far as replacing it.*
- Which is the shape of the whole evening's lesson again: the guard was written
  from the failure that had already happened, and checked against exactly the
  state that caused it before being trusted.
- The bad release and its tag were deleted rather than patched. Nobody but the
  owner had seen it, and a release whose assets were swapped underneath it is
  worse to reason about later than one that never existed.
- What this cost, and it is worth being plain: the update path was proven to
  work — download, exit, hand over to the installer — by a test that failed for
  a different reason. The half that still has not been seen is an installer
  that actually replaces the program and starts it again.
- Files: `scripts/release.ps1`, `.github/workflows/check.yml`.


### 2026-08-11 — The installer was being killed by the job object that keeps ffmpeg in line

- Reported twice, and the second time with the release finally correct: the
  update downloads, the progress bar fills, the window disappears — and nothing
  installs, nothing comes back.
- Found by elimination rather than by reading. The release was correct. The
  installer *was* written to `%TEMP%`, four copies of it, one per attempt. Run
  by hand with the plugin's own arguments — `/P /R /UPDATE` — it installed
  1.0.1 and restarted the application. Called through `ShellExecuteW` exactly
  as the plugin calls it, it worked. Called with the application still running,
  it worked. Every part worked in isolation.
- What was left was the one thing the plugin does that no test of mine did:
  `std::process::exit(0)` immediately afterwards. And this application puts
  itself in a **job object** with `KILL_ON_JOB_CLOSE`, so that whisper, ffmpeg
  and llama-server cannot outlive the window. The installer is started by this
  process, joins the same job, and is killed the instant the window exits. The
  safeguard was working perfectly on the one process that had to be exempt.
- `let_the_installer_out` adds `SILENT_BREAKAWAY_OK` to the job, and the window
  calls it immediately before `downloadAndInstall`. Added rather than replacing
  the kill flag, so everything already running still dies with the window; only
  what starts after the call — the installer — is left out.
- A detour worth writing down: the command would not compile in `main.rs`
  at all. `#[tauri::command]` in a binary's crate root collides with itself
  through `generate_handler!` — *`__cmd__…` is defined multiple times* — with
  one definition in the file. Every other command in this codebase lives in
  `commands/`, and that is not decoration.
- Files: `src-tauri/src/main.rs`, `src-tauri/src/commands/updates.rs` (new),
  `src/settings/updates.tsx`, `src/api.ts`, the dictionaries.

### 2026-08-11 — Two small things the owner saw

- The version that was found now goes to the notice bar with everything else,
  and the panel keeps only the button that acts on it. The button says what it
  does without repeating the number.
- The hover highlight in the sidebar lists ran into the card's border and its
  rounded corner, which reads as something that failed to stop rather than
  something placed. It was pulled out the full 12 px on purpose — a menu item's
  highlight spans its menu — and fully inset it had read as a floating cell.
  6 px is the half measure, deliberately.

### 2026-08-11 — An explanation and the list it explains were sitting on each other

- In the one sidebar card that has both — the unnamed interjections — the
  sentence above the list and the list itself were flush. The first row's
  highlight began exactly where the sentence ended, and in a card small enough
  to see both gaps at once they disagreed.
- The card already owns 10 px above its body, and the comment on that rule says
  why it is one number: five sections used to begin at 2, 2, 4 and 12 px
  because each list carried a top margin of its own. So the gap under the
  explanation is the same 10 px. One number, used twice.
- Files: `src/css/04-nastaveni.css`.

### 2026-08-11 — The unnamed interjections say what happened, not what they are

- Was: *Vsuvky, na které rozpoznání nestačí. Klepnutím si je poslechnete,
  jménem přiřadíte.* Now: *Tady bylo rozpoznání nejisté. Kliknutím přehrajete
  a tlačítkem přiřadíte.*
- The owner's wording, and the difference is worth naming. The old sentence
  described the rows as a category — *interjections the recognition cannot
  place* — which is the application's view of them. The new one says what
  happened and what to do, which is the reader's. And `tlačítkem` is what is
  actually on screen; `jménem` described the intention rather than the control.
- English follows the same shape rather than the same words.
- Files: `src/locales/cs/detail.ts`, `src/locales/en/detail.ts`.

### 2026-08-11 — An update installed itself, which is the first time that has been true

- Volocal 1.0.3 was published, and the installed 1.0.1 found it, downloaded it,
  handed it to the installer and came back running. End to end, on the owner's
  machine, without help.
- Recorded because until now this was a reasoned argument and not a fact. The
  pieces had each been checked — the feed is fetched and parsed, the signature
  verifies, the installer works when run by hand — but the whole chain had
  never once completed, and the two times it was tried it failed for two
  entirely different reasons: a release carrying the previous version's
  installer, and a job object killing the installer a moment after it started.
- The proof from the other side is one line in the log of the copy that
  replaced itself: `job object: the installer may now outlive the window`.
- What is still not proven, and it is worth keeping in view: none of this has
  happened on a machine that is not this one, and the installer is unsigned, so
  everybody who downloads it meets SmartScreen first.

### 2026-08-11 — The transcript menu names the speakers instead of pointing at neighbours

- Was: *Připsat výše — Roman Povala*, *Připsat níže — Janka Bílá*, *Připsat
  novému mluvčímu*. Now: **Přiřadit mluvčího**, which opens the speakers by
  name, ending in **Nový mluvčí**.
- The two old items were the machine's way of pointing at a person. They named
  the neighbour, so the reader had to work out who "above" was and whether the
  block above is even the right one, before they could agree. The names answer
  the question directly.
- One question — who said this — and every answer in one place. Somebody new is
  the last of them rather than a second item at the top: it is the same
  decision, reached when none of the names fit, and it is where the eye already
  is by then. That arrangement is the owner's; the first attempt had it as a
  sibling.
- Whoever already has the block is left out of the list. Assigning a block to
  the speaker it already belongs to does nothing.
- Each name carries the speaker's own colour on its mark, the same colour the
  panel uses, so the list can be read without reading it.
- The submenu opens in place with a way back, which is how the recording's own
  menu has always worked. A flyout would have to choose a side to open on in a
  window this narrow, and this menu already opens wherever it was pointed.
- Five tests, and both mutations bite: drilling in must not close the menu, and
  the menu must be measured again when its contents change — six names are
  taller than the item they replaced, and one opened near the bottom would hang
  off the edge.
- Files: `src/detail/TranscriptContextMenu.tsx`, `src/Detail.tsx`,
  `src/detail/TranscriptContextMenu.test.tsx`, the dictionaries.
- Verified: `npm run build`; `npm run test` (70, up from 65); `i18n check`. Seen
  on screen in the running application at the first arrangement; the second is
  a one-line move of the same item.

### 2026-08-11 — Correcting entry: it runs on another machine

- The entry above says that none of this had happened on a machine that is not
  the owner's. It has now: Volocal was installed on a second computer and works.
- Left standing, that sentence would have gone on describing a doubt that had
  been settled — and a log that keeps a doubt alive after the answer is in is
  worse than one that never raised it.
- Still open from the same list: the installer is unsigned, so every download
  meets SmartScreen first.

### 2026-08-11 — A speaker can be removed, and one can be added by hand

The panel could make people and join them, and undo neither. Since yesterday
"Nový mluvčí" sits one click away in the transcript's own menu, so an extra
person is now easy to make by accident and was impossible to take back. The
other direction was missing outright: a recording nobody diarized had no way to
name anybody at all, because every existing route to a new speaker began with a
block being handed to them.

- `db::delete_speaker` takes the name off that person's segments (`mluvci =
  NULL`) and deletes the row, both under one transaction — through
  `transaction_unless_in_one`, so it also works inside a larger errand, which is
  exactly what broke deleting a folder yesterday. It is not a merge with nobody:
  a merge needs somewhere to move the blocks to, and there may be nobody there.
- Where the blocks go is where anything diarization could not place already is:
  the panel's list of unnamed passages. Nothing has to be found again, and the
  transcript itself is untouched.
- Asked about only when there is something to lose. A person added by a slip of
  the hand holds a placeholder name and no blocks, and a dialog about that would
  be in the way.
- `add_speaker` already existed for the transcript menu; the panel now calls it
  on its own and focuses the new name field with its placeholder selected, so
  the first thing typed replaces "Mluvčí 3" instead of following it.
- The improved document goes stale on a removal for the reason a rename makes it
  stale: the names are in it.
- Placement, after three passes with the owner: a trash can in each row, 26 px
  and grey until pointed at; and "+ Přidat" beside "Rozpoznat mluvčí" in the
  heading rather than under the list — the sidebar is a column of three sections
  and a row spent here is one to scroll past to reach the other two. The add
  button wears `.sidebar-text-action`, the notes section's own, with 7 px of
  side padding instead of 9, which is what keeps both actions on one line in a
  283 px card.
- The trash can moved into `icons.tsx`: the recording menu drew it already, and
  the registry exists so one idea keeps one drawing.
- Files: `src-tauri/src/db.rs`, `src-tauri/src/commands/backups.rs`,
  `src-tauri/src/main.rs`, `src/api.ts`, `src/Detail.tsx`, `src/icons.tsx`,
  `src/RecordingActionsMenu.tsx`, `src/css/03-detail.css`, the dictionaries.
- Verified: `cargo test` — two new ones, that a removal leaves the words and
  takes only the name, and that it survives being run inside somebody else's
  transaction; `cargo clippy -D warnings`; `cargo fmt`; `npm run build`;
  `npm run test` (70); `i18n check --strict`.

### 2026-08-11 — The wording of that question, as the owner set it

- `detail.speakers.removeText` was `Mluvčí {name} zmizí ze seznamu. Text zůstane
  v přepisu, jen bez jména.` It is now `{name} zmizí ze seznamu mluvčích. Text
  přepisu zůstane beze jména.`
- The name carries "mluvčí" into the sentence on its own, so the word in front
  of it was saying the same thing twice; and the list it leaves is named at the
  point where the reader needs to know which list. The second sentence lost the
  comma clause the same way — what matters is the transcript keeps its text.

### 2026-08-11 — The plus in the new-folder icon was sitting on the floor

- `folderNew` and `folderOut` draw their mark inside the shared folder shape.
  Both were centred on y 14. The drawer's inside runs from the tab line at 7.5
  to the bottom edge at 19, whose middle is 13.25 — so the mark had 6.5 above it
  and 5 below, and at the 16 px the menu draws it at, that reads as a plus
  resting on the bottom of the folder rather than being in it.
- Both moved, though only the plus was reported: they are two items one under
  the other in the same submenu, and leaving the arrow three quarters of a pixel
  lower would have made the pair look misaligned instead of the single one look
  wrong.
- Files: `src/RecordingActionsMenu.tsx`.
- Verified: `npm run build`; seen against the folder outline it is drawn on.

### 2026-08-11 — An update says what is in it, before anybody agrees to it

Until now the application offered a restart for a version it could say nothing
about. The feed already carried the text — `latest.json` has a `notes` field and
the plugin hands it back as `update.body` — and we threw it away.

- `scripts/release.ps1` takes `-NotesCs` and `-NotesEn` instead of one `-Notes`,
  and refuses to publish without both. Czech goes into `latest.json`, which only
  an installed copy of Volocal fetches; English goes on the release page, which
  developers read. Neither audience meets the other's language, and neither text
  is a translation of the other — they do not want the same thing.
- The two are written by hand from `-Material`, a new pass that prints the
  commits since the last tag and the change log for the days they fall on. It
  summarises nothing on its own: a machine summary of commit subjects is exactly
  how a release ends up announcing "various fixes".
- What belongs in the Czech text, settled with the owner over the first draft:
  short noun phrases naming the gain — `Zpřehlednění nabídky mluvčích` — not
  sentences describing the change — `Nabídka nad přepisem nabízí jména mluvčích
  místo „výše/níže"`. "To nejsou přínosy ale popisy." The reader is deciding
  whether to restart their work; what they want is what improves. The detail is
  not lost, it is in the English text and in this file.
- In the application it is a dialog on the shared `.dialog` surface, not a block
  in the settings panel: it is a question with an answer, and a dialog is what
  this application asks questions with. It opens itself when a check finds a
  version that has notes, and the panel keeps a way to open it again — closing
  it must not mean asking the server a second time to reread three lines.
- `readNotes` turns the text into blocks: a leading dash, star or bullet makes an
  item, a blank line ends a block, consecutive prose lines are one paragraph. No
  markdown library for three bullet points, and nothing here interprets emphasis
  or links. Seven tests, including the two that came from thinking about the
  journey rather than the format: `\r\n`, because the string passes through
  PowerShell, and a paragraph after a list, which the first version swallowed.
- A release with no notes opens nothing and behaves exactly as before. Everything
  up to and including 1.0.4 is such a release.
- Files: `scripts/release.ps1`, `src/settings/updates.tsx`,
  `src/settings/releaseNotes.ts`, `src/settings/releaseNotes.test.ts`,
  `src/css/04-nastaveni.css`, the dictionaries.
- Verified: `npm run test` (77, up from 70); `npm run build`;
  `i18n check --strict`; `release.ps1 -Material` run against v1.0.3..HEAD. The
  dialog itself has no test — what it decides on is `readNotes(notes).length`,
  and that is tested.

### 2026-08-11 — The look of that dialog, settled on screen

Built with a temporary `SAMPLE` release and a DEV-only initial state so it could
be looked at without a newer version existing; both are gone from the code.

- The list is a column of ticks, not bullets: the green mark from
  `.kontrola-znak` at 16 px rather than 22, because there it labels a 40 px row
  of a checklist and here it stands beside one line of type, where 22 is a badge.
- The lead line is `Co je lepší a novější?` — a question, because that is what
  the reader is asking when they are deciding whether to interrupt their work.
  It was `Co se v ní změnilo.`
- **The gap above the list was set three times before it existed at all.**
  `.release-notes ul { margin: 0 }` outranks `.release-notes > * + *`, so every
  `margin-top` written there was overruled and the sentence stayed glued to the
  first tick whatever the number said. The column now owns the gap through
  `gap`, where nothing can outrank it. This is the failure mode the visual
  system warns about from the other direction: a value that changes nothing is
  as misleading as a rule that changes the look with no value changed.
- 14 px, after 22 read as two separate blocks. It is the amount this application
  already gives an explanation belonging to a whole group rather than to the
  line beneath it, which is the relationship a sentence has to the list it
  introduces.
- Class names: `release-notes`, `release-notes-mark`, `release-notes-dialog`,
  `speaker-remove`, `speaker-add`, `speaker-actions`. They were written in Czech
  first, matching their neighbours; the owner's rule is that code is English
  everywhere, and the neighbours are what is wrong, not the rule. A sweep over
  the rest of the application — and over the database schema, with a migration —
  is the next piece of work.
- Files: `src/settings/updates.tsx`, `src/css/04-nastaveni.css`,
  `src/css/03-detail.css`, `src/Detail.tsx`, the dictionaries.
- Verified: `npm run build`; `npm run test` (77); `i18n check --strict`; and on
  screen in the running application, which is where three of these were found.

### 2026-08-11 — Volocal 1.0.4, and the first release with notes in it

- Draft v1.0.4 with three assets: the installer, its updater signature, and
  `latest.json` carrying the Czech notes. Unsigned by Authenticode, as 1.0.3
  was, so a download still meets SmartScreen.
- The first release written with `-NotesCs` and `-NotesEn`. The two texts differ
  as intended: five noun phrases in the dialog, five sentences on the page.
- Nobody updating from 1.0.3 will see the dialog. It is the *installed* copy that
  shows the notes, and 1.0.3 does not have it — the text ships now so the path is
  whole when 1.0.5 is offered.
- Two things about passing the notes to the script, both worth knowing before the
  next release. `pwsh -File script.ps1 -NotesEn "a`nb"` loses the argument: a
  newline inside a value does not survive being marshalled onto another process's
  command line, and PowerShell reports it as a missing argument for the *next*
  parameter, which points at the wrong thing entirely. Called in-session with
  here-strings it is an ordinary string and works.
- And a backtick in a double-quoted here-string is an escape: the closing message
  said `eleases/latest/download/latest.json`, having eaten its own r. Fixed.
- Verified: the draft on GitHub carries the version, the tag, three assets and the
  English body; `latest.json` holds 1.0.4, the Czech notes and a signature over
  this installer.
