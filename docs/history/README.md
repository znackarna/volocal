# Change log

The reasoning behind every decision in Volocal, one file per day, moved out of
`CLAUDE.md` on 2026-08-10 because that file had grown to 630 000 characters —
four times what a session can read — and was being loaded in full before every
task. Nothing was rewritten or dropped in the move.

Read a day when you need the reason behind something you are about to change.
A correcting entry never deletes the one it corrects; it is added after it, so
the later entry wins and the earlier one still explains what was believed at
the time.

| Day | Entries | |
|---|---|---|
| [2026-08-01](2026-08-01.md) | 4 | Precise seeking for long VBR MP3 recordings … |
| [2026-08-02](2026-08-02.md) | 136 | Adaptive readable transcript blocks … |
| [2026-08-03](2026-08-03.md) | 52 | Multiline sidebar notes … |
| [2026-08-04](2026-08-04.md) | 50 | Phase A of the audit: nothing may quietly destroy work … |
| [2026-08-05](2026-08-05.md) | 99 | Recording from the microphone … |
| [2026-08-06](2026-08-06.md) | 41 | The portable copy is called Slobot, and so is the package … |
| [2026-08-07](2026-08-07.md) | 40 | A recording one worker holds is not offered to another … |
| [2026-08-08](2026-08-08.md) | 10 | The unnamed interjections get a list of their own … |
| [2026-08-09](2026-08-09.md) | 4 | The audio the application makes has a folder of its own … |
| [2026-08-10](2026-08-10.md) | 23 | The change log leaves CLAUDE.md … |
| [2026-08-11](2026-08-11.md) | 12 | Volocal 1.0.0, and a signing key that is worth something |
| [2026-08-12](2026-08-12.md) | 33 | The code speaks English, and five releases go out … |
| [2026-08-13](2026-08-13.md) | 40 | A backup taken twice in one second is one backup … |
| [2026-08-14](2026-08-14.md) | 40 | Settings regrouped, then cut to seven one-word tabs … |
| [2026-08-15](2026-08-15.md) | 4 | The empty dictionary is a row to write in … |
| [2026-08-16](2026-08-16.md) | 20 | The cube leaves and the name closes to its own middle … |
| [2026-08-17](2026-08-17.md) | 36 | The mark turns on its side, then the whole application is surveyed … |
| [2026-08-18](2026-08-18.md) | 16 | A presentation page: invented, rebuilt from the application, cut to a column, and the live windows returned as figures … |
| [2026-08-19](2026-08-19.md) | 5 | The one live preview becomes five, driven by hand … |
| [2026-08-20](2026-08-20.md) | 14 | Online import fixed at the chunk that returns 403, and the link it came from is now kept … |
| [2026-08-19](2026-08-19.md) | 5 | The page's one window becomes a carousel of five … |
| [2026-08-20](2026-08-20.md) | 13 | Online import, a setup that says why it failed, and the gains section proving itself … |
| [2026-08-21](2026-08-21.md) | 8 | The gains section on a phone, where it had nothing to show … |
| [2026-08-25](2026-08-25.md) | 41 | The limits section is asked as a question … |
| [2026-08-26](2026-08-26.md) | 1 | 1.2.21, for one line … |
| [2026-08-26](2026-08-26.md) | 9 | The cube comes back out of the panel … |

## What each day was about

**[2026-08-01](2026-08-01.md)** — Precise seeking in long VBR MP3s, and the transcription pipeline's phases.

**[2026-08-02](2026-08-02.md)** — Readable transcript blocks, the local language model — enhancing, summarising, translating — online import, and the first pass over Settings.

**[2026-08-03](2026-08-03.md)** — Interface text moved into a dictionary and the checks that keep it there; notes; the speaker-count question; the embedding model that fixed diarization.

**[2026-08-04](2026-08-04.md)** — The audit: nothing may quietly destroy work. Cancelling, backups, the dictionary page, the transcript context menu, corrections kept beside the text.

**[2026-08-05](2026-08-05.md)** — Recording from a microphone, folders, saving audio, one rule for choosing among a row, and the About tab.

**[2026-08-06](2026-08-06.md)** — The public repository, the installer's branding and Czech, the content policy, and the first speaker measurements.

**[2026-08-07](2026-08-07.md)** — Speaker recognition moved inside the application: DirectML measured, sherpa-onnx dropped, CAM++ through ONNX Runtime.

**[2026-08-08](2026-08-08.md)** — Unnamed interjections, names offered before a run, the sidebar as cards.

**[2026-08-09](2026-08-09.md)** — A folder for recordings, search inside a transcript, WebView2's own shortcuts switched off.

**[2026-08-10](2026-08-10.md)** — The hardening plan, start to finish. The log moved here; the baseline got its Windows numbers; the security documentation was made to match the code; all 21 Clippy findings fixed and CI now refuses new ones; fifteen downloads pinned and compared against a published digest; four oversized files split by what they do; the webview's access to the disk closed to one file at a time — which corrects an entry written the same morning; the interface's first tests; Vite 5 to 8; and a release candidate that has to install and run before it counts. Then the day kept going: Slobot became Volocal, identifier and all, with a migration that carries the archive over; the header took the drawn wordmark; a nested transaction that broke deleting a folder was found and fixed; and the release path got a script, a version command and documents that no longer promise there is no update check.

**[2026-08-11](2026-08-11.md)** — Volocal 1.0.0. By the end of the night an update installed itself end to end — after a release that carried the previous version's installer, and a job object that killed the installer a moment after starting it. The updater's signing key was replaced with one that has a password, which is free before the first release and impossible after it.

**[2026-08-12](2026-08-12.md)** — Every Czech identifier, class name and comment turned English, the database schema with them and a migration that carries each archive over. Five releases: 1.0.5 refused the archive it had migrated and was replaced by 1.0.6; 1.0.8 pointed the application at a folder of browser cache and was replaced by 1.0.9 and deleted. In between: a backup can be restored from inside the application, a failed start writes a report the owner can hand over, and the status dots got their colour back after being grey since 1.0.5. The day ended with one branch instead of five, and a repository cleared of dead files — and of documents that still called the product Slobot and said it had never been released.

**[2026-08-13](2026-08-13.md)** — Pressing *Zálohovat teď* twice in the same second reported *SQL error or missing database* about an archive that was fine. And the archive card arranged the way its owner wanted it: saving a copy on a band of its own, and the note about replacing an archive written as two plain sentences rather than one that explains itself. Then the day turned into the simplification: a three-second gap between the sound and the highlight that turned out to be the turbo model rather than any code, a first run that asks one question instead of four and downloads 1.2 GB instead of 6.4, and Settings taken from seven tabs and 59 controls to three tabs and two dozen.

**[2026-08-14](2026-08-14.md)** — Settings regrouped after its owner looked at yesterday's three tabs and said they do not help: `Vzhled` and `Aktualizace` stand on their own again, `O aplikaci` is `Informace`, and what was left under `Aplikace` is named for what is actually in it — `Složky a zálohy`. Only the grouping changed; every control the simplification removed stays removed. Then a sixth tab, `Nástroje`, for the other half of that subject: what is installed on this machine, where it is kept, and which files the application found. And then the decision the day was really about — one question at the beginning, *rychle nebo přesně*, written down and read everywhere it decides something: which transcription model, and how large a language-editing model is fetched the first time somebody wants a document. The acceleration control is gone and `Nástroje` says what actually ran instead; the language editor's three tiers stop claiming a quality nobody has ever measured. Then the owner at the screen, nine sentences of it: `Jazyková úprava` becomes two cards, `Menší` and `Větší`, with no switch and nothing to switch off — language editing only ever runs when somebody asks a transcript for a document. `Kde přepis běží` becomes `Výkon` and gets its choice back as two cards, `Grafická karta` and `Procesor`, with automatic as the resting state and CUDA against Vulkan decided by the drivers and never asked. Icons on all four choices, and the processor builds of both whisper and the language editor now come down on every machine, so that neither choice points at something that is not there and a broken driver has a floor. `Jazyk nahrávky` gets a card of its own. Then a screenshot with two things struck through in red, and the rule the day had written down but not applied: the card is silent when it agrees and speaks when it does not. The `Používá se` row goes, `Automaticky` becomes a switch under the cards rather than a button beside them, and with nothing left naming a build, `domain.compute.*` goes with it. Then: the by-hand list's group headings say which half of the list a component is in now that both halves hold language models, `Větší` leads the editor cards the way `Přesný` leads the model cards, and the wizard's detected-hardware panels go because the sentence above them already said it. Then two tabs named for both halves of what is on them — `Jazyk a vzhled` and `Výkon a modely` — with `Jazyk aplikace` given a card of its own beside the appearance card, and `Technické podrobnosti` folded away at the foot of its tab the way `Pokročilé` is at the foot of `Přepis`. And last, the tabs cut to one word each on the rule that a row of single words reads as a row of peers and that the single word must be a superset rather than a truncation — then `Výkon` given a tab of its own, making seven: `Přepis` · `Rozhraní` · `Výkon` · `Nástroje` · `Soubory` · `Informace` · `Aktualizace`. With it, the model list stops being an inventory and becomes an offer: picking a model that is not downloaded fetches it, and `settings.model` follows the file rather than the click. And at the other end of the day, the transcript screen: speaker recognition leaves the language-editing dialog for a button of its own — it never was language editing, no model is loaded and nothing is rewritten — and the card it vacates becomes `Vlastní prompt`, an instruction the reader writes themselves. The instruction is kept once per installation and saved as it is typed; its answers are kept per recording, keyed by the wording that produced them, in a table of their own so that regenerating or discarding the improved transcript cannot take them with it. A fourth tab in the preview window shows them, the empty field cannot start a run, and the dialog says in a line what `SECURITY.md` says at length: the model runs in this computer and nothing leaves it. And the day ended in the module listing: every row that can be deleted has a bin, and the ones that cannot wear a lock with a tooltip saying which of the two reasons it is — something is using it this moment, or the application never recorded which files are its own. Both were real: the lock had been asking whether a model is *chosen* rather than whether it is *working*, and `ffmpeg` and `deno` unpack into the shared programs folder with nothing to say which files are theirs, so an installation now writes down what it wrote and deletes by that list. Deleting the chosen model is allowed and never leaves the settings naming a file that is gone. With it: the download ring becomes the mini player's, the group headings get their space above rather than below, the online-video component is named for the audio it actually fetches, the button says where it goes, and the card states what this machine holds — how many components of how many are offered, and the space they take, measured off the disk rather than added up from the catalogue's hand-written sizes. One pair of words for that set wherever it is named, and the interface-language note says `okamžitě`. Then the dialog's grey bands, which were not two grounds under one scrim: the veil is a child of the wizard's `<main>`, so the rule centring that screen's content had clamped it to 620 px and left the window undimmed either side of it. And the listing takes the settings column's 720 px, on the owner's ask — same selector as the veil, two faults, one habit of centring everything a screen contains. And the first run itself, rebuilt: the wizard stops being a full screen that replaces the archive and becomes a larger dialog over it, because the application drawn behind it says *you have arrived* where a screen said *you are blocked*. Two cards stacked rather than side by side, each with room for a real sentence — the name of the choice, the model and what the reader gets in the catalogue's own words, and what it costs on this machine in minutes and megabytes. Above them, one line saying what was actually found here, which meant teaching the application to read its own memory: `total_memory_gb` in `tools.rs`, rounded to the number on the box and reporting nothing at all rather than guessing. And `doporučeno` stops being a badge and becomes a reason, argued from that same fact. The four time estimates are still unmeasured — a first run has no model and no recording to measure with, and deriving them from the new detection would have been a fabricated model wearing the authority of an arithmetic — so the note under the cards now says in as many words that they are a rough estimate and not a measurement. And at the very end, the development port moved a third time — 1421 turned out to be reserved on a second machine, whose Hyper-V blocks begin at 1041 rather than 1440 — with the rule behind the number corrected: a fixed port has to be chosen from outside the dynamic port range, which is where those blocks are handed out from, and not from today's exclusion list, which is what picked both numbers that failed. And the dictionary lost the form above its table: everything the day had spent making that composer look like the rows underneath was the tell that it *was* those rows, drawn a second time — so there is one table now and a button that lengthens it, the unwritten row held in the interface rather than in an archive that must never hold an entry with an empty side, and leaving it what decides it. With it the row grew to 56 px, because with the composer gone its fields are the only ones the card has and a field in this application is 38. Then the two column headings went too — written for a row that was two bare words, and outlived by the cross and the tick that now say the same thing per row without a word — taking the last two things outside the panel with them, the heading rule and the inset that existed so bands outside the panel could line up with the rows inside it. What the headings said is on demand instead: hovering either mark gives the words back, as a title the application draws itself rather than a second accessible name the field beside it already carries. And the row under the table got both its ends — the button to the right where it was chosen rather than where a lone child lands, and at the left the one rule about this table nothing on screen shows, that changes save themselves and an unfinished row is thrown away — the clause naming the moment cut on the owner's word, because when a change is saved is not a question anybody asks and whether it is saved at all is. And the transcript header became a row of nouns: `Vylepšit` is `AI nástroje`, since only one of the three cards behind it improves anything and one of them may be asked to translate, and the speaker pill is `Mluvčí` — which needed a key of its own, because the sidebar's verb was right where it stands and wrong in a header of names. On Aktualizace the line above the check button went: it said the application looks only when asked, which the switch at the foot of the card makes untrue whenever it is on, and a third wording had nothing true left to say that the switch and the button do not already say. The button stays — asking now is a different question from being told at start. And the empty archive asks for svou nahravku rather than a nahravku, which is the difference between a category and the file somebody is holding. Last, the update notice got a way on instead of a description of one: the bar can now carry an action, it does not count down or draw its ring while it does, and the button opens Settings on the Aktualizace tab rather than starting a download from a surface that behaves like an aside. It replaces a sentence that told the reader to look under O aplikaci, which had stopped being true the same day — a route cannot go stale the way a description can. Settings takes an initialTab that beats the remembered one and is never written back to it, because being carried somewhere is not the same as going there. And the preview window shows all four tabs whether or not the improved transcript exists: hiding the three made from it hid the subject rather than the refusal, and a window with one pill reads as broken instead of empty — so the three now share one empty state that names what is missing and offers the choice dialog that makes it, with their variant toolbars down until there is a document to vary. Its heading names the missing step rather than the state, on the owner's wording — a heading that says what was not done points at the button that does it, where one saying what is not there leaves the connection to the reader. Then the day corrected itself where it had been most sure: the custom prompt reads the improved document rather than the timed transcript, and the card offering it left the dialog that makes what it needs. The reason recorded that morning was true in every clause and rested on a premise the owner removed — first there must be an improved transcript, and everything else comes from it. So the fourth tab joined the other three behind one precondition stated once, the dialog is two cards and one button again, four keys went with the card, and the timed transcript stopped being the copy an instruction like shrn to was given about. And the two ways into speaker recognition stopped disagreeing about when it may start: the header pill counted the language model among the things that block it and the sidebar did not, so one expression answers for both now. And the day ended on the number behind most of what reads badly about a long recording: the editor server was started with a context of 8192, the one flag in that block with no reason beside it, and everything else followed from it — the 6000-character pieces, the instruction applied to each piece, the answers stacked up. The window is sized to the source now between 8192 and 32768, one function deciding both it and how much text a request may hold so the two cannot disagree, and an hour of speech goes to the model whole. With it, the summary stopped being handed its own outline: the CAST labels are gone when there is one part and explained when there are several. And a machine that cannot start the chosen model is now told so and offered the one below it, after the failure rather than before it: a threshold weighed against memory would be the invented number tools.rs refuses to invent, and would miss the graphics memory that actually runs out, while a silent switch would rewrite a choice somebody made. The two error codes that mean the model would not load were already there and correctly named, so nothing new had to be measured. That entry claimed a machine failing once would fail always, and the owner caught it within the hour: a load can fail for a reason that is gone a minute later, so the dialog got a third answer that retries the same model and changes nothing, and the copy stopped pretending the application knows which kind of failure it saw. Last, the summary was told to write Czech: a summary is new sentences rather than an edit of the speaker, so its language is the model reaching for the English shapes it saw most of — but the rule is narrow, because half the borrowed words in the output were the speaker own and tidying those would put words in his mouth. And the transcript header's  became  — a plural and a thing rather than a verb, since only one of the three cards behind it improves anything and one of them may be asked to translate.

**[2026-08-15](2026-08-15.md)** — The dictionary's empty state stopped being a sentence about the table and became the table: one unwritten row with both placeholders in it, where `Slovník je zatím prázdný.` had said the only thing a reader can already see and hidden the drawing that teaches what a row is. Yesterday's rebuild is what made it small — the unwritten row already lived in the interface rather than in the archive, so an empty table simply appends it, and `draft` goes on meaning *somebody is writing this* rather than *there is a row*. The two things that row must not do are what the entry is actually about: it must not take the cursor, because a row nobody asked for would move the focus out of whatever the reader opened Settings for, so the focusing ref attaches on the first keystroke instead; and it must not offer a bin, because the standing row cannot be thrown away and a control claiming what it cannot do is the fault the module listing's lock was built to stop telling. Then the entry corrected the argument it had just made: it claimed the row's two placeholders teach what a pair is, and they did not — they held a real mishearing and its repair, `součas DNA` / `součást DNA`, which is an example rather than an explanation. Both field names exist but live in the `aria-label` and the marks' hover title, so the two boxes were the only visible words on that card and were spending them on a sample. They say `Špatný výraz` and `Správný výraz` now — `správný` and not `dobrý`, because a word is not good or bad in itself but right or wrong in this transcript, which is also what the green tick already means. And a mishearing cannot be translated, only re-invented for each language, which the English pair had already proved by drifting into a phonetic joke. Then `Špatný` became `Chybný`, because the argument that picked `správný` over `dobrý` had been applied to one field and not the other: a *špatný výraz* is one badly chosen, where what the dictionary holds is what the transcript misheard, and `chybný` ↔ `správný` is one opposition where `špatný` pulls towards the `dobrý` the field beside it had already refused. Last, the sentence under the model cards stopped describing a model and started naming the reader's act — `Výběr nestaženého modelu zahájí stahování.` where it had been `Chybějící model se výběrem automaticky stáhne.` — with `automaticky` gone because `se stáhne` was already saying it, and `chybějící` gone because a model that has never been fetched is not missing, it is simply not here yet.

**[2026-08-16](2026-08-16.md)** — The isometric cube leaves the interface, and what replaces it was inside the wordmark all along: `olo` is the middle of `volocal` and the smile is already centred under it to the hundredth, so the header does not swap one drawing for another, it crops to what the drawing contained. The full name stands at start and then closes to `olo™`, and the pointer or the keyboard opens it again. In the archive's empty state the same mark is written rather than placed — a pen fills the outline eye, stem, eye, smile, the order a hand takes, with the smile last because it is the point — and three faults had to be found before that read as writing rather than as a smear: a round pen tip reaching parts of the letter the pen had not got to, paths that stopped short of the shapes they filled, and a dash pattern resting exactly on its own boundary so a black tick showed before anything moved. The icon is that mark again, and the blur reported on it turned out to be a missing size rather than a soft drawing: at 150 % scaling Windows asks for 36 px and `tauri icon` makes no 36, so a script now builds the file with eleven sizes and lives in the repository because regenerating without it silently undoes the fix. The lockup measured off the installer art goes with the cube, except the 1.9 px drop that was deleted by mistake and came back as 2 — its reason was never the cube but the ™ lifting the top of the drawing's box and the smile dropping its bottom. Then the hold went from two and a half seconds to ten, which forced two things nobody asked for: the countdown had to be measured from the application starting rather than the header mounting, or anybody moving between screens would be introduced to the name again and again; and the written face had to stop waiting for the header, because the objection was to two things moving at once and during the hold the header only stands. The window title is lower case to match the drawing under it, `productName` is not, because it names the executable and the path the updater looks in. The publisher's mark joins the `Autor` row in `Informace` rather than the header — the day was spent taking a second mark out of that bar, and three colours beside a black wordmark would leave the publisher outshining the product in the product's own window. And the mark leads back to the archive from a transcript as it always did from the archive, where it had been a decorative span with the back button carrying the whole job. Last, the installer's own bitmaps, drawn by their owner: the wordmark on the header, and on the sidebar the written face large with the wordmark under it and the publisher at the foot. With them the cube is gone from everywhere. And at the end of the day the mark stopped holding still: it blinks, it smiles now and then, and it smiles under the pointer and holds while it is there. The wider smile had failed twice and been written off the same day — scaling it changes the radius, which reads as a face moving closer, and the stroked version left an artefact nobody could find. The third attempt did not fix the stroke, it removed it: the smile is a filled path again, an annulus sector regenerated for whatever angle is wanted, so every mechanism the artefact could have lived in went with it. The geometry was refitted while doing so and the old numbers were wrong — the drawing rests at exactly 45° about (27.66, 13.833), not 44.9° about (27.66, 13.81) — and a test now holds the generator to the drawing, which is the check the pen still does not have.

**[2026-08-17](2026-08-17.md)** — While a transcript runs the mark turns ninety degrees and stops being a face: the two `o` become rollers, the stem becomes sheet metal passing between them, and the smile leaves because a machine does not smile until it is done. Nothing is drawn for it — the same four shapes from another side, which is the only reason the idea holds, since anything that had to be added would be a second mark rather than a state of the first. Three things the movement needed: the sheet travels along y in unturned coordinates, because it is the parent's rotation that makes the journey horizontal and along x it would run lengthwise through the rollers instead of between them; it turns about the centre of the drawing rather than of the frame, and comes down by 26.2 / 30.41 because upright the drawing is longer than its frame is tall; and it sets off with the turn rather than after it, the rule being not *do not set off early* but *do not reach the rollers early*. When a transcript finishes the loop is not cut — a pass runs from +26 to −26, so the nose is exactly halfway, and stopping means waiting for that halfway point and dropping the animation there, so the last sheet through stays as the nose. And the smile returns by turning about the centre of its own arc, which makes it slide along the curve it is made of rather than travel in a straight line; it cannot leave by turning alone, being always the same distance from that centre, so the turn carries the movement and a fade carries the departure. It lives in the drop zone, which stands at the top of the archive whether or not the list below it is empty. What is deliberately not answered: the loop says that something is happening, not how far along it is. Then the header's two states were given to the screens rather than to a clock: the archive rests on the whole name and shrinks under the pointer, a transcript rests on the mark and opens under it. The ten-second introduction went with that and had to — on a screen that rests open it would have contradicted itself inside those ten seconds — and nothing of the idea is lost, because the crop still happens on the way into a transcript, where a reader causes it instead of a timer. And a rule about numbering: a release moves by one patch, and a minor is asked for rather than inferred — grading a release with its version number mistakes an order for a grade, when what it contained is in the change log and in this folder. Last, the header inversion was taken back on the owner's word — it did not work under a hand. The argument for it was sound on paper, but a resting state that shrinks when you approach it is a control that retreats, and the archive's mark is a button people aim at, so the one gesture that should have shown more took something away. The mill stays; the record of the reverted change stays too, because a log that quietly removes the decisions it later dropped cannot be trusted about the ones it kept. Then a custom instruction stopped following the reader from one recording to the next — which reverses a decision from 14 August without discarding its reason, because a draft surviving the window being closed is a good rule and an instruction about one interview standing over another is worse than retyping it, since it can be run by accident and answered against a recording it was never about. The refusal *Stahování už probíhá* became a queue, the guard having never been about the run but about the destination — two threads writing one component file — so a duplicate is still dropped per component while anything else joins the end, and one worker rather than several, because gigabytes over one connection do not finish sooner in pairs. A cancelled row then ran for ever, three places having asked whether a row was live as a list of what it is not; the phases are named positively now and in two questions rather than one, because a queued row draws a stop control but must not draw a ring frozen at nought, which reads as a download that has stalled. The silence before a resumed download turned out to be a full hash of the gigabytes a previous attempt had left, running unreported and unstoppable, and it reports through them now — those bytes are downloaded, and a bar climbing to where the last attempt reached is the truest picture available of picking up where it left off. And stopping one download stopped the whole queue, inherited honestly, because one application-wide flag was exactly the right size while there was one download at a time; what a control means now follows from what it says it is — the square on a row stops that row, the bubble's cross the component it names, `Přerušit` in the guided run everything. The blur missing from the installed copy cost four diagnoses — a compositing hint, the WebView2 profile, a renderer without hardware compositing, each written up as fact — and was settled in one difference by a test card put in front of the engine that draws the window: Blink does not implement `-webkit-backdrop-filter`, esbuild had kept only that prefixed form because Vite's default browser list still contains Safari 14, and the belt-and-braces prefix added that morning was the entire fault. What ended it was building an instrument instead of a theory, which should have been the first move on being told *dřív to fungovalo*. Then two reviews were run over the whole application, and the first thing the second one found was not in the day's diff at all: the microphone. A take could be lost whole by two independent paths — the raw WebM deleted before the conversion was checked, and no guard anywhere on the one gesture that ends a recording session — and a take is the only artefact here that cannot be made again, which is precisely what the audit of 4 August could not have covered, the recorder having been built the day after it. The same asymmetry stood three more times, all of it around things nobody can download again: exporting a recording onto itself truncated it, `export_archive` deleted the reader's chosen file before writing the replacement that might never arrive, and restoring or importing an archive under a running transcription wrote that transcription's results into a database nobody was looking at. Four mechanisms in the class the product's own promise rests on came next — an edited segment's subtitles clamped to six seconds with the rest of the span blank, two speakers merged into one caption because a merge fired on exactly the shape it should refuse, a dictionary entry ending in a full stop compiled to a pattern that could never match, and the dictionary rewriting sentences a person had corrected — and then the half-finished things were finished: one Volocal per archive, the English complete at 927 of 927, a stylesheet compiled for the only browser this application has, and a release script that refuses a dirty tree or an installer built from another commit before anything is signed. Its own new line then died on a backspace character standing where a backslash had been written, which is the argument for building a path with `Join-Path` rather than inside double quotes. Last, and on the owner's word, a dropped file is answered by the archive alone: the drag listener had been registered for the life of the application, so a transcript or Settings offered to transcribe a file and then put it somewhere the reader was not. Last of all, the close guard written that morning to stop the window closing over an unsaved take turned out to stop it closing at all: `onCloseRequested` is not a notification but a transfer of the decision — Tauri calls `prevent_close` the moment the webview is listening — and the wrapper then ends the window through `destroy()`, which `core:default` does not grant. So the published 1.2.14 could be shut down only through Task Manager. The listener is registered for the length of a take now rather than the length of the session, and the permission is granted for the one path that genuinely needs it. Neither review could have caught it, both having read a diff that was correct in itself, and closing the window is the one gesture no test performs and nobody tries after a release, because it is how you stop testing. Then the last of the two reviews' list, and the records it said were owed: what 1.2.10 changed in `Jazyková úprava` — seven gigabytes no longer starting on a single press — and the hooks-order crash that the same dialog caused on the way in, which is worth a record precisely because nothing but opening the application could have found it. A dropped connection is now picked up rather than reported, three retries and only a continuation accepted, which is the review's diagnosis without its remedy: hashing before the request would have put the resume bar back at nought, and the bar is what made that wait legible this morning. A panicking worker no longer ends a run in silence, and it names what it did not reach rather than an empty list, which would say everything landed. A release tag lands on the commit its installer was built from, because a draft's tag is made when somebody presses publish and takes the default branch's head at that moment — an hour of drift away from wrong, today. Left deliberately: the one screenshot check, which is the instrument that would have caught all three of the day's invisible faults and is too large to bolt onto a hotfix; an update-check offer on a failed online import, which is a new behaviour rather than a repair; and one sentence saying where the updater key is kept off this machine, which is the owner's to write.

**[2026-08-18](2026-08-18.md)** — A one-page presentation site for Volocal, in Czech, quoting `README.cs.md` sentence for sentence rather than writing a second description of the same product. It borrows the application’s colour tokens and both its typefaces, draws the product window in HTML because no screenshot exists — and makes it play, so that clicking a word moves the playhead, which is the interaction the product exists for. The list of what Volocal does badly is set at the same size as the list of what it does well. Nothing publishes it yet. Then the whole thing again, after the owner said the design was invented rather than built: the windows on the page are now the application’s own markup under its own stylesheet, and all three states of the mark come out of `brandArt.ts` and `Brand.tsx` rather than being redrawn. Then three corrections he made from the screen: a `max-width` in the page reset that made the mark shrink instead of being cropped, the fade added to cover for it, and a window title bar that carried a publisher’s mark and a title neither of which the window has. And then the whole thing a third time, simpler: the reconstructed windows go, and what remains is one reading column — the copy, the application’s tokens and typefaces, and the mark’s real animation in the header and at the foot. And at the owner’s ask the two live windows came back into that column as figures — the same faithful previews, now standing in a simple page instead of carrying it. The day ends with the page at a normal 1180 px width, a headline that is a punchline, and one lesson worth keeping: a page that hosts the application’s own markup must not style bare elements, because `section {}` reaches into the window and breaks its cards. The headline is the owner’s own: *Potřebuješ zápis? Zahoď notes. Zapni noťas.* — and a cartoon Volocal in the hero tells the whole story on a loop, using the mark’s real face and mill rather than a second drawing.

**[2026-08-19](2026-08-19.md)** — The page's one live window becomes a carousel of five, moved by hand and never by itself: the transcript, the archive with a recording running, the *AI nástroje* dialog, the finished document with its four tabs, and the first run asking its one question. Three of them needed the same lesson learnt three ways — a component built for a window behaves differently inside a picture of one: `.dialog-overlay` is `position: fixed` and would veil the page, the blur under the veil had to be copied from the application at its own 3 px, and a dialog sized in `vh` measured the browser rather than the 744 px window and hung its footer outside the frame. The archive window itself had to be recovered from a published copy of the morning's artifact, because it had been deleted earlier the same day and `docs/site/` is not in git — an untracked folder has no undo, and the only reason today's deletion was survivable is that a tool had happened to keep a file. The gains section went through five arrangements and ended on the second one, which is the entry worth reading: rows, then three lead cards over a masonry, then an accordion, then small folding blocks, then a paragraph and a specification, then the rows again with every sentence intact. The owner's verdict on the fourth — *vypadá to jako každý jiný AI cringe* — was right about the genre rather than the execution: a drawing, a bold name and one sentence, twelve times over, is the shape every generated page has. The fifth then fixed the shape by summarising the writing away, which is the more expensive mistake: what keeps a section from reading as generated is the copy, so when the arrangement is wrong, change the arrangement and do not pay for it in sentences. A new section says what the program runs on, with nothing invented — Tauri, Rust, whisper.cpp, CUDA or Vulkan with the processor build always on disk as the floor, and an hour of recording in about a minute on a card, which is `estimatedMinutes` in `SetupWizard.tsx` and the only measured speed claim this repository holds. Recommended hardware in numbers is deliberately absent, because nothing here has ever measured a memory threshold and the wizard says so in its own comment. The copy is the owner's throughout and the headline ended formal — *Zapněte počítač. Vypněte předplatné.* — the informal register having been worth its inconsistency only while it carried the *notes / noťas* joke; and **Zdarma** in the lede is what finally backs a headline that had been telling people to cancel a subscription on a page that never said the program is free. The limits are parked in an HTML comment bound for a FAQ rather than deleted, which reverses 18 August without discarding its argument. And one correction owed from that clean-up: removing the three small screens deleted a line too many and took an opening `<section>` with it, so the markup was invalid to the end of that block while the page went on rendering correctly — a browser closes what an author does not, which is why nothing looked wrong and nothing reported it.

**[2026-08-20](2026-08-20.md)** — *Online služba* answered every link with `HTTP Error 403: Forbidden`, and the link in the screenshot turned out to be a red herring — that video had been taken down, but a 403 on the media data is only reachable after extraction has already succeeded, so the failure was somewhere else and it was everywhere. What located it was asking how much arrives before the refusal rather than whether anything does: about 950 KB of 3.27 MB, at every chunk size, on every video, while a ten-kilobyte ranged request succeeds outright. An address that serves a megabyte and then stops is not expired, blocked or throttled — it is YouTube's PO token condition, an authorisation failure wearing a network error's message. The embedded web player's address carries no such condition, so `player_client=web_embedded,default` goes to yt-dlp, and the order is the whole of it: with the defaults named first the 403-prone address still wins a duplicate format id and the download still dies at a megabyte. The argument is addressed to `youtube:` alone, which matters because what this application supports is what yt-dlp supports and a repair to one site must not be a change to the other 1751. Not done and recorded as such: updating yt-dlp, the natural first guess, which has nowhere to go — the pinned `2026.07.04` is the newest stable release there is; and adding a PO token provider, which is a second runtime service beside yt-dlp and deno for a feature that now works without one. The dialog still shows yt-dlp's English inside Czech copy, and that is the only reason this was reportable at all — a translated *stahování se nezdařilo* would have told nobody it was a 403. The same day's second report is the same lesson from the other side: on a friend's network the first run ends on *Nepodařilo se spojit se serverem* and a four-line huggingface.co URL, which names the one thing the reader cannot act on — the reason was not missing but collected and thrown away, `UserMessage::detail` being shown only where the dictionary has no entry and this code having one, so the better the message got the less it said. The sentence now carries the host instead of the whole path and the innermost cause under reqwest's outermost layer, which only repeats the address; `download.rejected` gained its status code, 403, 404 and 451 being three different problems behind one sentence. Nothing about the cause is guessed at: no mirror, no fallback host, no retry policy, because each of them repairs one candidate and does nothing for the others. What ships instead is the instrument — `scripts/network-check.ps1`, run on the machine that cannot download, separating the name lookup, every address resolved, the handshake and who issued the certificate, the proxy as Windows has it rather than as the environment has it, and last the real 64 KB through the redirect. Two things it had to learn: a `.ps1` is read as ANSI without a BOM, so one em dash in a comment made the whole file a parse error; and an address family is chosen per address, or a machine with no IPv6 route prints eight failures against a host that is perfectly well — a report where half the lines say FAIL cannot be read, and this one is read standing in somebody's living room. And a correction owed the same evening: the release commit `ffdab3a` says it is about the online import and a download that says why, and also contains the whole presentation site and the records of 18 and 19 August. The site had been committed locally and never pushed, so `main` here was a commit ahead of `main` there, a branch taken from it carried that commit too, and GitHub computes a pull request against its own `main` — review, checks and squash all took in work nobody had put up. Nothing is lost and the tree is right; what is wrong is the record. Not rewritten, because `main` is public and the release is built from it, and the rule here for a superseded record is to write the correction after it. The lesson is one command before branching: `git log --oneline origin/main..main`, which is the only view an unpushed commit shows up in. Then two reports an hour apart with one cause under them: delete the transcription model by hand and the application knows and will not say. The card's warning comes from `tools::check`, which asks the disk; the badge in the list it sends the reader to is the catalogue's `required` flag, true of `ffmpeg` and `vad` and of nothing else — and rightly, because no single model and no single whisper build is required, any one of several will do and a per-row boolean cannot say *one of these*. So the two agree until a set requirement goes unmet by a row that is not individually required, which is precisely what deleting the model does, and the screen then says something is missing and refuses to say what among fifteen rows. `ToolCheck.needed` answers it from the same reads that raised the warning, and for the model it names the component that writes the file the setting names — the model the reader had, not the one the machine would have been sold. The second report is the same blindness in the other screen: preselection read the drivers and never the disk, so a machine with a graphics card reopened on *Přesný, 3,0 GB ke stažení* beside a *Rychlý* marked *staženo* — a fine answer to *which model suits this machine* and the wrong answer to *what happened to mine*. And under that the chaos, which was not where it looked: with nothing left to fetch the button is `Zavřít` and calls `dokonci([])`, which writes the quality but deliberately not the model — that is `App.tsx`'s to write from the pending record once `download:complete` says the file landed, and with no download there is no event and nothing written, so the dialog closed while the setting went on naming the deleted file. The first attempt fixed a line that never runs: it went into `start()`, on sound reasoning about `install_bundle` returning `Nothing` and beside the point, because with nothing selected that button is not the one pressed. What caught it was writing the test, which drove the actual control and found the setting still unwritten. And that answers the day's other question — the three faults are covered, four Rust tests and four rendered in the window, all run against the code as it was before the repair and three of them failing there; a test that passes both ways is not a test. The screenshot check is still owed for what a rendered tree cannot see — whether the badge is legible, whether the card reads as chosen — but it was not owed for this, which is what the first version of the entry got wrong. Then the badge marked the wrong row, and that fault was mine too: `settings.model` on the reporter's machine is an empty string — it is written when a download finishes and his had never finished — so the lookup matched nothing and the answer fell straight through to the drivers, marking *Přesný* and three gigabytes as necessary at somebody who had chosen *Rychlý*, could see they had, and had just deleted 575 MB. The answer to the person was in the row underneath: `quality_choice` records what was chosen and survives the model being empty. Three answers now, ordered by how much each knows about the reader — the model the setting names, the choice that was recorded, the drivers last as a guess about the computer made where there is nothing else to go on. It was read off the database in one query rather than reasoned about, which beat three plausible theories about what that field contained; and two of the day's four faults were mine, found within the hour, which is the argument for the loop being this short rather than for slowing down. A review of the whole flow was then asked for and returned eight findings over one sentence: the application kept three separate notes saying *this is the one I asked for* — `pending-model` in `localStorage`, `MODEL_WANTED` in Settings, `startedHere` in the wizard — with three lifetimes, three consumers, and not one of them asking whether the file had arrived. Every paradox was a note outliving what it described: closing the list mid-download wrote the setting for a file still coming down, a first run rescued by *Zkusit znovu* never wrote the model and congratulated itself anyway, a record left by an interrupted run survived a restart to fire on the next unrelated download, and a cancelled one left a card reading *stahuje se…* for ever. The rule that ends it: a note may say what somebody wanted, and whether it arrived is a question for the disk. `resolve_transcription_model` asks it — the setting, then the recorded choice, then any offered model actually here — the same shape as `resolve_editor_model` beside it and for the reason written there, and the card now draws the model that is transcribing rather than the one on record. `pending-model` is deleted rather than fixed: a note that does not exist cannot go stale. Two more, and the second is the lesson: re-queueing never took a component out of `dropped`, so stop-reconsider-press-again ended with the file on disk and the run calling it failed — which no test could catch, because the test's `admit` was its own copy of the accept loop rather than the real one, and a copy is not a model. Fable was then asked to go through the whole application and write a repair plan for somebody else to execute; six items came back and all six were done. **A microphone take existed only as a `Blob` in webview memory until somebody pressed save** — `MediaRecorder` started with no timeslice — so the close guard of 17 August covered the deliberate gesture and nothing else, and a renderer crash, a driver reset, a power cut or a restart from Windows took a two-hour interview with nothing left behind. It is streamed to a shadow file now and offered back at the next start, which is only sound because every deliberate ending deletes that file: save, *zahodit*, and closing the microphone. **An update could walk over a running take** — `downloadAndInstall` ends in `process::exit(0)`, which raises no `close-requested` — so the same loss had a second door, now refused. **The dictionary corrected `text` and not the words**, and the screen, the subtitles and click-to-seek are all drawn from the words: `součas DNA`, its own worked example, changed nothing anybody could see, and the existing test passed only because it hand-built a two-word chunk that real tokenization never produces. **A panicking language-edit worker locked the feature until restart**, the slot being freed on the closure's last line — the defect `WaveformJob` was given a drop guard for, in a second place. **A re-transcription left the previous run's speakers behind**, named people with nothing they had said. And **a damaged row made a recording vanish** from the one screen that says what exists; it comes back as a card naming itself. What is deliberately not claimed: nothing here exercises a real `MediaRecorder`, so that a sliced take still converts to M4A is owed a try by hand.

**[2026-08-21](2026-08-21.md)** — `SECURITY.md` had been cut in half the day before and still opened by explaining its own bilingualism to a reader who can see it, and described the interface as *rozhraní zabalené v programu*, which tells nobody anything. Seven sentences came out, every one of them serving the text rather than the reader: an explanation of why the sentence above was phrased that way, a line praising the document's own honesty, a count of the items about to be listed, a pointer at the heading directly below, and one message said twice. What replaced the worst of them is a single line — *Rozhraní není webová stránka. Je součástí programu, takže se do něj nemá jak dostat reklama ani sledování.* The rule went into `CLAUDE.md`, because this was the third hand repair of the same fault: say each thing once, Czech written as Czech without calques, both halves of a bilingual document identical in content and each written as if a person wrote it in that language, and lead with what the reader gets rather than the mechanism. `docs-check.mjs` already counts headings and points per side, so the parity half is enforced rather than trusted. Then the policy itself was split the way the READMEs are: `SECURITY.md` in English, `SECURITY.cs.md` in Czech, each with the switcher line, because GitHub links the first from the Security tab and whoever arrives there may not be Czech. `docs-check.mjs` had been written for one file with two halves — it counted the CSP quotation twice and compared headings either side of `## ENGLISH` — so it now compares across the pair instead, and the same comparison was extended to the READMEs, which had never had one. Then both were tested by breaking them on purpose, and that is what mattered: the SECURITY pair failed as it should, and the README pair **passed**, because `headings` counted `^### ` while the READMEs use two hashes. It had been comparing nought with nought. A check that cannot fail is worse than no check, because it reads as a promise.
**[2026-08-20](2026-08-20.md)** — *Online služba* answered every link with `HTTP Error 403: Forbidden`, and the link in the screenshot turned out to be a red herring — that video had been taken down, but a 403 on the media data is only reachable after extraction has already succeeded, so the failure was somewhere else and it was everywhere. What located it was asking how much arrives before the refusal rather than whether anything does: about 950 KB of 3.27 MB, at every chunk size, on every video, while a ten-kilobyte ranged request succeeds outright. An address that serves a megabyte and then stops is not expired, blocked or throttled — it is YouTube's PO token condition, an authorisation failure wearing a network error's message. The embedded web player's address carries no such condition, so `player_client=web_embedded,default` goes to yt-dlp, and the order is the whole of it: with the defaults named first the 403-prone address still wins a duplicate format id and the download still dies at a megabyte. The argument is addressed to `youtube:` alone, which matters because what this application supports is what yt-dlp supports and a repair to one site must not be a change to the other 1751. Not done and recorded as such: updating yt-dlp, the natural first guess, which has nowhere to go — the pinned `2026.07.04` is the newest stable release there is; and adding a PO token provider, which is a second runtime service beside yt-dlp and deno for a feature that now works without one. The dialog still shows yt-dlp's English inside Czech copy, and that is the only reason this was reportable at all — a translated *stahování se nezdařilo* would have told nobody it was a 403. The same day's second report is the same lesson from the other side: on a friend's network the first run ends on *Nepodařilo se spojit se serverem* and a four-line huggingface.co URL, which names the one thing the reader cannot act on — the reason was not missing but collected and thrown away, `UserMessage::detail` being shown only where the dictionary has no entry and this code having one, so the better the message got the less it said. The sentence now carries the host instead of the whole path and the innermost cause under reqwest's outermost layer, which only repeats the address; `download.rejected` gained its status code, 403, 404 and 451 being three different problems behind one sentence. Nothing about the cause is guessed at: no mirror, no fallback host, no retry policy, because each of them repairs one candidate and does nothing for the others. What ships instead is the instrument — `scripts/network-check.ps1`, run on the machine that cannot download, separating the name lookup, every address resolved, the handshake and who issued the certificate, the proxy as Windows has it rather than as the environment has it, and last the real 64 KB through the redirect. Two things it had to learn: a `.ps1` is read as ANSI without a BOM, so one em dash in a comment made the whole file a parse error; and an address family is chosen per address, or a machine with no IPv6 route prints eight failures against a host that is perfectly well — a report where half the lines say FAIL cannot be read, and this one is read standing in somebody's living room. And a correction owed the same evening: the release commit `ffdab3a` says it is about the online import and a download that says why, and also contains the whole presentation site and the records of 18 and 19 August. The site had been committed locally and never pushed, so `main` here was a commit ahead of `main` there, a branch taken from it carried that commit too, and GitHub computes a pull request against its own `main` — review, checks and squash all took in work nobody had put up. Nothing is lost and the tree is right; what is wrong is the record. Not rewritten, because `main` is public and the release is built from it, and the rule here for a superseded record is to write the correction after it. The lesson is one command before branching: `git log --oneline origin/main..main`, which is the only view an unpushed commit shows up in. Then two reports an hour apart with one cause under them: delete the transcription model by hand and the application knows and will not say. The card's warning comes from `tools::check`, which asks the disk; the badge in the list it sends the reader to is the catalogue's `required` flag, true of `ffmpeg` and `vad` and of nothing else — and rightly, because no single model and no single whisper build is required, any one of several will do and a per-row boolean cannot say *one of these*. So the two agree until a set requirement goes unmet by a row that is not individually required, which is precisely what deleting the model does, and the screen then says something is missing and refuses to say what among fifteen rows. `ToolCheck.needed` answers it from the same reads that raised the warning, and for the model it names the component that writes the file the setting names — the model the reader had, not the one the machine would have been sold. The second report is the same blindness in the other screen: preselection read the drivers and never the disk, so a machine with a graphics card reopened on *Přesný, 3,0 GB ke stažení* beside a *Rychlý* marked *staženo* — a fine answer to *which model suits this machine* and the wrong answer to *what happened to mine*. And under that the chaos, which was not where it looked: with nothing left to fetch the button is `Zavřít` and calls `dokonci([])`, which writes the quality but deliberately not the model — that is `App.tsx`'s to write from the pending record once `download:complete` says the file landed, and with no download there is no event and nothing written, so the dialog closed while the setting went on naming the deleted file. The first attempt fixed a line that never runs: it went into `start()`, on sound reasoning about `install_bundle` returning `Nothing` and beside the point, because with nothing selected that button is not the one pressed. What caught it was writing the test, which drove the actual control and found the setting still unwritten. And that answers the day's other question — the three faults are covered, four Rust tests and four rendered in the window, all run against the code as it was before the repair and three of them failing there; a test that passes both ways is not a test. The screenshot check is still owed for what a rendered tree cannot see — whether the badge is legible, whether the card reads as chosen — but it was not owed for this, which is what the first version of the entry got wrong. Then the badge marked the wrong row, and that fault was mine too: `settings.model` on the reporter's machine is an empty string — it is written when a download finishes and his had never finished — so the lookup matched nothing and the answer fell straight through to the drivers, marking *Přesný* and three gigabytes as necessary at somebody who had chosen *Rychlý*, could see they had, and had just deleted 575 MB. The answer to the person was in the row underneath: `quality_choice` records what was chosen and survives the model being empty. Three answers now, ordered by how much each knows about the reader — the model the setting names, the choice that was recorded, the drivers last as a guess about the computer made where there is nothing else to go on. It was read off the database in one query rather than reasoned about, which beat three plausible theories about what that field contained; and two of the day's four faults were mine, found within the hour, which is the argument for the loop being this short rather than for slowing down. A review of the whole flow was then asked for and returned eight findings over one sentence: the application kept three separate notes saying *this is the one I asked for* — `pending-model` in `localStorage`, `MODEL_WANTED` in Settings, `startedHere` in the wizard — with three lifetimes, three consumers, and not one of them asking whether the file had arrived. Every paradox was a note outliving what it described: closing the list mid-download wrote the setting for a file still coming down, a first run rescued by *Zkusit znovu* never wrote the model and congratulated itself anyway, a record left by an interrupted run survived a restart to fire on the next unrelated download, and a cancelled one left a card reading *stahuje se…* for ever. The rule that ends it: a note may say what somebody wanted, and whether it arrived is a question for the disk. `resolve_transcription_model` asks it — the setting, then the recorded choice, then any offered model actually here — the same shape as `resolve_editor_model` beside it and for the reason written there, and the card now draws the model that is transcribing rather than the one on record. `pending-model` is deleted rather than fixed: a note that does not exist cannot go stale. Two more, and the second is the lesson: re-queueing never took a component out of `dropped`, so stop-reconsider-press-again ended with the file on disk and the run calling it failed — which no test could catch, because the test's `admit` was its own copy of the accept loop rather than the real one, and a copy is not a model. Fable was then asked to go through the whole application and write a repair plan for somebody else to execute; six items came back and all six were done. **A microphone take existed only as a `Blob` in webview memory until somebody pressed save** — `MediaRecorder` started with no timeslice — so the close guard of 17 August covered the deliberate gesture and nothing else, and a renderer crash, a driver reset, a power cut or a restart from Windows took a two-hour interview with nothing left behind. It is streamed to a shadow file now and offered back at the next start, which is only sound because every deliberate ending deletes that file: save, *zahodit*, and closing the microphone. **An update could walk over a running take** — `downloadAndInstall` ends in `process::exit(0)`, which raises no `close-requested` — so the same loss had a second door, now refused. **The dictionary corrected `text` and not the words**, and the screen, the subtitles and click-to-seek are all drawn from the words: `součas DNA`, its own worked example, changed nothing anybody could see, and the existing test passed only because it hand-built a two-word chunk that real tokenization never produces. **A panicking language-edit worker locked the feature until restart**, the slot being freed on the closure's last line — the defect `WaveformJob` was given a drop guard for, in a second place. **A re-transcription left the previous run's speakers behind**, named people with nothing they had said. And **a damaged row made a recording vanish** from the one screen that says what exists; it comes back as a card naming itself. What is deliberately not claimed: nothing here exercises a real `MediaRecorder`, so that a sliced take still converts to M4A is owed a try by hand. And the presentation page, on its own branch: the ten rows under *Co získáte?* now carry proof rather than a claim, each opening a card of the application's own markup under its own stylesheet. The first build unfolded it under the sentence and grew the row by 106 px, so everything below hopped; the card is out of flow now and slides in over the words — 1107 px with every row shut and 1107 px with all ten open, and a floor of 128 px on every row so the last two, which carry one line where the rest carry two, stop holding their card six pixels off the hairline. Three faults worth the day: `--surface` is defined nowhere in this page or in the application, so the card's background dropped and it stood transparent for a day without anybody seeing it, the sentence underneath fading at the same moment — the exact failure `CLAUDE.md` warns about, met in the wild; `.rows li` is a descendant selector, so every list inside a fragment took the feature row's 24 px and its hairline and three file names came to 217 px in a 92 px card; and a `.status-mark` takes its 9 px from `flex: 0 0 auto`, so stacked in a grid slot it is an inline span again and draws nothing. *Titulky sedí na obraz* is one animation and not two — two identical copies, the upper one clipped by an animated inset, so a peak and the word above it light in the same frame and no clock can disagree with another. A sixth carousel view shows the recorder mid-take, its spectrum drawn as bars each on its own timing because one shared animation makes a strip breathe as a block, which no microphone does. The owner renamed the three lead cards to two-word titles; what that costs is written in the day's entry, and it was said before it was applied. The numerals over *Třikrát a dost* went through two forms and came off entirely: the second, a figure in the page's own background colour raised by nothing but its shadow, looked good and said nothing. None of it is in `index.html` yet.

**[2026-08-21](2026-08-21.md)** — The ten rows under *Co získáte?* carry a card of the application's own markup, and below 900 px one line of the stylesheet threw all ten away: a phone got the sentences and no proof, at the width where most of a shared link is read. The reason the card was an overlay is that a card which unfolds moves everything under it — and that is a two-column reason, so in one column it comes into the flow and stands under the sentence that makes the claim, with no transition and nothing to discover. The room was already there: the fragments were drawn for a 526 px column and one column is 820 px at 899 and 565 px at 620. Two of them needed the width taken seriously — the header bar drops its mark below 620 px, the page wearing it two screens up, and lets the recorder pill take what is left instead of a fixed 290; the eight format sheets become a share of the card capped at the 52 px they are drawn at, which also repairs them between 900 and 1150 px, where they had been cut at both ends since the day they were built. Three more faults came out of measuring rather than looking. `min-height: 128px`, the floor that lets a card clear the hairline evenly, was never turned off with the card, so each row kept 48 px of empty space under text that measures 79 — ten rows and 480 px of nothing. The three looping cards start on `pointerenter`, which a phone never sends, so *Sledovaná složka* stood empty with its file rows at `opacity: 0`; an `IntersectionObserver` starts each one as it arrives, once and never again, because a picture that replays on every scroll past is a flicker rather than a proof. And the page scrolled sideways at every window narrower than 1276 px: `.carousel-viewport` took 48 px of padding for the window's shadow and gave it straight back as a negative margin, against a 32 px gutter — the overhang is the gutter now, at both breakpoints, because the negative margin makes it the overhang and the overhang has to be something the page can afford. At 320 px the header wraps rather than losing one of its three places to go. The ten cards are `inert`: they held 19 buttons of the application's own furniture in the tab order behind an invisible surface, and what that costs is the keyboard's `:focus-within` way of opening a card, which is the right trade because the sentence carries the claim. Measured at fifteen widths from 320 to 1440: no sideways scroll anywhere, no card cut except a hairline at 320, and the desktop untouched — 1131 px, the same as before. And then, from the owner's own screen, the second half of the same lesson: 92 px was the card's height because an overlay that grew would have covered the row above it, and in the flow that reason is gone — three cards were cut off at the bottom on a phone, the note by 50 px at 320. It is a floor from there down and nothing is cut at any width. The file names in *Sledovaná složka* were the one thing height could not help, a name being one line by nature: the rows take the list's width and truncate, which needed `min-width: 0` said out loud, because the min-content of an unbreakable name is the whole name. Parked with his blessing rather than decided: with all ten cards open the section is 3603 px on a phone against 1131 on a desktop, and he would rather they opened on a tap — the argument on both sides is in the day's entry. And one word from a comment on the published page: the third lead card, *Inteligence po ruce*, becomes *AI funkce po ruce* — the third name that card has had, and the first one that names what a reader is looking for rather than a category. And the numerals come back to *Třikrát a dost* on the third try, as a 5 × 7 dot-matrix panel that rolls through other digits and stops on its own — the number itself written in the markup, so a browser that never runs the script still shows it. Two faults came out of checking rather than looking: the script's flattened bitmap for the digit 3 disagreed with the markup it was supposed to end on, which nothing but comparing the two representations would have shown; and a brace left orphaned by the counter rule removed on 20 August had been standing in the stylesheet ever since. Then, on 22 August in the same file, the owner's own line for *Sledovaná složka*: two sentences become one, and what goes is the half describing a setting rather than a gain — *automaticky* now stands where *sám* used to sit at the end of the second sentence. And on 25 August the stack section's heading becomes *Jak a na čem běží* — it had always answered two questions and named one, and *how* is the half a reader of this page came for. And the page is made ready to go out: the limits stop being an HTML comment and become a FAQ of five `<details>` — his sentences as the answers, the questions written for them — the version stops saying 1.2.15 and is rewritten at deploy time from the latest release tag, with the substitution asserting it found exactly two mentions so a reworded sentence fails the build; and `.github/workflows/site.yml` serves `docs/site/` to GitHub Pages on every push, with no build step, because the page is static and its assets are relative. Pages itself still has to be switched on by hand. The page then went live at znackarna.github.io/volocal after two refusals worth keeping: `configure-pages` cannot create a Pages site however `permissions` is written, because that is an admin call — its own error message recommends the flag that does not work — and a Pages environment lets only the default branch deploy, so `site` had to be added to the environment's branch policy. Checked from outside afterwards: every asset answers 200, the three tool files answer 404, and both version mentions read 1.2.20 from the release tag rather than from anything checked in.

**[2026-08-24](2026-08-24.md)** — The presentation page comes back into the repository. It was moved to a branch of its own four days earlier because `release.ps1` refuses a working tree with uncommitted changes, and a half-finished paragraph on the page had blocked a release three times in one day — which is the release script's rule rather than a reason to split a repository. What the split cost was visible by the time the owner asked about the two branches on GitHub: fourteen commits ahead and four behind, with the page reading `brandArt.ts` and `Brand.tsx` from the application and no way to keep up but by hand. Brought back by a merge rather than a copy, so the fourteen commits keep their history; `site.yml` triggers on `main`, and `docs/site.md` goes, having existed only to say where the page had gone.

**[2026-08-25](2026-08-25.md)** — *Co nedělá dobře* becomes *Jaké má limity?*, at the owner's ask from a comment on the published page: the section is a FAQ and every heading under it is a question, so the one over them should be too. What it costs is written down rather than glossed — the old heading admitted a fault in the program's own voice and bought trust with it, where the new one asks neutrally and lets the answers, which are unchanged, do the admitting. Also checked and recorded here: the `site` branch was merged into `main` and deleted, and the page survived a merge that a following commit says had *quietly left deleted* part of it. The first FAQ answer is then rewritten in the owner's words, and it brings a claim the repository does not hold: *časové značky jsou přesnější u robustnějšího modelu*. `dtw_preset` gives every model an alignment preset, and the only alignment measured anywhere here is large-v3's — 881 words, median offset −0.08 to +0.03 s — with no fast-model run to compare against. The claim is very likely true and it is his to make; the claims table now carries it with *nothing here measures it* written where the source goes, which is the rule that also keeps recommended hardware off the page. And the second FAQ answer goes in in his words too, carrying a contradiction with the third that is recorded rather than quietly fixed: *Volocal bez problémů rozliší jednotlivé mluvčí* stands two rows above *Pět lidí v jedné místnosti sloučilo jednu dvojici*, and a limits section that overclaims in one sentence and takes it back in the next is trusted for neither. The repair is two words — drop *bez problémů* — and it was offered rather than applied. The FAQ's lede becomes *Volocal je mocný pomocník, ale má svá omezení*, which changes what the sentence does rather than how it reads: the old one told the reader to read this now or meet it later, and the new one opens the page's one honest section by complimenting the product — the register the owner himself called *AI cringe* on 19 August. A merged version keeping his opening clause is offered in the entry. The third answer follows, with one comma added to close a clause the comment left open — and with it the contradiction flagged an hour earlier disappears by the wrong door: the second answer still claims *bez problémů*, and what disagreed with it, the measured *pět lidí v jedné místnosti sloučilo jednu dvojici*, is simply no longer on the page. The entry then names the pattern under all four of the day's edits once and drops it: every specific left the section — a second, five people, one merged pair — and every replacement is a degree word, which is the difference between a limits page a reader believes and one that performs humility. The fourth answer needs no note: it keeps the ten, which is the number that makes it concrete, and says the same thing in plainer Czech; only the clause *a je to vidět* — that the queue is on screen — did not survive, recorded so it can be put back rather than as an objection. Two things the owner then spotted on the published page, both in `<head>` and neither visible from inside it: the tab still wore the Značkárna cube, the mark this project stopped using on 16 August, and is now the application's own icon as a data URI — a file would leave `bundle.py`'s single-file copy with none. And a pasted link still sold *Zahoď notes, zapni noťas*, the informal headline retired on 19 August, making the description the one sentence on the whole page still tykající; it is the page's current copy now, with Open Graph and Twitter tags added, and `og:image` deliberately absent because a preview card with a bad picture is worse than one with none. A correction the same hour: that new description was mine, and the owner read it back as *slátané*. The entry that introduced it had said what was wrong with it without noticing — *the page's own copy, recombined rather than invented* — and recombined is the fault, because every clause was his and the joins were not, and the joins are where writing happens. It is his sentence now, with one word put back: he wrote *přepisuje mluvené přímo*, and *slovo* is the noun his own lede uses two lines away. And the day's first entry is corrected by its own invitation: the stack heading is *Na čem běží*, the short form, the owner having said which of the two he meant. And *Opravíte, co nesedí* gets a shorter sentence of his: *V textu můžete vyhledávat a opravovat celé věty nebo jednotlivá slova* — four words fewer, the double *v něm* gone, the same two things named. The day ends on a decision deferred rather than taken: the site is Czech only — the automatic language choice the owner had in mind is the application's, not the page's — and an English version was measured at 1 600 words that split 915 already sitting in `src/locales/en/` against 685 that would have to be written. His idea of loading translations from JSON is right one step earlier than he put it: in the browser it would hand crawlers an empty shell and cannot swap `og:` tags at all, but as a build step it renders both languages as plain static HTML and turns agreement between them into a comparison of key sets. Parked until he has spoken to a colleague; the entry is the briefing. And the header gets a menu on the phone: from 620 px down the two quiet links go behind a button while *Stáhnout* stays out, because the stylesheet has said since the page was written that only one of the three places to go is what the page is for. The panel is the `<nav>` itself rather than a copy, so a phone and a desktop cannot disagree about what is on it, and it collapses only under a class the script adds — with no JavaScript the links stay in the header, which is the right way to fail. Escape, a pointer elsewhere, following a link and crossing the breakpoint all close it. Measured at seven widths from 1280 to 320: the button appears at 620 and not at 621, the panel lands inside the viewport everywhere, and its links are 44 px where the header's are 14 px of text. Last, the three steps under *Než začnete* number themselves in their own headings — *1. Stáhněte instalátor.* — and `.step-no`, an 11 px micro-label that treated the number as a badge, is gone. Unlike the numerals twice removed from *Třikrát a dost*, these carry information a reader needs: the three blocks are a sequence, so the number belongs in the sentence. And the names on the format sheets finally shrink with the sheets: when those became a share of the card on 21 August the writing on them stayed at a fixed 10 px, so on a 390 px phone a 28 px sheet carried a third of its own width in text. The exact unit for it, `cqw`, was tried and does not work here — `container-type` on either the sheet or the row collapses the row to zero, because the sheets are sized in `%` of a container then told not to size itself from them — so it is `vw`, with the reason written into the stylesheet. Last of the day, from a screenshot: the pills in the preview windows had a heavier stroke on their rounded ends than on their flat edges, and the cause was the scale — the window is laid out at 1180 px and scaled to the column, and without a compositing layer Chrome rasterises it at 1:1 and resamples the texture down, which a straight edge survives and an arc does not. Photographed four ways at device scale 1 rather than argued; `will-change: transform` on the stage makes Chrome rasterise at the scale it shows, and `zoom`, which also works, was rejected because it reflows the window and not reflowing is the whole point of the preview. And a `">` in the corner of every bundled copy since the morning, spotted by the owner on the published artifact: the page was never wrong, `bundle.py` was. `[^>]*` is the obvious way to say *the rest of this tag* and holds until an attribute contains a `>` — the new favicon is an inline SVG in a `data:` URI, full of them, so the strip stopped inside the drawing and left the rest of the mark standing in the document as text. Both patterns take a quote-aware rest now, and the script asserts the invariant it had always relied on: between the title and the first `<style>` nothing may remain but comments. And a correction to the menu built an hour earlier: *Stáhnout* goes in with the rest. The argument for keeping it out — that of the three places to go only one is what the page is for — is true of the header on a desktop and beside the point on a phone, where the hero's own full-width download button sits two hundred pixels below it; the copy in the header bought nothing and cost the line it stood on. And the three links in the header get one hover instead of two: the quiet pair darkened and the download underlined, because darkening something already at full strength does nothing — so both now do what the footer has done since the page was written, darken and underline, with the resting colour left to carry the hierarchy. In the panel all three light up and none underline, a row being a target rather than a link in prose. The measurement corrects the previous entry's claim that the download keeps a distinct colour in the panel: it does not, and should not. The hamburger then loses its ring, at the owner's ask: it had taken the theme switch's geometry, which is the right dress for a symbol nobody recognises and a hamburger is not one. Three lines at 2 px on screen, the weight of the wordmark's own strokes; hover steps back to 60 % because a black icon has nowhere to darken to, and the open state now rests on `aria-expanded` and the panel itself since there is no border left to change. The theme switch beside it is still a circle, which is a real inconsistency and is left for the owner to settle. The menu then reads as aligned to nothing, and the owner is right twice over: the button sat second from the right with the theme switch beyond it, so it could not look like the panel hung from it, and the panel was positioned against the page wrapper, whose 20 px gutter put its edge out in the margin past the button and past every line of text. The button is last in the row now and the panel hangs off `.top-right`, whose edge is the button's own — measured at 0 px difference on all three widths. The lines also lose their rounded caps, three lozenges not being three lines. The panel still covers 42 % of the headline, which is named again rather than quietly left. And the theme switch moves into the menu, which was asked for when the menu was built and settles the bare-lines-beside-a-ring inconsistency by taking the ring inside: in the header it is the same circle, in the panel a row with the label left, the icon right and a hairline above saying the rows over it go somewhere and this one changes something. A mistake worth keeping came with it — the first panel rule showed every `.theme-label` it could reach, so the row read *Tmavý motiv Světlý motiv* on two lines, and the repair was a narrower selector rather than a stronger one. Then a correction to that row: its highlight was square across the top, on the written argument that the hairline above squares those corners off — which a hairline does not do, being a line with no opinion about the shape under it. What it needed was somewhere for the tint to stop, so `background-clip: padding-box` keeps the fill inside the border and the highlight is the same 8 px the links have. The shape of that mistake is worth naming: it rendered exactly as written, and what was wrong was the reason in the comment, which nothing checks and which outlives whoever believed it. And the FAQ stops jumping open: a `<details>` has been a switch everywhere for twenty years because CSS could not animate to a height it had to work out, and `interpolate-size: allow-keywords`, `::details-content` and `allow-discrete` between them now can — all three checked as supported before a line was written, and all three behind `@supports` so a browser without them keeps the instant open it has today. The chevron moves onto the same 0.26 s, two clocks on one gesture being how a disclosure feels loose. Both end states verified from the rendering; the motion itself was not, headless Chrome not advancing the transition clock under virtual time. Last, the hamburger turns into a cross: one `<path>` with three moves in it becomes three `<line>` elements, because each has somewhere different to go, and every rotation is about the line's own centre — `fill-box` would give a horizontal line a bounding box no pixels tall, and the middle of nothing is not somewhere to turn about. Under the pointer the outer two draw in to 82 %, the button's old whole-icon fade going with it, and the open rules outweigh the hover ones so a pointer on an open button cannot pull the cross apart. Five end states measured with the transition removed, three of them rendered; the motion between them was not photographed, for the third time today and the same reason. And the lede of *Na čem běží* becomes *Pořádné GPU z něj dělá raketu, ale poběží i na starším CPU* — better than the *bezpečný a bleskurychlý stack s důrazem na udržitelnost* it replaces, which claimed safety the section never returns to and sustainability nothing in the repository is about. Half of it is measured: `estimatedMinutes` puts an hour of audio at 1 minute on a card against 8 on a processor. The other half is not — the processor build is always on disk, but how old a processor still finishes in a usable time has never been measured, so the claims table carries it with *nothing here measures it* where the source goes. Then the stack sentence comes back in front of the hardware one as a framing — *Bezpečný a bleskurychlý stack je základ* — without *udržitelnost*, which was the half nothing in the repository supports; the correction landed where it should have and the rest is the owner's ear. And the hamburger stays hovered after a tap on a phone, which is sticky hover and not a fault in the morph: a finger has no hover, so a touch browser gives the last-touched element one anyway and leaves it until something else is touched — the animation only made an old condition visible. All five hover rules in the header are behind `(hover: hover)` now, counted from the rendered stylesheet rather than the source, and they still work where a pointer exists. The same condition holds for every other hover on the page and is named rather than swept, so that doing it is a decision. And the theme row turned out to be set in Arial: a `<button>` does not take the page's type, it takes the operating system's, so the label stood in Arial beside links in Geist at the same size and weight. `font: inherit` fixes it. It got in because the row was styled by setting the two properties being thought about, `font-size` and `font-weight`, and not the one that had never needed saying on this page — it is the only button here with words in it, the download being an `<a>` dressed as a button. And the panel for companies stops offering Volocal and starts offering *ještě silnější AI nástroje do privátního cloudu* — his line, and a change of what is being sold rather than of how it is worded, which is worth knowing on a page whose every other sentence is about the program. It is also the first place on the page to admit an upper limit exists, which is true and which the section about hardware never quite says. And the FAQ chevrons get a middle and an edge: `align-items: baseline` had been pushing a pseudo-element with no text in it onto a baseline, and `padding: 16px 0` left the arrow balanced on the end of the hairline. Centred, and 8 px in from the right while the left stays at nothing so the question keeps the edge the heading stands on. The bigger number is recorded rather than acted on: at 1280 px the arrow is 960 px from the end of its question, because the list spans the column while the answers under it are held to 62 ch. The panel for companies then gets its third wording of the evening — *Chcete silnější modely, napojení na vaše dokumenty a privátní AI uzel ve vaší síti?* — which names the offer better than either before it and ends on *ve vaší síti*, settling the small wrongness of a page about nothing leaving your computer offering a cloud. Two arguments were made and lost, and are recorded as such: the audience is no longer named, and the page's own words for two of these things, planted in the carousel three sections above, went unused. And *vaše dokumenty* becomes *firemní dokumenty*: it takes a repetition out of a sentence that said *vaše* twice, it says who owns the documents in a paragraph that no longer names its audience, and it lands on the adjective the page already uses three sections above in *firemní knihovny znalostí* — the argument for the page's own words having been lost an hour earlier, the adjective came back by itself. The panel then reaches its fifth and best wording, and the context behind it is recorded because it is not in the sentence: the panel is a door to Značkárna and Volocal is the demonstration, which answers the question the page raises on every screen and never answers. Three things improved with it — a verb in the first person, so the panel offers rather than advertises; *s pomocí lidské i umělé inteligence*, which says there are people here and disposes of the suspicion the word AI had been collecting; and the loss of the concrete nouns added an hour earlier, which is the price of a door instead of a specification. A sixth wording then takes the qualifier off both halves — *Potřebujete silnější nástroje? Propojíme lidi, obsah a design napříč vaší firmou.* — trading what the offer is made of for how far it reaches, and letting go, within the hour of adding them, of both *ve firemní síti*, the panel’s one tie to the privacy argument the rest of the page is strung on, and *s pomocí lidské i umělé inteligence*, along with the word *AI* itself, which leaves the suspicion nothing here to attach to. The page then came out broken, because it was published as `index.html` rather than as the bundle: the host wraps what it is given in its own skeleton, so the raw file arrived as a second document nested inside the first with its fonts pointing at a disk that is not there. `docs/site/README.md` now carries that as a rule with the failure attached, since the instruction on its own had been in front of the session that ignored it — and the republish before it had silently failed too, leaving the reader two wordings behind what the log claimed was deployed. Two words then land the panel: *silnější* becomes *chytřejší*, trading a measure for a difference in kind — nobody wants a stronger tool, they want one that understands the job — and *vaše* moves off the building onto the people, which is what the sentence was about all along. Both of those come straight back out in a seventh wording — *Potřebujete silnější nástroje? Propojíme lidi, obsah a design napříč firmou.* — because the measure is the point for a reader whose tools already do the job and ran out of room, and because *vaše lidi* had Značkárna talking about a company’s staff as belonging to it; what stands is the shortest the panel has been and the first version carrying no possessive at all. The owner’s isometric cube then goes in front of that sentence — before it rather than above it, because the panel is one row and a drawing on its own line would make it a card; the mark and the sentence are wrapped in one group so the button cannot land between them when the row wraps, and the cube is sized by height because it is 0.866 as wide as it is tall. It is the first full colour on an otherwise neutral page, which the panel can carry precisely because it is the one place not about the program. An hour later the cube gains a white circle on the green face and a square in the blue top, and the export that brought them draws the group twice — which this session read as an accident, dropped, and so published a triangle on the red face that the second copy existed to hide. The upper copy is how the drawing says *not this one*. The corrected file is pasted in whole, duplicate and all, with a note to do the same next time: an export is the designer’s, and tidying it is how a drawing quietly stops being his. And the last entry of the day leaves the page for the application: switching between `Procesor` and `Grafická karta` on the `Výkon` card flashed a red refusal that took itself back a fraction of a second later. Neither value was wrong — the stored setting changes on the press and `check_tools` answers a round trip later, so the card was reading a new question against an old answer, and reading the difference is exactly what the card is for. The question is kept beside its answer now, and the rule moved to `compute.ts` where the same idea already lives twice over; `src/compute.test.ts` is new and fails in both directions when the pairing is removed. And the fix is then verified where the fault actually lived: the entry before it pinned the rule, which had never been wrong about the values handed to it, so a second test renders the real screen and presses the real cards with `check_tools` held open by hand. Its third case is the one that keeps the repair honest — remove the pairing and the two stale-window cases fail while a pick the machine genuinely cannot honour still speaks, which is what says the card was made to wait rather than silenced.

**[2026-08-26](2026-08-26.md)** — The owner’s isometric cube, drawn and placed in the panel for companies the evening before, is taken back out at his word: the group around the mark and the rule that sized it go with it, so the panel is the markup it was before and nothing is left standing that has nothing to hold. Three ways it could have assembled itself — faces arriving along their own axes, the cube built from the bottom with the lid last, and the empty cube whose white shapes grow from their own centres — were built as a page to choose from and never chosen, and are not in the repository. Then a release learns to redeploy the page: 1.2.21 went out at 07:48 and the published page offered 1.2.20 all morning, because the number is read from the latest release at deploy time while the deploy only ran on a push touching `docs/site/**` — the version was automatic, the moment to apply it was not. `site.yml` now also runs on `release: published`, and `version.ps1` moves the page’s own two numbers with the seven other places a version is written down, because the file’s copy is what a reader sees when the page is opened from disk or handed over as a bundle. And the FAQ animation turns out to have been Chromium-only: `interpolate-size` and `::details-content` are Chrome’s and Edge’s alone, and the `@supports` around them meant the page slid open for whoever built it and jumped for Firefox and Safari without saying so — a fallback nobody can see is indistinguishable from a fault. The panel’s height is now measured and animated in script, with the fill, the backstop timer and the token each written down beside the line that needs them; without JavaScript it is still a `<details>` that opens at once. And the panel for companies keeps its button on the right when the row wraps: `space-between` only spreads a line with two things on it, so the rule that puts the button at the right edge is the one that stops applying the moment the button is alone on its own line. The row then stops wrapping at all above 620 px: the sentence was holding its one-line width and pushing the button onto a line of its own, so it now gives way to two lines with the button beside it — one offer with a way to answer it, rather than two blocks stacked. Then the veil over the carousel windows stops reading the backdrop: `backdrop-filter` inside the scaled, promoted preview layer samples a texture rasterised in tiles, so hovering an arrow repainted part of the region and left the tile seams standing as straight lines — in Firefox and Chromium alike. A `filter` on the window is rasterised once and reads nothing back; the rule asks each veil which siblings are the window rather than naming `.app`, because in two of the four slides the veil lives inside it. The day ends with the page in English at `/en/`, made from the Czech one at deploy time rather than kept beside it: 4 600 lines of layout would not disagree about a word, they would disagree about the page, so only the sentences are doubled. Keys are the Czech sentences, so rewriting one orphans its translation loudly in `docs:check`; the words of the invented transcript carry their English on their own spans, the version is a placeholder so a release cannot orphan two sentences, and the four strings the scripts say out loud read the document’s language for themselves. Nobody is redirected — a switch in the header, `hreflang` on both, and a shared address opens what it says it opens. The English lede is then rewritten at his reading of it: Czech drops the subject, so its three bare verbs land like a drumbeat, while the English had put `It` back in front of each one — every sentence translated and the paragraph still worse. English drops the subject too in that register, and *bez ztráty* is a loss rather than a surrender. The language switch then leaves the header for the footer: *Stáhnout*, *Firmy* and *Kontakt* are places on the page and *English* is an instruction about it, standing in the same voice — downstairs it sits with GitHub, the releases and the security policy, which is where a reader looks for it anyway.


**[2026-08-26](2026-08-26.md)** — 1.2.21, carrying one change an installed copy can see. The owner has 1.2.20 and the `Výkon` card still flashes there, which is what the tag says it should: `v1.2.20` predates the repair by five days. Everything else since is the presentation page, which nobody running the application ever sees, so the notes say the one thing and stop.