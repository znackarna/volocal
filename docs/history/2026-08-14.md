# 2026-08-14

### 2026-08-14 — Three tabs back up to five, because three did not help

Yesterday's entry ends with Settings at three tabs — `Přepis`, `Aplikace`,
`O aplikaci`. Jakub opened that arrangement and turned it down in one sentence:

> Ty tři karty nepomáhají. Karta vzhled by měla být zvlášť. A O aplikaci bych
> dal Informace a Aktualizaci bych dal zvlášť.

That is the reason for this change, and it belongs in the record: nothing was
measured, nothing broke — the person the screen is for looked at it and said the
grouping does not answer the question he arrives with. So the grouping changed
and only the grouping. **Every control the simplification removed stays
removed**, because those were dead (`Hledání změn mluvčího`, whose setting no
Rust code reads), harmful (`Počet mluvčích`, which silently overrode `Nevím`),
or derivable from a neighbour (`Řádkování`, `Sledovat složku`). None of that was
about tabs.

**The tabs are `Přepis`, `Vzhled`, `Složky a zálohy`, `Aktualizace`,
`Informace`.**

- `Přepis` is untouched: the model, the recording's language, the speakers, the
  language editing, the dictionary, the band saying what is downloaded, and
  `Pokročilé`.
- `Vzhled` is the appearance card taken back out of `Aplikace` — interface
  language, theme, transcript type and size, and the live sample.
- `Aktualizace` is the check, `Co je nového`, `Stáhnout a nainstalovat` and the
  automatic-check switch, lifted out of the About page. It is the one thing on
  this screen that is neither a setting nor a fact: it is an errand, one press
  of which closes the application and starts an installer. It sat under the
  licence list, which is the last place a reader looking for a new version
  looks.
- `Informace` is what `O aplikaci` was, minus the updates: what the application
  is, what it can do, and what it stands on. Nothing on it can be pressed.
- `Aplikace` is `Složky a zálohy`. **This is the one decision the owner did not
  make for us.** With appearance gone, what was left under that name is the
  recordings folder, the watched folder, the archive's backups with export and
  import, and — on an ordinary installation — the portable copy. Not one of them
  is what anybody would look for behind the word `Aplikace`, and a label that
  does not describe its contents is how the seven tabs got their names in the
  first place. The new one names both halves of what is there: the two folders,
  and the copies you go to once something has already gone wrong.

**Nothing moved between cards.** Every card is the same card with the same
contents; only which tab draws it changed. The one component change is that
`About` no longer takes the four update props — the update block is rendered by
the new `settings-card-updates` section instead.

**The tab names in the code changed with the visible ones**, so `application`
became `files` and `appearance`, `updates` and `about` stand beside them. The
last open tab is remembered in `localStorage` under `settings-tab`, and the
guard that was already there is what makes a rename safe: a stored name that is
not one of `SETTINGS_TABS` opens `Přepis` rather than an empty panel. Every
machine that opened Settings yesterday has `application` in there and will land
on `Přepis` once.

**Every card on every tab has an `order` rule**, including the two tabs that
hold a single card. Alone the number changes nothing; without it, the next card
put beside them takes `order: 0` and jumps above whatever asked for 1 — which is
precisely the defect written up yesterday about the recordings folder.

**Two keys were retired and two added.** `settings.tab.application` is gone from
`cs`, `en` and `sources.json`; `settings.tab.files` and `settings.tab.updates`
are new; `settings.tab.about` keeps its name and says `Informace` / `Information`
now. The appearance tab is labelled from `settings.appearance.title` — the card
that fills it already carries that word, and a second key holding the same word
is a second thing to keep in step. The updates card does the same in the other
direction, taking its heading from `settings.tab.updates`, exactly as the
transcription card takes its own from `settings.tab.transcription`.

- Files: `src/Settings.tsx`, `src/css/04-settings.css`,
  `src/locales/cs/settings.ts`, `src/locales/en/settings.ts`,
  `src/locales/sources.json`.
- One spacing rule moved with the block: `.about-updates` carried 22 px as the
  last band of the About card, where it followed a panel. As the whole of its
  own card it follows a heading, so it takes the 24 px `h2 + .field` and
  `h2 + .about-abilities` already take, written as
  `.settings section > h2 + .about-updates`.
- Verified: `npx tsc --noEmit`, `npm run build` (which runs `i18n:check`,
  `docs:check`, `tsc` and `vite build`), `npm test` 132. `i18n:check` required
  the three reworded or new English strings to be approved against their Czech —
  *Information*, *Folders and backups*, *Updates*, each read against its source —
  and the fingerprint left behind by `settings.tab.application` was deleted from
  `sources.json`.
- Not verified by machine: the screens. What to press is all five tabs at both
  window widths, the update check on `Aktualizace` (the button, the release
  notes dialog, the automatic switch), and Settings opened on a machine whose
  `settings-tab` still says `application`.
- **Left undone**: two tabs hold exactly one card each, so `Vzhled` and
  `Aktualizace` each carry a heading repeating the tab above it. Dropping the
  heading would make them the only cards in Settings without one, and inventing
  a second sentence for each would be words for the sake of the layout. It is
  visible and it is small; if it grates, the answer is a card that earns the
  tab, not a special case in the stylesheet.
- Also left, from yesterday and still true: 13 unused `wizard.*` keys from the
  wizard rebuild, which belong to that change rather than this one.

### 2026-08-14 — A sixth tab, `Nástroje`, for what is installed rather than what is transcribed

The owner read the five-tab arrangement above and asked for one more:

> Modely bych dal jako Nástroje, zvlášt záložku

So there are six: **`Přepis` · `Vzhled` · `Nástroje` · `Složky a zálohy` ·
`Aktualizace` · `Informace`.** `Nástroje` sits third, between `Vzhled` and
`Složky a zálohy`, which is where the reading changes subject: the first two
tabs are about what the application produces and how it looks, and everything
from here on is about this machine — what is on its disk, where, which version,
and what the application is.

**Three things moved and nothing was rebuilt.**

- The status band — everything downloaded, or *N* missing, with the button that
  opens the by-hand component list. It was the `settings-card-modules` card at
  the foot of `Přepis`. Same card, same heading, same button.
- `Technické podrobnosti`, the disclosure with the found paths, `Zkopírovat
  údaje` and `Otevřít log`. It was the last band inside `Pokročilé`, which meant
  somebody whose transcription will not start had to open a block titled
  *advanced* to see what the application can see. It is the folded footer of the
  status band's card now — a `spaced` action row followed by one `card-footer`
  disclosure, which is the pair those two stylesheet rules were written for and
  exactly what the archive card does. No new CSS.
- `Složka nástrojů` and `Složka modelů`, the two read-only rows with `Vybrat…`.
  They were also inside `Pokročilé`, where a folder is not a decoding value and
  `Zpět na výchozí` two rows below them had to carry a note saying it does not
  touch them. They are a card of their own, `settings-card-locations`, under the
  band that says what has been downloaded into them.

**`Pokročilé` keeps the beam width, the five decoding thresholds and
`Akcelerace zpracování` with `Změřit rychlost`** — how a transcript is made,
which is the tab it is on. It is not only decoding, so it is not renamed; and
naming it `Dekódování` would put a word from whisper.cpp's manual on a screen
that uses one nowhere else. What did change is that the block now holds values
and only values: `Zpět na výchozí` covers everything inside it, and the
exception for the two folders is gone with the folders rather than being kept as
a rule with no visible reason.

**This is not the old `Modely` tab coming back**, and the record should say so
plainly because yesterday's entry argued against it. What was deleted there were
four read-only tiles — the model, the acceleration, the editor, the speakers —
each repeating a value chosen a card or two higher up and none of them
pressable. Those stay deleted. What the owner has asked for is the other half of
that argument: the part of the subject that is neither a repeat nor a decision
about a transcript — is it here, where is it, and what exactly did the
application find — now has one place instead of being spread across the foot of
one tab and the inside of a disclosure on it. Yesterday's entry described that
remainder exactly — *what that tab was for is one line saying whether anything
is missing and one button to the list that fixes it* — and put it at the bottom
of `Přepis`. The line is still one line. It has moved to the tab where the rest
of its subject lives, and the paths and the folders have joined it.

**The red dot moved with the band.** It marks the tab that shows what is
missing, so it is on `Nástroje` now, not `Přepis`.

**The remembered tab still survives a name it does not know.** `settings-tab` in
`localStorage` is read through `SETTINGS_TABS`, so a stored `application` — or
anything else this build has never heard of — opens `Přepis` rather than an
empty panel. Nothing was renamed this time; `tools` is simply added, and every
machine keeps whichever tab it was last on.

**Every card on the new tab has an `order` rule**: `settings-card-modules` is 1
and `settings-card-locations` is 2. `settings-card-modules` was 5 on `Přepis`
and its number went with it, so `settings-card-advanced` moved 6 → 5 and the
transcription list is 1–5 with no gap. This is the defect that keeps being
written up here: a card with no rule takes `order: 0` and jumps above everything
that asked for 1.

**Two keys added, none retired.** `settings.tab.tools` is `Nástroje` /
*Tools*, and `settings.files.locationsTitle` is `Umístění nástrojů a modelů` /
*Location of tools and models* — the new card needed a heading, and the sentence
above its two rows is the `settings.files.locations*` pair it already had, now
wearing `settings-section-description` instead of `small-text` because it has
become a card's opening sentence rather than a line inside a disclosure. Both
new keys have a context entry. Two existing contexts were corrected rather than
left lying: `settings.advanced.title` no longer claims the block holds the
folders, and `settings.advanced.reset` no longer explains an exception that does
not exist any more. Context is not fingerprinted, so neither correction asks for
a re-approval.

- Files: `src/Settings.tsx`, `src/css/04-settings.css`,
  `src/locales/cs/settings.ts`, `src/locales/en/settings.ts`,
  `src/locales/sources.json`.
- Verified: `npx tsc --noEmit`, `npm run build` (which runs `i18n:check`,
  `docs:check`, `tsc` and `vite build`), `npm test` 132. `i18n:check` reported
  three English strings with no source fingerprint: the two new ones, read
  against their Czech, and `settings.appearance.fontUi` — *Interface type* for
  `Písmo rozhraní` — left unrecorded by the previous commit on this branch when
  the control came back. All three approved.
- Not verified by machine: the screens. What to press is all six tabs at both
  window widths; the red dot on `Nástroje` on a machine with something missing;
  `Vybrat…` on both folders, which writes a settings value from a card that has
  never held one before; `Zkopírovat údaje` and `Otevřít log` in their new
  place; and `Pokročilé` with its badge and `Zpět na výchozí`, which is now the
  last thing in the block.
- **Left undone**: `Nástroje` carries a card headed `Modely`, which is the word
  the tab used to be called. It reads correctly — the card is about the models
  and the tab is about everything installed — but if the pair ever grates, the
  answer is to name the card for the state it reports rather than to rename the
  tab the owner asked for.
- Still open from the entry above: `Vzhled` and `Aktualizace` each hold one card
  whose heading repeats the tab, and 13 unused `wizard.*` keys belong to the
  wizard rebuild rather than to this change.

### 2026-08-14 — One question at the beginning, and everything follows from it

The owner read the settings screen back and answered it with a decision rather
than a correction:

> Prostě možná by člověk neměl mít na výběr. Prostě má dvě možnosti, rychle
> nebo přesně. Ta podpora pro CPU nebo GPU se stáhne podle toho co má.
> … Jazyková úprava Rychlá vs Přesná, žádné modely in the middle.
> … Prostě na začátku si vybere jestli chce rychle nebo přesné výstupy. A to
> stáhne všechno potřebné. Jazykovou úpravu bych stahoval až potom, až bude chtít.

The wizard already asks `Rychle, nebo přesně?`. What was missing is that the
answer was thrown away the moment the model was downloaded, so every later
question — which language-editing model, which acceleration — had to be asked
again, of a reader who had already said which end of the trade they are on.
**The answer is now written down and read everywhere it decides something.**

**`quality_choice` in the settings record**, `fast` or `accurate`, with
`#[serde(default)]`. Empty is the honest value for every settings record written
before today: nobody was asked. It is not guessed in Rust; `qualityChoice()` in
`types.ts` reads it against `model`, and a machine transcribing with the turbo
model is a machine that answered *rychle* when it was set up. The wizard writes
it from both paths — the two cards, and ticking a transcription model by hand in
the component list, which is the same answer given in a different place. It is
written whether or not the model landed: `model` names a file and must not name
a missing one, while this names a decision.

**The language editor follows it and is never asked about again.**
`EDITOR_TIER` maps `fast` to `editor-model-light` (3350 MB) and `accurate` to
`editor-model-best` (6980 MB). There is no card, no dropdown and no second
question anywhere: the three choice tiles left `Přepis`, the three-tier table
left the wizard, and `editor-model-balanced` is offered nowhere.

**And it is named by nothing it cannot prove.** This is the part of the change
worth reading twice, because the brief for it asked us to check before writing
and the check came back empty. `docs/history/` contains no comparison of the
three models' output — none, on any axis. Every inference ever recorded here was
run with the 12B model alone, because it was the only one installed on the
machine that did the work (`2026-08-02.md`, "Full inference was not run during
development because none of the new 3.35–6.98 GB models was already installed
locally"). The single speed number in the record — 1.2 tokens/s reasoning-on
against about 75 reasoning-off — is 12B against itself with a llama-server flag
changed, not one tier against another. And `2026-08-05.md`, the entry that gave
the three cards their counted sparkles, says it outright: they "trade nothing;
they do the same work with more of it".

So `Lépe opravuje zjevné chyby a drží souvislosti` and `Nejspolehlivější
výsledek` were exactly the middle transcription model's *asi jedna chyba na
odstavec*, deleted yesterday for being measured nowhere. They are not replaced
with a smaller claim. The catalogue now names these three by size —
`Jazyková úprava — menší / střední / větší model` — and describes them by what
follows from a file's size and nothing else: how much memory it needs. Where the
reader meets the editor there is no tier name at all, only one sentence with the
number of gigabytes in it, because at that point which model it is is not
something they can act on.

This supersedes the 5 August decision that gave the tiers one, two and three
sparkles. `EditorMark` is deleted with the cards it marked: a mark that says
"more of the same" is right only where there are three things to tell apart, and
there are none.

**It downloads once, on demand, in the background.** The first press of a
document action on a transcript asks one question — *Chcete z přepisu hotový
dokument?* — with the size in the sentence. Yes starts the download and closes
the dialog: `download` in `downloads.rs` starts a thread and returns, so the
reader carries on reading and the application's own progress bubble reports it
with a way to stop. Pressed again while that runs, the dialog says the model is
downloading instead of offering again; asked once means once. `editor_model` is
written at the press rather than when the file lands, deliberately —
`resolve_editor_model` falls back to whatever is actually on the disk, so a name
ahead of its file is not a dead end, and a listener that had to survive the
reader walking to the Archive would not have survived it. The same press exists
on `Přepis` for anyone who arrives there instead, and does the same thing;
neither walks to the wizard any more.

**`Akcelerace zpracování` and `Změřit rychlost` are gone from Settings.** The
machine picks its backend from its drivers, exactly as the wizard already picks
the programs. A reader choosing here could only choose something slower or
something this computer cannot run — and `choose_compute` then silently
substitutes, which is the defect underneath: the screen went on badging
`používá se` on a card that had never run anything. Verified in `tools.rs`
before this was written: with a stored `cuda` and no `nvcuda.dll`,
`usable_compute("cuda")` is false, the stored branch is skipped, and the next
line down that the machine can run wins.

**So `Nástroje` gained a card that says what actually ran**, `Kde přepis běží`,
built from `.about-panel` — the panel of facts the About page and the archive's
backups already share — rather than from the four choice cards it replaces. It
shows `check.compute`, which is `choose_compute`'s answer and the folder the
next `whisper-cli` comes out of, not the stored setting. Three sentences under
it, at most one at a time:

- a backend was named by hand on some earlier day and this machine can run it —
  it says so, and offers `Nechat na aplikaci`. The same shape as `Odebrat`
  beside the watched folder: when a control is removed, whatever it could set
  must keep a way back, or one press made a year ago is permanent;
- it was named by hand and cannot be run here — it says that, and offers the
  same button. This is the reported case: CUDA on a machine with an AMD card;
- nothing was named, there is a graphics card, and the transcription is not
  using it. With nothing pinned that can only mean the build is not downloaded,
  so it offers to download it. And on a machine with no graphics card at all,
  one line saying so, which is a fact and not a fault.

`compute` therefore left `ADVANCED_DEFAULTS`: a badge reading `upraveno` about a
value with no control on the screen is a badge nobody can act on. It stays in
the settings record, with a comment saying why.

**The shortcut strip has a way back.** Yesterday's entry called permanence "the
trade: one fewer control against one fewer way back", and it is not a trade
anybody needs to make. Dismissing the strip is still a × on the transcript
screen; bringing it back is a keyboard button in the player's row, drawn only
while the strip is hidden — so it costs nothing when it is up, and it is where
the thing disappeared rather than two screens away. `detail.tips.hideHint` still
said *vrátit se dají v nastavení*, which stopped being true when the switch was
deleted; it names the button now.

**Two more from the owner, about the by-hand component list:**

> V tom ručním výpisu stažení modelů a nástrojů: Rozpoznání mluvčích a detekce
> řeči bych dal do jednoho. Dal bych pryč ty vyvážené modely.

- **One heading for the two speech models.** `vad` was in `Jazykové modely` and
  `model-hlasy` had a heading to itself over one 2 MB row. Both decide something
  about the sound rather than about the words — where somebody is speaking, and
  whose voice it is — so both carry the group `speech` in `download.rs`, and the
  list draws them under `Řeč a mluvčí`. The grouping is changed at the
  catalogue, not in the list, so anything else that reads a group agrees.
- **`model-large-q5` and `editor-model-balanced` are not offered by hand
  either.** The rule is now the same in the wizard, in Settings and in the list.
  Both catalogue entries, their pins in `components.json` and their download
  paths are untouched — this is about what is offered, not what exists — and one
  that is already on the disk still appears, as the ticked, disabled row every
  installed component gets. Hiding a gigabyte that is there would make the list
  lie in the other direction, and the list is also how somebody reads what this
  machine holds.

**A contradiction found on the way, and fixed.** `catalog()` in `download.rs`
marked `model-large-q5` as `recommended` on any machine with a graphics card —
the middle model, which the wizard stopped offering yesterday. The wizard's own
card said `model-large`, so the badge and the choice disagreed; worse,
`Spravovat modely` ticks every recommended component that is not on the disk, so
opening the list to look would have queued a gigabyte of a model no screen
mentions. `model-large` is the recommendation now, which is what the wizard has
always downloaded.

**A machine set up before today keeps working, and that was checked rather than
assumed.** `settings.model` may hold `large-v3-q5_0`: `Přepis` lists
`check.found_models`, which is read off the disk, so the model is still shown,
still named — `domain.model.large-v3-q5_0` stays in the dictionary — and still
selected. `editor_model` may hold `gemma-4-e4b-q4`: `EDITOR_MODELS` in
`types.ts` keeps all three components, `resolve_editor_model` in `tools.rs`
already falls back to any installed quality, and the switch on `Jazyková úprava`
turns the feature on with whatever is actually there, preferring the implied
tier. Nobody is asked to download seven gigabytes to replace a model that works.

- Files: `src-tauri/src/db.rs`, `src-tauri/src/download.rs`,
  `src-tauri/src/tools.rs`, `src/types.ts`, `src/api.ts`, `src/SetupWizard.tsx`,
  `src/Settings.tsx`, `src/Detail.tsx`, `src/css/04-settings.css`,
  `src/css/05-later.css`, `src/locales/cs/` and `src/locales/en/`
  (`settings`, `wizard`, `detail`, `catalog`), `src/locales/sources.json`.
- Stylesheet: `settings-card-compute` takes `order: 2` on `Nástroje` and
  `settings-card-locations` moves 2 → 3, so every card on every tab still has a
  rule. `.benchmark` and `.field-prompt` are deleted with their only users —
  `.field-prompt` was a red panel saying an optional feature was not downloaded
  yet, which is not a fault and should not have been wearing the danger colour.
  No new rule was written: the compute card reuses `.about-panel`, and the
  editor's ask reuses `.settings-action-row`.
- Dictionary: 15 keys added and 44 retired, each with its `sources.json`
  fingerprint. The retired ones include the 13 unused `wizard.*` keys that two
  earlier entries today listed as left undone — the welcome screen's title and
  introduction, and the whole speaker step — which belong to this change now
  that the language-editing step's words have gone the same way. Every new Czech
  key has a context entry, and the reworded English was read against its Czech
  before approval: the six catalogue strings for the editor models, and
  `detail.tips.hideHint`.
- Verified: `npx tsc --noEmit`; `npm run build` (`i18n:check`, `docs:check`,
  `tsc`, `vite build`); `npm test` 132; `cargo fmt --all --check`;
  `cargo clippy --all-targets -- -D warnings`; `cargo test` 183.
- Not verified by machine: the screens. What to press is the wizard's two cards
  on a machine with and without a graphics card (the answer must land in
  `quality_choice`); the first document action on a transcript with no editor
  installed, twice, the second time while the download is running; the same ask
  on `Přepis`; the strip's × and the keyboard button that brings it back;
  `Nástroje` on a machine with a graphics card and no build for it, and on one
  whose `compute` still holds `cuda`; and the by-hand list, which should now
  show four headings and neither middle model.
- **Left undone, and deliberately.** `benchmark_compute` in Rust is still
  registered and now has no caller. It was kept rather than deleted because it
  is the only instrument this application has for timing a backend, and the
  estimates the wizard prints — 1, 8, 4 and 35 minutes — are still the oldest
  unmeasured numbers in the interface, which a comment in `SetupWizard.tsx` has
  been pointing at since yesterday. Deleting it would take `tools::clip` and the
  `benchmark.*` messages with it and make those numbers unsettleable. If the
  owner would rather have it gone, it goes in one commit; what should not happen
  is that it stays for a year with nobody remembering why.
- Also left: the `missingModule` branch in the wizard that adds the editor
  runtime beside an editor model is no longer reached, because neither screen
  walks to the wizard for the editor any more. It is correct if it ever is
  reached, and it is three lines; it is named here rather than quietly kept.


### 2026-08-14 — The owner at the screen: `Výkon`, two editor models, and a floor under both

One sitting, nine sentences from the owner, each one a reaction to what the
previous change put in front of him. The record describes the shape it ended in
and, where a route was walked and abandoned, says so — twice the reading of one
sentence was wrong and the wrong thing was built, and both are worth carrying so
that nobody rebuilds them.

#### Jazyková úprava: two cards, and nothing to switch off

Three sentences, an hour apart:

> Jazyková úprava nemá smysl. Prostě u ní mají být dva modely na výběr. Žádný
> toggle.

> Volba v průvodci bude jednoduchá. Rychle nebo přesně. Zbytek se zvolí podle
> toho. To samé jazyková úprava.

> Tlačítko Nepoužívat u Jazykové úpravy nedává smysl.

The card is **two choice cards, `Menší` and `Větší`, one of them always chosen,
and nothing else.** The `SettingsToggle` is gone and there is no off state.

**Why there is no off state, written here so nobody adds one back:** language
editing never runs by itself. It happens when somebody asks a transcript for a
document, and nothing is downloaded until they do. A switch would be turning off
something that only ever happens on request. The question the cards answer is a
different one — which model is used when the reader does ask — and that is a
setting, not a switch. A `Nepoužívat` button stood there for one round, built to
answer *how does somebody say they do not want it at all*; the question dissolves
rather than being answered, and the button was deleted rather than hidden.

**`editor_model: ""` was the old off state and is not one now.** It is what a
machine holds when nobody has ever asked for a document, and every reader of it
already treats it as *nothing downloaded yet* rather than *the reader declined*:
`Detail.tsx` offers the download, `tools.rs` resolves no model and reports no
fault, and Settings draws the default card as chosen without claiming the reader
picked it. Nothing had to change for that; it was checked rather than assumed.

**One question in the application, and Settings is where it is revisited.** The
wizard asks `rychle` or `přesně` once and `EDITOR_TIER` turns that into a model —
that is the default, and it is what the chosen card shows until somebody chooses
otherwise. **A card pressed here is stored in `editor_model` and wins**, exactly
as the transcription model chosen a card above wins over the one the wizard
downloaded, and nothing writes over it afterwards. The middle sentence above was
read as *the editor can never be chosen anywhere*, and a version with no cards at
all — an `.about-panel` row naming the model — was built and thrown away. The two
are different things: one question at setup, and a screen where an answer can be
changed later, are the same arrangement the transcription model has always had.

`askForEditor` in `Detail.tsx` was the one place the two could have fought, and
did: it computed the tier from `quality_choice` and ignored `editor_model`, so a
reader with a model chosen and its runtime missing would have been offered a
different model — several gigabytes to replace a working choice with an
inference. It reads the stored value first now.

**Sizes on the cards, one square at two sizes beside them, and no claim about
what comes out.** Pressing a card fetches whatever it still needs — the model,
and the runtime where that is missing too — and the size in the card says how
much that is. A card already on the disk says `staženo`.

#### Menší and Větší, and where an adjective stops working

> Větší a Menší u jazykové úpravy stačí a měly by pro každý být ikona, to platí
> o Přesný a Rychlý taky.

> Stahuji Větší působí divně. Větší co? Model?

Both are right and together they are a rule worth keeping: **a card label and a
catalogue name are two different jobs.** Under a heading reading `Jazyková
úprava` the noun is supplied and `Menší` is correct. In `Stahuji {name}`, in the
by-hand list and in the failed-items list the name stands alone, and an adjective
with no noun is a sentence with a hole in it. So the cards say `Menší` and
`Větší` from the settings dictionary, and the catalogue says `Menší model
jazykové úpravy` and `Větší model jazykové úpravy`. The sentence under each card
is still the catalogue's, because that one reads correctly in both places.

The rest of the catalogue was given the same test. `Rychlý model`, `Přesný
model`, `Detekce řeči`, `Rozpoznání hlasů`, `Přepis na grafické kartě` all carry
their noun already; the three editor models were the only ones that did not. And
the reverse test on the model cards: `Rychlý` and `Přesný` under a label reading
`Model` correctly do not repeat the word.

**No quality claim came back in.** Nothing in `docs/history/` compares these
models' output — the entry above sets that out — so the descriptions say what a
file's size does decide: `Potřebuje nejmíň paměti a je hotová dřív` against `Dělá
totéž s víc parametry a potřebuje na to víc paměti i času`. The added *i času* is
arithmetic about parameter count, not a measurement.

#### Icons on four choices, and one pair that is weaker than the other

- `Rychlý` and `Přesný` on the wizard's quality cards carry `ModelMark`, keyed on
  the model identifier — the lightning is `large-v3-turbo`, the target is
  `large-v3`, the same drawings the model list in Settings has always given those
  two models. The mark moved from `Settings.tsx` to `src/icons.tsx` for that
  reason: the same model must not wear one drawing on the first screen and
  another on the fifth, and importing it out of `Settings.tsx` would have made a
  cycle — the problem `settings/toggle.tsx` was created to solve. The cards
  became `.choice with-icon` with a `.choice-body`, and `.choice-size` moved out
  to the right edge beside where a badge would sit. **This pair is honest in
  isolation**: speed against precision is the trade those two models make, and it
  is the one thing about them anybody has measured (13 August, time alignment:
  3.34 s against 0.56 s).
- `Menší`, `Střední` and `Větší` carry one square drawn at three sizes,
  `sizeSmall` / `sizeMedium` / `sizeLarge`. The same shape at a different size,
  because size is the only thing known about the difference — a drawing that also
  changed shape would be claiming something nobody has measured. **They read as a
  set and are weak alone**, and with two cards on screen the pair is visible,
  while the middle one on an older machine stands by itself and only reinforces
  its word. That is the ceiling for a mark whose meaning is a comparison, and it
  is written into the comment on the glyphs so nobody re-derives it. This is not
  `EditorMark` returning: that counted one, two and three sparkles and was a rank.
  A square at its own size is a size.
- `Grafická karta` and `Procesor` on `Výkon` carry `graphicsCard` — a new glyph,
  a board with its fan and pins — and `compute`, which was already a processor
  drawing. Two different objects rather than two chips, because telling them
  apart at 22 px is the whole job.

#### `Výkon`: the switch back, and CUDA never asked

> Kde přepis běží a tam bych dal CPU a GPU možnost přepnutí. Ale automaticky by
> to vždycky volilo tu rychlejší variantu.

> A dej tam stejnou kartu jako má výběr modelu u přepisu, ne tu trapnou záložku.

> A dej pod to info ikonu: Aplikace automaticky volí výkonnější variantu.

> Kde přepis běží změň na: Výkon

The entry above removed that control and left the card as a panel of facts. The
panel stays; the choice comes back above it as **two `.choice with-icon` cards,
the same ones the transcription model uses** — a segmented control was built
first and rejected in those words — and the card is headed **`Výkon`**.

**Automatic is not a third card.** It is the absence of a pick: what a fresh
installation has, and what `Automaticky` puts a machine back to. A third card
would say the work can happen in three places, and it can happen in two. What
says so instead is the note under the cards, `Aplikace automaticky volí
výkonnější variantu.`, which describes the resting state and does not promise
that the application overrides a deliberate choice — it does not, and the panel
below is what says whether a choice was honoured.

**CUDA against Vulkan is never asked, and that is the point of the shape.** They
are two builds of one thing for two kinds of card, and which suits the card in
this machine is a fact about its drivers. Asking it is how somebody came to have
`cuda` stored beside an AMD card — the build found no device, whisper fell back
to the processor, and the screen went on badging `používá se` on the card nobody
was using. So `settings.compute` gained the value `gpu`, which names the card
without naming a build, and `choose_compute` resolves it: `GRAPHICS_BUILDS` is
CUDA then Vulkan, filtered by what is downloaded *and* by what the drivers allow.
The same expression is what automatic prefers, which is why it is written once
and called twice — automatic taking the fastest available option and `gpu`
meaning the fastest card build are the same sentence.

**Automatic stays distinguishable from a pick**, because it is a stored value of
its own and not an absence in the database: a machine on `auto` follows a driver
appearing or disappearing, and one on `gpu` goes on asking for the card across
both. `cuda` and `vulkan` in older settings records still load, are still
honoured where the machine can run them, and are drawn as `Grafická karta` — the
position they meant. They are not rewritten on sight; a screen that quietly
edited a stored value while being looked at would be a worse habit than a
two-word translation in `computeMode`. The first press of either card replaces
them.

**The cards carry no `používá se`**, on the owner's word — *ta karty používá se
je v bloku výkonu nadbytečná*. A card drawn as chosen already says it is the
one, and two marks for one fact is noise. What the badge was carrying is the
opposite case and stays: the chosen card wears `missing`, the danger colour the
module tiles use, exactly when what ran is not what was picked. **The card is
quiet when it agrees and speaks when it does not** — an empty state there is
correct, and the comment says so, because the next person to see it will be
tempted to fill it.

The `Používá se` row under the cards is kept for a reason the cards cannot
cover: it is the only place that says *which* graphics build the drivers settled
on, which is the answer for a machine on automatic that wants to know where it
ended up.

**What could not be honoured is said, with the reason, and fixed where it can
be.** The substitution inside `choose_compute` is silent by design, because a
transcription must run; this card is the one place it is not silent. Three
sentences, at most one at a time:

- the card is idle with a driver present — with neither card picked the sentence
  says the build is not downloaded, with `Grafická karta` picked it says the
  choice could not be met and what ran instead, and both offer the download;
- `Procesor` picked and the transcription running on the card: the wizard used to
  download one build, and on a machine with a graphics card it was not the
  processor's. Same row, same button, pointing at `whisper-cpu`;
- no supported card at all, which is a fact and not a fault, unchanged.

`compute` stays out of `ADVANCED_DEFAULTS`: `Zpět na výchozí` restores what its
own block shows, and a reset that moved a control on another tab is one nobody
watching it could see.

#### A floor under both: `whisper-cpu` and `editor-cpu` on every first run

> Jazyková úprava na procesoru by se měla taky stahovat defaultně.

`catalog()` in `download.rs` recommends `whisper-cpu` (17 MB) and `editor-cpu`
(45 MB) unconditionally now, rather than only where there is no usable graphics
card. Two reasons, both in the comments where the change is made:

- **whatever the reader can end up pointed at has to exist.** A `Procesor` card
  naming a build that is not on the disk is not a choice — it would be answered
  by `choose_compute` finding nothing, falling through to the card, and the panel
  having to explain that the answer was no. That is the silent substitution this
  card exists to stop, arriving through the control instead of around it;
- **a driver that breaks needs somewhere to fall back to.** A graphics build is
  useless the moment its driver stops loading — a Windows update, a card swapped
  out, a laptop on its integrated chip.

`editor-cpu` is the runtime, not a model: the two Gemma models are 3350 and
6980 MB and stay on demand. `editor-vulkan` (150 MB) also stays on demand and
comes down with the model it runs, on a machine that can use it — the fast path
can wait for the thing it makes fast; the floor cannot.

`whisper-cpu` joins the guided selection through `root`, which takes every
recommended `program`; `editor-cpu` is in the `editor` group and is added by name
in `EDITOR_RUNTIME_COMPONENTS`, beside `DIARIZATION_COMPONENTS` and for the same
reason. `totalMb` reduces over that same selection, so **the number on the
download screen is the number that downloads** — roughly 1.25 GB now.

#### Two more moves, and one sentence that had gone quietly false

> Jazyk nahrávky dejme jako samostatný blok

`Jazyk nahrávky` has a card of its own again, its second move today: the old
`Jazyk a detekce řeči` lost its switch, the one remaining field was folded onto
the model card, and it reads better standing up — what language is spoken is a
fact about the recording, not a property of the model, and it sat under a column
of choice cards as an afterthought. **The heading carries the longer name**;
`Jazyk` alone stood two tabs from `Jazyk aplikace` with nothing to tell them
apart, and a heading is a worse place than a label to leave that. The dropdown
below it has no label of its own — the heading is the label — and takes `Select`'s
`description` so a screen reader still hears the name.

> Řeč a mluvčí → Detekce řeči a mluvčích

The by-hand list's group heading over `vad` and `model-hlasy`. Both detect
something in the sound — one where speech is, the other whose it is — and the old
name read as a heading over the subjects rather than over the work. Both genitives
are deliberate and the context entry now says so.

> Zobrazuje pouze stažené modely. Tohle už neplatí, ne?

Checked in the code rather than from memory, and **the sentence was true**: the
model card lists `check.found_models`, read off the disk, so it shows what is
installed and nothing else — one card on a machine with one model — and pressing
a card downloads nothing. That is also what keeps a machine set up before today
working: `settings.model` may hold `large-v3-q5_0`, the middle model nothing
offers any more, and because the list is the disk rather than an offer it is
still shown, still named by `domain.model.large-v3-q5_0`, and still the chosen
card.

What the same test caught instead was the sentence beside it. `settings.transcription.description`
still read *Model, který nahrávku přepíše, a jazyk, kterým se v ní mluví* on a
card the language had just left — false by one change, and unnoticed. It names
the model only. And `modelNote` was true but useless: it said what the list does
not show without saying where the rest are, so it says both now. The check that
found it is worth keeping: not *does it read well*, but *would a reader who
believed this be wrong*.

#### The last three, all about a glyph or a word

> ikona GPU je nehezká a dej do závorky (GPU) a (CPU) ať je jasné o co jde

The labels are `Grafická karta (GPU)` and `Procesor (CPU)`. The Czech words are
what a reader parses and the abbreviation is what they have seen everywhere
else; both context entries say the bracket is deliberate, so that a later tidy-up
does not drop it as redundant.

And the graphics-card glyph was redrawn. The first attempt had the board, a fan,
a row of memory and two bracket pins — five subpaths, where every neighbour in
that file has one or two, and at 22 px inside a 38 px circle the detail turned to
grain. It is a wide board with a fan now, which is `video`'s complexity (a
rectangle and a mark inside it). `compute` was not reused for both halves: it is
a die with its legs, and the pair on `Výkon` is the chip already in the machine
against the card you add to it. One glyph standing for both would make the choice
unreadable.

> Autor a Verze potřebují ikonku

Both rows of the `Informace` panel carry an `.about-mark` — the 30 px
`--accent-light` circle the abilities list on the same screen already uses,
rather than a size invented for two rows — with a release tag for the version and
a single figure for the author. **Both, not one**: a panel where some rows have a
mark and some do not reads as an accident. The licence list on the same tab stays
plain, and that is the same rule pointing the other way: its rows are one per
component, and a column of glyphs would say nothing the name beside each does not.

The row grows from 40 px to 46. A 30 px circle in a row with 6 px of padding sits
2 px from both edges, which is a mark wedged into a row rather than one sitting in
it; 8 px above and below is what the abilities list gives the same circle. The
rule is scoped to `.about-panel-marked`, so the licence list and the `Výkon` panel
keep 40.

- Files: `src-tauri/src/tools.rs`, `src-tauri/src/db.rs`,
  `src-tauri/src/download.rs`, `src/Settings.tsx`, `src/SetupWizard.tsx`,
  `src/Detail.tsx`, `src/icons.tsx`, `src/types.ts`, `src/css/04-settings.css`,
  `src/css/05-later.css`, `src/locales/cs/` and `src/locales/en/` (`settings`,
  `catalog`, `wizard`, `domain`), `src/locales/sources.json`.
- Stylesheet, five rules and no new block: `.choice.with-icon > .choice-size`
  joins the badge's `margin-left: auto` (no card carries both, so they cannot
  split the free space between them); `.settings-card-compute .about-panel` takes
  14 px, which is what `.choices + .settings-info-note.compact` already takes
  above it, so that card's three bands sit on one rhythm; two scoped to
  `.about-panel-marked` for the 46 px row and its flex label; and the `Přepis`
  tab's `order` rules renumber for the new card — model 1, language 2, speakers
  3, language editing 4, dictionary 5, advanced 6. Every card on every tab has
  one.
- Icons: four added to `LINE_ICONS` — `graphicsCard`, `sizeSmall`, `sizeMedium`,
  `sizeLarge` — plus `tag` and `author` for the two fact rows, and `automatic`,
  drawn for a third compute card that the final shape does not have. It is
  deleted rather than kept unused.
- Dictionary: 14 keys added, 7 retired, and every new Czech key has a context
  entry. Retired with the controls they belonged to: `settings.compute.pinned`,
  `settings.compute.substituted`, `settings.editor.download`,
  `settings.editor.enabledNote`, `settings.editor.offNote`,
  `settings.editor.turnOff`, `settings.editor.turnOn`, and their `sources.json`
  fingerprints with them. Sixteen English strings were read against their Czech
  and approved, `domain.compute.auto` among them: it said *Automatically* while
  the new copy said *Automatic*, and one Czech word translated two ways is the
  drift `i18n:check` exists to catch.
- Verified: `npx tsc --noEmit`; `npm run build` (`i18n:check`, `docs:check`,
  `tsc`, `vite build`); `npm test` 132; `cargo fmt --all --check`;
  `cargo clippy --all-targets -- -D warnings`; `cargo test` **187** — four new
  ones in `compute_choice_tests`, all about the value this change added: `gpu`
  resolving to a build rather than naming one, the processor being honoured over
  a usable card, automatic preferring the card, and a build that is not
  downloaded not being chosen however it was asked for.
- Not verified by machine: the screens. What to press is both cards on `Výkon`
  and `Automaticky` back off them, on a machine with a graphics card and on one
  without, including the state where a chosen card goes red because what it asked
  for is not what ran; `Procesor` on a machine that only ever downloaded the graphics build,
  which must say so and offer the other — a state new installations no longer
  reach and older ones still do; a machine whose settings still hold `cuda`,
  which must open showing `Grafická karta`; both cards on `Jazyková úprava`, the
  second of which starts a multi-gigabyte download; a first run, whose total must
  now include 62 MB more and must match what is fetched; the wizard's two quality
  cards with their marks at both window widths; and the by-hand list, where the
  editor models now carry names that stand alone.
- **Left undone.** The size glyphs are a comparison, and the middle model is
  drawn alone on the machines that have it. `benchmark_compute` in Rust still has
  no caller — with a processor/card switch back on the screen there is now
  something worth timing, and the wizard's 1, 8, 4 and 35 minutes are still the
  oldest unmeasured numbers in the interface. And the model card is still headed
  with the tab's own word while its only remaining field is labelled `Model`;
  naming the card `Model` would move the duplication rather than remove it, and
  the answer is a heading that earns its card.

### 2026-08-14 — `Výkon` after the screenshot: one thing said once, and a switch instead of a button

The entry above shipped and the owner opened it. He sent back a screenshot of
`Výkon` with `Procesor (CPU)` deliberately chosen and **two things struck
through in red**: the note reading `Aplikace automaticky volí výkonnější
variantu.`, and the whole `Používá se — Procesor (CPU)` row. Then two more
sentences about the same card, and one about the language editor.

He is right, and the rule underneath is the one the previous entry had already
written down without applying properly: **the card is silent when it agrees and
speaks when it does not.** On that screenshot the chosen card, the `Používá se`
row and the note stated one fact three times. Worse, the note was not merely
redundant there — with a card deliberately picked it was *false*. The
application had not chosen anything.

**The `Používá se` row is deleted.** The previous entry argued for keeping it,
because it was the only place naming which of CUDA and Vulkan the drivers
settled on. That argument does not survive the screen: in two of the three
states it repeats something already visible, and in the third — a pick that
could not be met — a label and a value are far too quiet for what is the
application contradicting an instruction. That case needs a sentence, and it has
one.

**What sits under the cards now depends on the state, and two of the three
states say nothing:**

- **picked and running where it was asked to** — silence. The highlighted card
  is the whole answer;
- **automatic** — nothing either, unless there is a fact worth having: no
  supported graphics card at all, or a card whose build was never downloaded,
  which comes with the button that fixes it;
- **picked and refused** — the one case worth ink: what ran, why the pick could
  not be met, and the download where a download is the answer.

**`Aplikace sama zvolí výkonnější variantu.` moved onto the control that does
the choosing**, as the switch's own description, so it is only ever on screen
while it is true. That is where the second correction landed:

> Tu volbu automaticky bych nedělal tlačítkem, ale toggle.

**Automatic is a state, not an errand**, and a `SettingsToggle` says so where an
`Automaticky` button said *do this now*. It is on by default and after a reset,
because following the drivers is the resting state. Turning it off keeps
whatever was already running — `onGraphicsCard ? "gpu" : "cpu"` — so the switch
changes who decides and never where the work happens.

> Ten toggle dej pod ty karty.

**And it sits under them**, which settles the question the shape had left open:
the cards stay visible while it is on. A switch beneath them reads as a
qualifier on what is above it — *these two, and let the application decide
which* — and it means somebody can see where their transcript runs without
touching anything. While it is on, the highlighted card is what the drivers
settled on rather than what anybody picked, which is the switch's effect made
visible; pressing a card turns the switch off and takes that card, which is the
most direct reading of what pressing one means. The red `missing` state cannot
occur while the switch is on: with no instruction to contradict,
`choose_compute`'s answer is simply the answer.

> Hele to automaticky nedávej jako nadpis ale dej to jako text před ten toggle.

**The plain row, not `heading`.** That prop renders an `<h2>` and turns the row
into the card's section switch, which would have given `Výkon` a second heading
below its own and claimed everything under it as a new section. What it is
instead is the shape `Přepisovat nové soubory automaticky` and `Kopírovat
přidané soubory` already use: a 15/680 title with the switch opposite it and its
sentence underneath.

The geometry is the stylesheet's own, and the plain variant's rather than the
section variant's borrowed. `.field + .settings-toggle:not(.section)` already
places a plain switch below the controls it qualifies at 24/22 with a divider; a
column of choice cards is a block of controls exactly as a field is, so
`.choices` joins that selector. `.settings-toggle.separated` at 22/20 is the
other case — a switch after free-flowing text, as on `Aktualizace` — and is not
this one.

The section variant also gives its note `max-width: 62ch` and the plain one does
not, which was checked rather than assumed: the plain row is a flex box with the
switch opposite and `.settings-toggle-copy` taking the rest, so the note is
already bounded by the card. At 39 characters this sentence cannot wrap badly,
and `Archiv vytváří kopie přidaných souborů. Zabere to místo navíc.` has been
sitting in the same unbounded row at 62. Nothing added.

`Vybraná varianta platí i tam, kde by aplikace zvolila jinak.` was the button's
sentence and is deleted with it. A switch that is visibly off already says
nobody is choosing over the reader.

**`settings.badge.inUse` is gone from the model card too**, and that was checked
rather than assumed before deleting: the list is `found_models`, so the badge
appeared exactly on the card `n.model` names and that card is the chosen one;
where the stored model is not on the disk no card is chosen and the badge did
not render either. It never said anything the highlight did not. The key is
retired — nothing else used it.

**And with `Používá se` gone, nothing in the application names a build to the
reader any more.** `labels.compute`, `COMPUTE_IDS` and the five
`domain.compute.*` names — `Grafická karta (Vulkan)`, `Procesor (CPU)`,
`Výchozí` and the rest — are deleted with their last caller. `Výkon` asks for
the card or the processor; which build suits the card is `choose_compute`'s
business, and it is neither a question nor a report now. Comments where each
stood say so, because the next person to need a backend name will otherwise
re-add the table rather than ask whether it should be on screen at all.

#### And the sentence under the language-editing cards

> Text dole u jazykové úpravy změň na: Úprava se spustí pouze na vyžádání a
> vytvoří nový dokument.

Used exactly. It is shorter than what it replaces and says one thing the old
sentence did not: the result is a **new** document, not a rewrite of the
transcript. That is the reassurance somebody wants before pressing anything on
this card, and it is what the feature is most likely to be feared for.

It drops the download sentence, and that was checked rather than waved through:
`detail.ai.offerText`, the ask-once dialog, still reads *Text upraví model přímo
v počítači, takže nic neodejde ven. Stáhne se jednou, {size}, a stahování běží
na pozadí* — at the moment the size matters, with the number in it. The cards
themselves carry the size or `staženo`. So the information moved to where it is
useful rather than disappearing; the only phrasing not repeated anywhere is
*pracuje bez připojení k internetu*, and *přímo v počítači, takže nic neodejde
ven* is the same fact put better.

`settings.editor.description` gave up `Vytvoří nový dokument.` in the same
breath, because the new note says it one line lower and the card would have said
it twice — which is the defect the whole of this entry is about. It names what
the model does instead, in the shape the card two above it uses: *Model, který z
přepisu udělá čitelný text.* against *Model, kterým se nahrávky přepisují.*

- Files: `src/Settings.tsx`, `src/labels.ts`, `src/types.ts`,
  `src/css/04-settings.css`, `src/locales/cs/` and `src/locales/en/`
  (`settings`, `domain`), `src/locales/sources.json`.
- Stylesheet: `.choices` joins `.field + .settings-toggle:not(.section)`, and
  the `.settings-card-compute .about-panel` rule from the previous commit is
  deleted with the row it placed. No new amounts.
- Dictionary: 11 keys retired and none added. `settings.badge.inUse`,
  `settings.compute.running`, `settings.compute.pinnedNote` and the five
  `domain.compute.*` names, each with its `sources.json` fingerprint;
  `settings.compute.autoNote`, `settings.editor.description` and
  `settings.editor.note` were reworded and their English read against the new
  Czech before approval. `Aplikace sama zvolí výkonnější variantu` says `sama`
  rather than `automaticky` on purpose — the switch beside it is labelled
  `Automaticky`, and the context entry records that so a translator working key
  by key does not tidy the echo back in.
- Verified: `npx tsc --noEmit`; `npm run build` (`i18n:check`, `docs:check`,
  `tsc`, `vite build`); `npm test` 132; `cargo fmt --all --check`;
  `cargo clippy --all-targets -- -D warnings`; `cargo test` 187. No Rust changed
  in this round; the four new compute tests are the previous commit's.
- Not verified by machine: the screens. What to press is the switch off and on,
  which must leave the card that was already running chosen; each card while the
  switch is on, which must turn it off and take that card; a machine whose
  chosen build is not downloaded, where the card must go red and the sentence
  under it must both explain and offer; and `Jazyková úprava`, whose card should
  now read as three lines and not four.
- **Left undone.** `settings.compute.description` still says *Kde se přepis
  počítá*, which is the card's old name doing duty as its opening sentence. It
  is true and it is not a repeat of `Výkon`, so it stays; if it grates, the
  answer is a sentence about what the choice costs rather than about what the
  card is.

### 2026-08-14 — Four headings that say which group a thing is in, `Větší` first, and a panel that said nothing twice

#### The by-hand list's group headings

> Jazykové modely
> Jazykové modely pro přepis

The owner named the heading and gave the replacement, and the reason is the one
that broke `Stahuji Větší` earlier today: **that list now holds language models
of two kinds.** Whisper models transcribe and Gemma models rewrite, and both are
language models, so a heading reading `Jazykové modely` had stopped separating
anything. `Jazykové modely pro přepis` says which half it is.

Applying the same test to the other three — *can somebody scanning the list tell
from the heading alone which group a component belongs in?* — found one real
failure and one that the owner's change would otherwise have created.

**`Jazyková úprava` → `Programy a modely pro jazykovou úpravu`.** That group has
always held two programs (llama.cpp for the graphics card and for the processor)
beside its models, while a group three headings above is called `Programy`. So
`Jazyková úprava na procesoru` is a program filed away from the programs, and a
reader looking for it would look in the wrong place. That failure predates
today; it is answered by the heading naming both halves rather than by moving
anything, because a reader adding language editing wants its runtime and its
model together, which is why the catalogue groups them that way.

It also settles what the coordinator flagged: `Jazykové modely pro přepis` names
things, `Jazyková úprava` named an activity, and a split whose two sides are
different kinds of phrase reads as accidental. Both are `<things> pro <job>` now.

**`Programy` → `Programy pro přepis`**, and this one is a consequence of the
owner's own change rather than an old defect. Once the models group says *pro
přepis*, a bare `Programy` sitting beside it invites the question of why the
transcription programs are not under the transcription heading. Saying *pro
přepis* on both makes the pair explicit — programs for transcription, models for
transcription — and mirrors the editor group, which has both in one. The
qualifier stretches furthest over `yt-dlp` and `deno`, which fetch audio from
the web; inside this application there is no other reason for them to exist, and
the context entry says so.

**`Detekce řeči a mluvčích` is untouched.** The owner wrote that heading himself
two entries above, and it already passes the test. It leaves the four headings
in three shapes rather than one — a job, and two `<things> pro <job>` — and that
is the right trade: matching it would mean overruling a wording he chose, to fix
something no reader can be confused by.

So the list reads: **`Programy pro přepis` · `Jazykové modely pro přepis` ·
`Detekce řeči a mluvčích` · `Programy a modely pro jazykovou úpravu`.** All three
changed context entries say what the qualifier is doing, so that a later tidy-up
does not shorten them back.

#### `Větší` first

> Dej větší model jako první volbu u jazykové úpravy

`EDITOR_MODELS` is ordered largest first now, and the cards on `Jazyková úprava`
follow it: **`Větší`, then `Menší`.** It matches the quality cards, where
`Přesný` is drawn above `Rychlý`, so the two columns of cards on the `Přepis`
tab read the same way down the screen. The comment on the table says this in as
many words, because the instinct when listing a pair by size is to go small to
large and somebody will otherwise tidy it back.

**`Střední` keeps the middle.** It is not an extra offer to be parked at the
end — it is a size, and the column is one monotonic run of them. On the machines
that hold it the three read `Větší · Střední · Menší`, which is the same list as
before with the axis reversed rather than a pair with a leftover after it.

**Three things read that array, and only one of them cared.** Which card is
chosen is a lookup by stored value, in Settings and in `Detail.tsx` both, and a
`find` over unique values does not care what order it walks. The recommendation
does not read the array at all: `EDITOR_TIER` names its two components outright.
The one that changed behaviour is the wizard's by-hand path, where ticking two
editor models at once takes the first one found — so the **larger** now wins
where the smaller used to. That is the better answer and it was already the
answer everywhere else: `resolve_editor_model` in `tools.rs` has always fallen
back through 12B, E4B, E2B. Both files are on the disk in that case, so the one
not written stays perfectly usable.

#### The wizard's detected block

> Tu konfiguraci a přepis na grafické kartě dejme pryč, zbytečné

**The two-panel grid is gone** — `KONFIGURACE / Grafická karta (Vulkan)` beside
`PŘEPIS / na grafické kartě`, under the one question. The sentence directly
above it is `wizard.quality.introGpu`: *Odhady časů platí pro grafickou kartu v
tomto počítači.* The panels restated that line in two boxes, on the screen with
the fewest words in the application.

**The sentence stays and the panels go**, and the distinction is worth writing
down: the sentence is not a status report, it is the reason the estimates below
it say what they say. A reader needs to know the four minutes assume a graphics
card. Nobody needs to be told twice, in a frame, which graphics card.

`usesGpu` is untouched — it picks the recommended card and chooses between the
two sets of time estimates. **What was removed is the announcement, not the
detection**, and the comment on the step says so along with *do not put a status
panel back here*, because this is the second time that screen has had one
deleted: the welcome page it came from went on 13 August for the same reason.

Cleaned up rather than hidden:

- `.detected`, `.detected-body`, `.detected-label` and `.detected-value` in
  `04-settings.css` had no other user and are deleted, with a comment where they
  stood.
- The seven `wizard.welcome.*` keys — `configurationLabel`, `gpuNvidia`,
  `gpuVulkan`, `gpuNone`, `transcriptionLabel`, `onGpu`, `onCpu` — are retired
  in both languages, with their context entries and their `sources.json`
  fingerprints. That empties the `wizard.welcome.*` namespace completely; the
  comment where it stood records what was there.
- `LineIcon` is no longer imported by `SetupWizard.tsx`. The two icons it drew,
  `compute` and `transcription`, both have other users — the compute cards on
  `Výkon` and the abilities list on `Informace` — so the icon set is unchanged.

- Files: `src/SetupWizard.tsx`, `src/Settings.tsx`, `src/types.ts`,
  `src/css/04-settings.css`, `src/locales/cs/wizard.ts`,
  `src/locales/en/wizard.ts`, `src/locales/cs/settings.ts`,
  `src/locales/sources.json`.
- Dictionary: 7 keys retired, none added, 3 reworded and approved against their
  English. Five context entries rewritten — three to record why each heading's
  qualifier is there, two because `Menší` and `Větší` swapped which card they
  describe.
- Verified: `npx tsc --noEmit`; `npm run build` (`i18n:check`, `docs:check`,
  `tsc`, `vite build`); `npm test` 132; `cargo fmt --all --check`;
  `cargo clippy --all-targets -- -D warnings`; `cargo test` 187. No Rust changed
  in this round.
- Not verified by machine: the screens. What to look at is the by-hand list's
  four headings with a component under each; `Jazyková úprava` with `Větší`
  above `Menší`, and on a machine holding the middle model, `Střední` between
  them; and the wizard's question step on a machine with and without a graphics
  card — the intro sentence must still change with the hardware even though
  nothing else on the screen reports it.

### 2026-08-14 — Two tabs named for both halves, a card for the app's language, and the paths folded away at the foot

*(Four messages in this round; the fourth deletes a sentence and is at the end
of the `Technické podrobnosti` section.)*

Three messages, all about where things stand rather than what they do.

#### `Jazyk a vzhled` and `Výkon a modely`

> Jazyk a vzhled, Výkon a modely

The same move that turned `Aplikace` into `Složky a zálohy` two entries ago:
**name both halves of what is actually there.** `Vzhled` held `Jazyk aplikace`
beside the theme and the transcript's type and mentioned one of them; `Nástroje`
held the compute card, the status band and the two folders, and named none of
them. The tabs are:

**`Přepis` · `Jazyk a vzhled` · `Výkon a modely` · `Složky a zálohy` ·
`Informace` · `Aktualizace`**

**The appearance tab gets a key of its own, and the borrowing that stood there
is the reason to write this down.** `SETTINGS_TAB_KEYS` named that tab from
`settings.appearance.title` — the card that fills it — with a comment saying a
second key holding the same word is a second thing to keep in step. That was
true while the two words *were* the same word. The tab is `Jazyk a vzhled` and
the card is still `Vzhled`, so the borrowing turned into the defect it was
avoiding: one string doing two jobs that had parted. `settings.tab.appearance`
is new and the comment now says which borrowings survive and why —
`transcription` still borrows in the other direction, and that one stands,
because the card and the tab both say `Přepis` with nothing between them.

**`settings.tab.tools` is retired rather than reworded**, and the internal name
went with the visible one, as it did when `application` became `files`: the tab
id is `performance` now. The key was used in exactly one place, so changing its
value in place would have worked — and would have left a key called `tools`
holding `Výkon a modely`, which is the same kind of stale borrowing the
appearance key had just been rescued from. `appearance` keeps its id because the
`Vzhled` card it names is still on it; `tools` named nothing that is left.

A machine whose `settings-tab` in `localStorage` still says `tools` opens
`Přepis` once, which is the guard that has made every rename on this screen safe
and is the third time it has earned its place.

**The card headed `Modely` stays**, and this is the answer to a question two
entries above left open. Under a tab called `Výkon a modely` it repeats half the
tab name directly below it — and that is a table of contents, not an echo. The
tab holds four cards and every heading distinguishes one of them; the tab name
is `Výkon` and `Modely` joined, which is exactly what `Složky a zálohy` does over
its folder cards and its backup card. The earlier complaint was about tabs
holding *one* card whose heading repeated the tab and added nothing; that is a
different shape and it is still true of `Aktualizace`. What is still imprecise —
`Modely` over a card whose own description says `Nástroje a jazykové modely` —
is older than today and unchanged.

**And the cards on that tab reordered**: `Výkon`, then `Modely`, then
`Umístění nástrojů a modelů`. The status band used to lead. The tab's name
settles it, and it is the better rule anyway: somebody arriving at a tab called
`Výkon a modely` should meet `Výkon` first.

#### `Jazyk aplikace` as a card of its own

> Jazyk aplikace dej do samostatného bloku

Built the same way as `Jazyk nahrávky` on `Přepis`, so the two screens read
alike: a section with the heading as the label, the same `Select` unchanged, no
`<label>` above it, and the name handed to screen readers through `Select`'s
`description`. That retired `settings.language.label` — `Jazyk rozhraní` existed
only to name a dropdown that now has a heading.

The heading keeps the full `Jazyk aplikace`. Two cards on two tabs both headed
`Jazyk` would be worse than the collision the longer names were chosen to avoid,
and it is the same reason `Jazyk nahrávky` keeps its own qualifier.

**The one thing about this card that is not like its neighbours, written down
because it is invisible on the screen: the app's language is not in the settings
record.** It lives in `localStorage["app-language"]`, is read by `i18n.tsx` and
written by `setLanguage`, and never passes through `save(n)`. So it does not
travel in an exported archive and does not come back with an imported one — a
machine restored from a backup keeps the language it was already showing. Giving
it a card of its own makes it look exactly like every other card on this screen
while it works unlike all of them, which is precisely why the comment above it
says so and says not to wire it into the settings write path to tidy the
asymmetry away.

`settings.appearance.description` went quietly stale in the same move — it still
listed `Jazyk` among what its card holds. It names the theme and the type now.
That is the second time today a card's opening sentence outlived what was on the
card, and the check that catches it is the same one: not *does it read well*, but
*would a reader who believed this be wrong*.

#### `Technické podrobnosti` at the foot of its tab

> Technické podrobnosti dej na konec bloku modely a výkon, stejně jako máš
> pokročilá nastavení

It was the folded footer of the models card. It is a card of its own at the foot
of `Výkon a modely` now, drawn exactly the way `Pokročilé` is drawn at the foot
of `Přepis` — and the *exactly* is the point, because **the two blocks are the
same kind of thing: everything a reader does not need and must be able to
reach.** `Pokročilé` holds the decoding values somebody opens when a transcript
came out wrong; this holds the paths somebody opens when it did not start at
all. A reader who has learnt that the last block on one tab is the one they can
ignore has learnt it for both, and that only works if they look and behave the
same.

`Zkopírovat údaje` and `Otevřít log` moved with it. One thing inside did change,
in the same breath:

> Vloží se do schránky. Nikam se nic neposílá — kam text pošlete, je na vás.
> Tohle info můžeš úplně smazat

**Deleted**, with its Czech and English strings, its context entry and its
`sources.json` fingerprint. It sat under a button that says *copy*, inside a
block nobody opens by accident, explaining what the verb had already said.

The reassurance in its second half is worth keeping and **it was already kept**,
which was checked before deleting rather than assumed: `SECURITY.md` says
`**Nikam nic neposílá.** Přepis, nahrávky ani nastavení neopouštějí počítač.
Žádná telemetrie, žádné hlášení pádů, žádný účet.` That is the same claim at the
level it belongs on — a property of the application, not a caption on one
button — and the diagnostics report is exactly the *hlášení pádů* it names. No
document changed, and `docs:check` does not read this key: it was grepped for in
`scripts/`, in the four documents and in `src/` before the deletion, and lives
only where it was drawn.

The buttons are the last thing in the block now, and the comment on
`.diagnostics-actions` says so — it used to explain the 8 px the note below took.

Two stylesheet consequences, both by widening what is there rather than adding:

- `ToolDiagnostics` dropped its `card-footer` class. That variant is for a
  disclosure folded under other content *inside* a card, taking the 22/20 rule
  that separates it from the action row above; here the disclosure **is** the
  card and there is nothing above it.
- so it takes the rule that zeroes such a disclosure's own top separator, which
  `settings-card-advanced` has had since the block was built — without it the
  14 px line is drawn along the card's own top edge, 26 px in from both sides
  and parallel to the border. The selector covers both cards now and its comment
  says it serves two.

`settings-card-diagnostics` is `order: 4` on that tab, the last number on it, and
`settings-card-app-language` is `order: 1` on `Jazyk a vzhled` with the
appearance card renumbered to 2 — language first, which is the order the tab's
own name states. Every card on every tab still has a rule and no tab has a gap.

- Files: `src/Settings.tsx`, `src/css/04-settings.css`,
  `src/locales/cs/settings.ts`, `src/locales/en/settings.ts`,
  `src/locales/sources.json`.
- Dictionary: 2 keys added (`settings.tab.appearance`,
  `settings.tab.performance`), 3 retired (`settings.tab.tools`,
  `settings.language.label`, `settings.diagnostics.copyNote`) with their
  `sources.json` fingerprints, and
  `settings.appearance.description` reworded. Both new Czech keys have a context
  entry, and two existing contexts were corrected rather than left lying:
  `settings.appearance.title` no longer claims the card holds the interface
  language or that the tab shares its word, and its description entry matches
  what the card is left holding.
- Verified: `npx tsc --noEmit`; `npm run build` (`i18n:check`, `docs:check`,
  `tsc`, `vite build`); `npm test` 132; `cargo fmt --all --check`;
  `cargo clippy --all-targets -- -D warnings`; `cargo test` 187. No Rust changed
  in this round.
- Not verified by machine: the screens. What to press is all six tabs at both
  window widths; `Jazyk a vzhled` with its two cards, and the language dropdown,
  which must still change the interface immediately and must survive a restart;
  `Výkon a modely` in its new order with the red dot on it when something is
  missing; `Technické podrobnosti` opened and closed at the foot of that tab,
  with `Zkopírovat údaje` and `Otevřít log` as the last thing in it and no
  sentence under them; and Settings opened on a machine whose `settings-tab`
  still says `tools`, which must land on `Přepis`.
- **Left undone.** `Aktualizace` is now the only tab whose single card repeats
  its heading, so the note from two entries above narrows to one tab rather than
  two. The answer is still a card that earns the tab, not a special case in the
  stylesheet.

### 2026-08-14 — Six tabs, one word each, and the rule that decides the next one

> Pojďme do jednoslovných

**`Přepis` · `Rozhraní` · `Nástroje` · `Soubory` · `Informace` · `Aktualizace`**

Three of them changed, all from the two-word names the entry above gave them.

**The rule, written down because it decides the next tab as well as these
three.** A row of single words of equal weight reads as a row of peers, and a
two-word name among them reads as more important than its neighbours — which is
a claim about the tab that nobody meant to make. The two-word names arose
honestly: those tabs hold two subjects each and the name was compensating for
it. The compensation was the mistake.

**A single word is allowed only where it is a genuine superset, never where it
is a truncation.** Folders and backups are both files. Performance and models
are both the machinery the transcription runs on. Language and appearance are
both how the application presents itself — which is why `Vzhled` alone would
have been wrong and `Rozhraní` is not: a language is not an appearance, so the
short name had to move up a level rather than drop half the subject. That is the
test to apply to the seventh tab, whenever there is one.

#### The ids, and the trap that has been sprung all day

`Nástroje` and `Soubory` were both tab names earlier today, and `tools` was
retired one commit ago. So the ids were checked against what they hold before
anything was reused, because a key named for one thing holding another is the
defect this screen has produced three times since morning.

- **`performance` → `tools`.** The key `settings.tab.tools` comes back with the
  word it had this morning, `Nástroje`, because the tab went `Nástroje` →
  `Výkon a modely` → `Nástroje` inside one day. Net of the two commits, nothing
  changed — and that is worth saying plainly rather than hiding a round trip.
  It is also the happier accident of the round: a machine that has not opened
  Settings since this morning has `tools` in `localStorage` and lands on the tab
  it was last on, because it is the same tab.
- **`appearance` → `interface`.** `appearance` was kept last round on the
  grounds that it still named the `Vzhled` card living on the tab. Under
  `Rozhraní` it is exactly the truncation the owner's rule rejects for the
  visible name, and the house rule has been that the internal name follows the
  visible one since `application` became `files`. `settings.tab.interface` is
  the key.
- **`files` is unchanged**, id and key both. `Složky a zálohy` → `Soubory` is a
  new value under a key that was already named for it — the one place today
  where the name caught up with the identifier rather than the other way round.

The `localStorage` guard is what makes all of this safe and has now earned its
place four times: a stored tab name this build does not know opens `Přepis`
rather than an empty panel. A machine carrying `performance` from the previous
commit bounces once.

#### No card is headed with its tab's name

Checked after the rename, every card heading against every tab name. One match
was found and fixed, one was found and left, and both are worth recording.

**Fixed: the transcription card is headed `Model`.** It took
`settings.tab.transcription` for its own `<h2>`, which was defensible while the
card was most of the tab. It is not: that tab holds six cards, this one holds a
list of models, and a card wearing the word from the tab strip above it has a
heading nobody chose. `settings.transcription.model` was the label over the list
inside it, so the heading is the label and there is no `<label>` under it — the
third card today built that way, after `Jazyk nahrávky` and `Jazyk aplikace`.
The list is buttons rather than a form control, so nothing needed a name handed
to it separately.

Its opening sentence went with the old heading. *Model, kterým se nahrávky
přepisují* under a heading reading `Model` is the heading again with a relative
clause attached, and the clause was carrying nothing the tab does not already
say. `settings.transcription.description` is retired.

**Left: `Aktualizace`,** whose single card takes `settings.tab.updates` for its
heading. The match is real and the fix is not a rename. What makes an exact
match bad is a card claiming the tab's name while siblings sit under it, and
that tab has no siblings — the heading and the tab are one thing said twice,
which is redundancy without a misleading claim. Renaming the card to a synonym
would invent a distinction that does not exist. The standing answer from two
entries above holds: a card that earns the tab, or a tab that earns more than
one card.

The comment on `SETTINGS_TAB_KEYS` now records both failed borrowings together —
the card taking the tab's key and the tab taking the card's — because they broke
the same way within a day of each other, and six keys is the price of them not
breaking a third time.

#### The licence sentence on `Informace`

> Vše kromě modelů Gemma je open source. Gemma se řídí podmínkami Googlu a
> FFmpeg licencí GPL v3.

Used exactly, and the English follows the same cut. What goes is the trailing
clause *— s tím počítejte, když přenosnou kopii předáváte dál*.

**That clause was not decoration, and where it went matters.** `NOTICE` is the
document of record and keeps it in both languages: *A portable copy does carry
it, because it copies the whole `bin` directory; passing that copy on is
distributing FFmpeg, and GPL v3 applies.* Nothing is lost from the project, and
`docs:check` was checked before the cut — it reads `CLAUDE.md`, `SECURITY.md`,
`ARCHITECTURE.md` and `CONTRIBUTING.md` against paths, classes, tokens and Rust
tests, and pins `SECURITY.md` to the CSP, the asset-protocol scope and the
component digests. No licence sentence is pinned anywhere, in a document or in
the dictionary.

- **What this cut leaves open, as a decision waiting rather than a thing that
  vanished.** A licence paragraph at the foot of `Informace` was a poor place
  for that warning even while it was there. The person who needs it is standing
  at **`Vytvořit kopii` on `Soubory`** — `settings.portable.copyAction`, the
  card `settings-card-portable-copy` in `src/Settings.tsx` — about to press the
  one button in this application that can put a reader under an obligation.
  Nothing on that card says so today. The honest fix is a sentence there, not
  the sentence that was just cut from a page nobody reads before pressing it.
  **The owner has not asked for that and it was not written.** It is named here
  with the file and the button so that somebody can decide it rather than
  rediscover it.

- Files: `src/Settings.tsx`, `src/locales/cs/settings.ts`,
  `src/locales/en/settings.ts`, `src/locales/sources.json`.
- Stylesheet: nothing. No card moved, no order changed, and the `Přepis` card
  that lost its description sits on `h2 + .field`, a rule that has been there
  since the block was built.
- Dictionary: 2 keys added (`settings.tab.interface`, `settings.tab.tools` —
  the second re-added with the value it held this morning), 3 retired
  (`settings.tab.appearance`, `settings.tab.performance`,
  `settings.transcription.description`) with their `sources.json` fingerprints,
  and 2 reworded (`settings.tab.files`, `settings.about.licenceNote`), read
  against their English before approval. The context entries carry the
  single-word rule itself, so a translator reaching for two words is told why
  not before they do it.
- Verified: `npx tsc --noEmit`; `npm run build` (`i18n:check`, `docs:check`,
  `tsc`, `vite build`); `npm test` 132; `cargo fmt --all --check`;
  `cargo clippy --all-targets -- -D warnings`; `cargo test` 187. No Rust changed
  in this round.
- Not verified by machine: the screens. What to press is all six tabs at both
  window widths, and the tab strip itself — six single words in a row is the
  thing being judged, and it is the one thing no check here can see. Then
  `Přepis`, whose first card is `Model` with no sentence under the heading; and
  Settings opened on a machine whose `settings-tab` says `performance`, which
  must land on `Přepis`, or `tools`, which must land on `Nástroje`.
- **Left undone**, besides the portable-copy sentence above: `Aktualizace` is
  still a one-card tab whose card repeats it, and the owner raised
  `Informace a podpora` and set it aside himself — not acted on.

### 2026-08-14 — A seventh tab for `Výkon`, and a model list that fetches what it offers

> Dejme výkon zvlášť

**`Přepis` · `Rozhraní` · `Výkon` · `Nástroje` · `Soubory` · `Informace` ·
`Aktualizace`** — seven, all still one word.

`Výkon` sits third, before `Nástroje`, because where the transcription runs
belongs nearer the transcript than the inventory of what is installed does: one
is about the work, the other about the disk. Only the compute card moved. The
status band, the two folders and `Technické podrobnosti` stayed where they were.

**The seventh tab, and the branch opened by cutting seven to three.** The
numbers are worth having side by side without an argument attached: the original
seven were `Přepis`, `Modely`, `Výkon`, `Mluvčí`, `Slovník`, `Soubory`,
`Pokročilé` — the shape of the code, 61 controls between them, several of which
did nothing or overrode a neighbour. These seven are subjects a reader arrives
with, holding about two dozen controls. Same count, different thing counted.

**`performance` left and came back inside one commit.** The id was retired
earlier today when `Výkon a modely` became `Nástroje` and `performance` became
`tools`; it is now the id of the tab that is actually about performance, which
is what it should always have named. `settings.tab.performance` comes back too,
with `Výkon` rather than the `Výkon a modely` it held this morning. Written
plainly rather than tidied away: this key has now held two different values and
been retired twice, and anybody reading `sources.json` history will see it.

**One card on the tab, so the heading repeats it.** Same shape as `Aktualizace`
and the same answer: an exact match misleads when a card claims the tab's name
while siblings sit under it, and there are none here. Dropping the heading would
make this the only card in Settings without one that is not a disclosure, and
would have the two single-card tabs behaving differently from each other — worse
than saying one word twice. Both are named in the standing note at the end of
this file.

**`order` on both tabs, no gaps.** `settings-card-compute` is 1 on `Výkon`,
alone and still numbered, because a tab with one card is exactly where somebody
would think the rule is unnecessary — and the next card put beside it would take
`order: 0` and jump above it. `Nástroje` renumbers 1, 2, 3 with its three cards
in the order they already had.

**Does `Nástroje` still describe what is left?** Better than it did an hour ago,
and not perfectly. What is on it now is one subject — what is downloaded, where
it is kept, and what was found where — instead of that plus the compute choice,
so the name covers more of the tab than before rather than less.

The strain that remains is real and worth recording rather than defending: a
model is not a *nástroj*, and the application's own vocabulary says so in two
places. The card below is headed `Umístění nástrojů a modelů`, naming them as
two things, and the by-hand list has `Programy pro přepis` and `Jazykové modely
pro přepis` as separate groups. Under the owner's own rule from the entry above —
a single word is allowed where it is a superset, not where it is a truncation —
`Nástroje` is a shade of truncation. **The application already has the superset
word**: `součásti`, which is what `settings.modules.missingRequired` and
`settings.modules.add` call these things in their context entries and what the
download screen calls them. `Součásti` would cover the programs, the models, the
folders and the found paths exactly. It is not changed here, because the owner
named this tab in the same message that named the other six and it is his to
decide; it is written down so it can be decided rather than rediscovered.

#### `Model` offers what it does not have

> Nabízí jen modely, které jsou v počítači. Další se stahují v Nástrojích.
> → Chybějící model se výběrem automaticky stáhne.

**Read as a behaviour change, which is what it is.** The old sentence described
a list of what is on the disk with a signpost to another tab; the new one
promises that picking a model you do not have fetches it. The behaviour was
changed to match, rather than the sentence shipped over the old one.

**The list is an offer now, not an inventory.** `Přesný` and `Rychlý` are always
on it — `TRANSCRIPTION_MODELS` in `types.ts`, the same shape as `EDITOR_MODELS`
and next to it — with the size in the same right-edge slot the language-editing
cards use, `staženo` where it is already there. Anything else on the disk is
drawn beside them and sorted into the same best-first order: `large-v3-q5_0` on
a machine set up before 13 August, an older generation, anything a hand put
there. **A list that is partly an offer must not hide what the disk holds**, or a
reader whose model is not one of the two would find their own choice missing
from the screen that names it.

**`settings.model` is not written until the file is there**, which is the whole
of the care in this change. `tools.rs` resolves that value strictly as
`ggml-{model}.bin` with no fallback, so writing it at the press leaves the
application demanding a model it never fetched — not hypothetical, it is the
defect the wizard's by-hand path produced and which is written up in
`SetupWizard.tsx`. So the press starts the download and records the *intent* in
`localStorage["model-wanted"]`, and `load` in `Settings.tsx` is the only place
that turns an intent into a setting, when `found_models` contains the file.

`localStorage` and not React state, deliberately: the reader must be able to
leave while several gigabytes come down — out of Settings, out of the
application — and the promise has to survive it. There is a `download:progress`
listener while the screen is open, and it does nothing but call `load`; if it
never fires because the reader walked away, the next opening of Settings does
the same work. **Nothing depends on that listener being alive.**

The rest follows from the same rule:

- a download that fails or is cancelled never reaches the branch that writes, so
  the model in force stays exactly what it was;
- while it runs, the card says `stahuje se…` in place of its size and **the old
  card keeps the highlight**, because nothing has changed yet;
- pressing another card cannot recall the bytes, but it does clear the intent —
  whatever lands, what is in force is the last card pressed that is actually
  there.

**The sizes came off the seven `domain.modelDescription.*` sentences.** They were
written into the copy by hand and there is now a slot that shows the catalogue's
own number, so the same figure would have appeared twice on one card — the
redundancy the owner has been striking out all day. It was also already wrong:
`large-v3` said *(3,1 GB)* against the catalogue's 3095 MB, which renders as
3,0 GB. One number, one source.

Checked with the same test as the neighbouring copy and left alone:
`settings.transcription.modelDescription`, the fallback *Stažený model.*, is
still true — it renders only for a model the interface does not know by name, and
the only way one of those reaches this list is by being on the disk.

#### And the sentence over the two folders

> Programy i modely se do složky stahují samy. Cestu měňte jen v případě
> potřeby.

Used exactly. The change is `se do složky stahují samy` where it said `se
stahují samy` — it names what the two fields below are for, which the sentence
had been leaving the reader to infer from the labels.

**The singular over two fields, which is the thing worth saying rather than
editing.** `do složky` stands above `Složka nástrojů` and `Složka modelů`, and it
reads as distributive: each of the two things downloads into its own. It is also
not new. The second sentence has always said `Cestu`, singular, over the same two
fields, and the reader does change one path at a time — the two rows have a
`Vybrat…` each and nothing changes both. So the owner's edit extends the register
the card already had rather than introducing a mismatch, and both sentences would
have to move together if it ever reads wrong. The context entry records that the
singular is deliberate, so a translator does not helpfully pluralise it.

`měňte` is vykání and stays vykání; `i18n:check` enforces that and nothing near
it was touched.

**One thing this sentence does not reach**, noted while reading it: a portable
installation shows `settings.files.locationsPortable` instead — *Relativní cesty
se vztahují ke složce s programem* — which is about drive letters and says
nothing about downloads. So the reassurance that a reader need do nothing is not
on that variant at all. It is a pre-existing split, it is not part of what was
asked for, and it is written down here rather than fixed.

- Files: `src/Settings.tsx`, `src/types.ts`, `src/css/04-settings.css`,
  `src/locales/cs/settings.ts`, `src/locales/en/settings.ts`,
  `src/locales/cs/domain.ts`, `src/locales/en/domain.ts`,
  `src/locales/sources.json`.
- Stylesheet: `order` on both tabs, and nothing else. The size slot is
  `.choice.with-icon > .choice-size`, which was widened for the wizard's quality
  cards earlier today and needed no change.
- Dictionary: 2 keys added (`settings.tab.performance` returning with a new
  value, `settings.transcription.modelDownloading`), none retired, 9 reworded —
  `settings.transcription.modelNote`, the seven model descriptions and
  `settings.files.locationsDescription` — each read against its Czech before
  approval. Two context entries were written rather than left implicit: the
  note's says in as many words that it is a promise about behaviour and not a
  description of what the list contains, so that nobody edits it back into the
  sentence it replaced; and the folders card's records that its two singulars
  are deliberate.
- Verified: `npx tsc --noEmit`; `npm run build` (`i18n:check`, `docs:check`,
  `tsc`, `vite build`); `npm test` 132; `cargo fmt --all --check`;
  `cargo clippy --all-targets -- -D warnings`; `cargo test` 187. No Rust changed.
- Not verified by machine, and this round needs a machine more than any other
  today. What to press is a model that is **not** downloaded: the card must show
  its size, then `stahuje se…`, the old model must stay highlighted and in force
  throughout, and the switch must happen when the file lands. Then the same
  thing with the reader leaving Settings mid-download and coming back, and with
  the application closed and reopened mid-download — both must end with the new
  model chosen. Then a download stopped from the progress bubble, which must
  leave the old model in force and nothing half-written. Then all seven tabs at
  both window widths, and a machine whose `settings-tab` says `performance`,
  which now lands on `Výkon` — the id means what it says again. And the folders
  card on both kinds of installation, ordinary and portable, since only the
  first shows the sentence that changed.
- **Left undone.** `Nástroje` and the word `Součásti`, above. `Aktualizace` and
  `Výkon` are both one-card tabs whose card repeats the tab name. The portable
  variant of the folders sentence says nothing about downloads. And the
  portable-copy licence sentence named in the entry above is still unwritten.

### 2026-08-14 — `Vybrat ručně` leaves the wizard, and the mode it opened stays

> To vybrat ručně už ve wizardu nedává smysl. Dejme tam prostě výpis modelů a
> nástrojů. Nic víc.

**The button is gone from both footers.** It contradicted the screen it stood
on: this wizard asks one question and everything follows from it, and a button
beside that question offering to tick twelve components by hand reads as
*unless you would rather answer twelve questions instead of one*. The list it
opened is not lost — it is on `Nástroje` behind `Spravovat modely`, which is
where somebody who wants it goes.

**And the download step already was the listing he asked for.** `.summary` — a
bordered list of names with sizes, under a sentence carrying the count and the
total — is what the review step has drawn on the guided path all along; the
tickable list only ever replaced it when `manual` was on. So nothing was built.
What changed is that the reader can no longer flip one into the other.

#### Dead, or merely unreachable from here — which, for each thing

The distinction matters and the answer is different for almost every piece:

- **`ManualSelectionButton`** — dead. Both callers were the two footers. Deleted
  with its icon; a comment stands where it was.
- **`chooseByHand`** — dead. It existed to be that button's handler.
- **`manualFrom`** — dead, and it took a defect with it. It remembered which
  question the button had been pressed on so `Zpět` could return there, and the
  guided run was the only thing that ever set it. What was left after deleting
  it was the fallback, `setStep(manualFrom ?? STEP_CHOICE)` — and the fallback
  was the wrong answer for the two paths that remain. See below.
- **`manual`, `manualSelect`, `ManualSelection`, `.manual` in the stylesheet** —
  **alive**, and reached twice. `load` sets the mode from how the wizard was
  opened: `missingModule` for a document asking for its own model, and
  `!required` for `Spravovat modely`. Neither goes through the guided path, and
  both were checked in `App.tsx` rather than assumed — every caller of
  `onToModule` sets `wizardRequired` false, so **`manual` is true exactly when
  `required` is false**. Nothing here was touched.
- **`wizard.manual.switchToManual` and `wizard.manual.switchToSimple`** — dead,
  retired in both languages with their context entries and `sources.json`
  fingerprints. The other `wizard.manual.*` keys are the group headings inside
  the list and are alive.
- **`.summary`** — alive and now the only thing the guided path draws.

#### Three things that stopped being true when the button went

Each was true while `Vybrat ručně` existed and false the moment it did not, which
is why they belong in this entry rather than being left for a later reading.

**`Zpět` out of the by-hand list is the screen it came from.** It read
`missingModule ? onBack() : setStep(manualFrom ?? STEP_CHOICE)`, so
`Spravovat modely` landed the reader on *Rychle, nebo přesně?* — the one screen
that mode exists to avoid, because finishing there writes `model`, `diarization`
and `editor_model` for somebody who opened a list to look at it. That was the
defect fixed earlier today at the entrance and left standing at the exit. It is
`manual ? onBack() : setStep(STEP_CHOICE)` now, which is correct for both ways
in and needs no special case for `missingModule`.

**No step counter and no progress bar in that mode.** `Krok 2 ze 3` numbered a
walk the reader never took, and the three segments with the first already filled
drew it. While the button existed somebody could arrive there from step 1 and
the number meant something. Both are hidden when `manual`.

**`Kontrola výběru` is `Co se stáhne`.** *Your selection* invited the reader to
check something they had selected, on a screen where there is now nothing to
select — and the same heading also sits over the by-hand list, where there is.
The new one is true on both: it names what the screen is about rather than what
the reader is supposed to have done. Its context entry says it covers two
screens, so neither gets reworded for the other.

- Files: `src/SetupWizard.tsx`, `src/locales/cs/wizard.ts`,
  `src/locales/en/wizard.ts`, `src/locales/sources.json`.
- Stylesheet: nothing. `.manual` and `.summary` both still have users, checked
  rather than assumed.
- Dictionary: 2 keys retired, 1 reworded and approved against its English, 1
  context entry rewritten — `wizard.download.reviewTitle`, which also still
  called this the fifth step of a wizard that has had three since 13 August.
- Verified: `npx tsc --noEmit`; `npm run build` (`i18n:check`, `docs:check`,
  `tsc`, `vite build`); `npm test` 132; `cargo fmt --all --check`;
  `cargo clippy --all-targets -- -D warnings`; `cargo test` 187. No Rust changed.
- Not verified by machine: the screens, and there are three entrances to walk.
  A required first run, which must show two steps with a counter and a bar and
  no way to pick components. `Spravovat modely` on `Nástroje`, which must open
  the tickable list with no counter, no bar, and a `Zpět` that returns to
  Settings rather than to the question. And a document action on a transcript
  with no language-editing model, which must open the same list with that model
  and its runtime already ticked, and come back to the transcript.
- **Left undone.** Nothing new. The standing items are `Nástroje` against the
  word `Součásti`, the two one-card tabs whose card repeats the tab, the
  portable variant of the folders sentence, and the portable-copy licence
  sentence.

#### The listing: too big, then missing its sentences, then a switch

Three messages in a row landed on the same list, and the shape is the
resolution of all three rather than a compromise between them.

> Ten výpis je zbytečně velký, ikony nejsou vycentrované, ten výpis to chce
> předělat.

> Hele ty popisky na výpisu modelu chybí, ten dvouřádkový výpis dával smysl.

> Dej tam přepínač na kompaktní a full výpis jako máme na výpisu nahrávek.

**`DownloadListing` is a new component and a new rule**, and the comment on both
says why it is neither of the two things it resembles. Not `.summary`, which is
one line and cannot hold a sentence. Not `.component`, which is a control — a
checkbox, a hit area, a pointer — and **nothing on this screen can be ticked**:
the guided run downloads what the one question implies. The container is
`.summary`'s geometry line for line so the two read as one family; only the row
differs, a two-column grid rather than a flex pair, because the name and its
sentence are one block against the size rather than two things at opposite ends.

**The switch is the archive's shape and not its size.** `.archive-view-toggle`
carries a comment saying its 40 px is 32 px buttons matched to the filter select
beside it — there is no such neighbour here, so this is the plain segmented
control, 30 px in a 36 px track, which is what `CLAUDE.md` documents. It
remembers in `localStorage` under `download-view`, the way the archive remembers
under `archive-view`: this screen is rare enough that re-choosing every time
would be the whole of the interaction. **Compact is the default**, because the
objection to the height came first and the sentences are what somebody opens
when they want to know why an item is on the list.

**Compact stays one line while the download runs**, and that was the thing to
decide rather than let clip. The size gives way to the status — waiting, running,
done, failed — in the same column. Once the bytes are moving, how big each item
was is not the question and how far it has got is.

**A size on every row, in both views.** *A u každého chybí velikost.* The total
above answers *how much*; only the rows answer *which of these is the big one*,
which is what somebody actually asks when a listing says 1,2 GB.

**Nothing complete can reach that list, so no row can say `0 MB`** — checked
rather than assumed. `selected` is built from `!complete` in all four places
that add to it: `root`, the quality model, `DIARIZATION_COMPONENTS` and
`EDITOR_RUNTIME_COMPONENTS`. And `totalMb` is a reduce over that same array, so
**the number agreed to and the number downloaded are one expression apart** and
cannot disagree — which was worth confirming now that two components joined the
guided selection earlier today.

**The `0 MB` the owner saw was on the quality cards**, not in the listing: they
rendered `dataSize(p?.complete ? 0 : p?.megabytes ?? 0)`, so a model already on
the disk wore a `staženo` badge *and* `0 MB` beside it — the same thing twice,
the second time as a number that reads like a cost. The size is simply not drawn
for a model that is there.

#### The by-hand list, rebuilt at the same weight

The list `Spravovat modely` opens got the same treatment, minus the switch and
the read-only part, because it is still a control surface — ticking is the whole
of what it is for. Fifteen framed cards with 6 px between them became one
bordered list with hairlines, at `.summary`'s 10/16 padding and 14/500 type.

**The icons were not centred, and the cause was the layout rather than the
mark.** `.component label` was `align-items: start`, which top-aligned the row
and then needed a `margin-top: 3px` on the checkbox and an `align-self: center`
on the mark to undo itself in two directions. The mark lost on any row whose
text ran to two lines. It is `align-items: center` now and both exceptions are
gone — a row centred as a row needs neither. The mark went 22 px → 16 px, the
size and reason `.release-notes-mark` already documents, with its tick 13 → 10
and its stroke 1.8 → 2 so a smaller line does not read as a fainter one.

**The description is dropped on rows that are already downloaded** and kept on
the rest. It is what somebody deciding needs — a name alone does not say whether
`Detekce řeči` is worth 2 MB — and there is nothing to decide about a component
whose checkbox is ticked and disabled. On a machine that has been set up that is
most of the list, which is where the height was going.

#### `Technické podrobnosti` removed

> Technické podrobnosti dle mého nemají smysl.

Gone from `Nástroje`: the found-paths table, `Otevřít log`, and the report.
Separated before deleting, because the three were never one thing.

The **paths** are what his sentence is plainly about — which `ffmpeg` and which
model file were resolved where, diagnostic output on a settings screen, read by
almost nobody and by nobody who could act on it. **`Otevřít log`** went with
them.

**`Zkopírovat údaje` went too, and it is the one worth naming.** It is not
something a reader consults; it is what they are asked to send when something has
gone wrong, and losing it turns *can you send me what your machine says?* into
*find this file yourself*. **It is not relocated** — a button surviving alone on
another card is a decision the owner has not made. What it takes to put back, in
one commit: `api.diagnosticReport()` calling `diagnostic_report` in
`commands/settings.rs`, `copyPlainText` with `ClipboardRefused` from
`detail/clipboard`, and the six retired keys `settings.diagnostics.copy`,
`copied`, `copyRefused`, `copyFailed`, plus `showLog` and `noLog` for the log
button. Both Rust commands are **left registered and callerless**, deliberately
and for `benchmark_compute`'s reason: a callerless command is cheap, and a
deleted one is a day's work to restore.

`docs:check` passes on all four documents — none of them names the report, the
log path or those keys, which was checked before deleting rather than after.

**And `Nástroje` still describes what is left**: the models status band and the
two folders, which is what is installed and where. The strain named in the entry
above is unchanged — a model is not a *nástroj*, and `Součásti` is the word this
application already uses for the superset.

- Also files: `src/Settings.tsx`, `src/css/04-settings.css`,
  `src/locales/cs/settings.ts`, `src/locales/en/settings.ts`.
- Stylesheet: `.download-list` and `.download-view` are new, next to `.summary`
  whose container they copy. `.check` and its four children are deleted with the
  table they drew, `.diagnostics-actions` with its two buttons, and
  `settings-card-diagnostics`' two rules with the card — including its half of
  the selector that zeroes a whole-card disclosure's separator, which is
  `Pokročilé`'s alone again. Three comments elsewhere that pointed at deleted
  rules now say they point at rules that were.
- Dictionary in this round, all told: 3 added for the switch, 11 retired —
  `wizard.manual.switchToManual`, `switchToSimple`,
  `wizard.download.startTranscribing` and the eight `settings.diagnostics.*` —
  each with its `sources.json` fingerprint, and `wizard.download.reviewTitle`
  reworded.

### 2026-08-14 — The by-hand list gets the switch, and stops having two row shapes

The owner sent a screenshot of that list with five things on it:

> Přepínač chybí, nvidia má dva řádky i na zjednodušeném výpisu, checkbox je
> pořád stejný, chybí mazací ikonka a velikost

Three are done here. The other two — the green circle as the checkbox, and a bin
per row — are a behaviour change with a Rust side to it and are **not in this
commit**; they are set out at the end.

**The switch is on both listings and is one control with one stored value.**
`ListingSwitch` and `localStorage["download-view"]` serve the wizard's read-only
listing and the by-hand list alike. Somebody who reads listings with their
sentences wants them in both places, and a reader who learns the switch on one
screen already knows it on the other. **What the two views mean is identical**:
full adds the catalogue's sentence under the name, compact does not, and nothing
else about a row changes with it. Where the two lists differ they keep only the
difference they have to — this one has a checkbox per row, the other has nothing
to press.

**Two row shapes were being chosen by install state, and that is what the
screenshot was really showing.** `Přepis na kartě NVIDIA` had a description and
a size while every other row had neither, which looked like a compact/full
inconsistency and was not: the sentence and the size were rendered only for
components that are *not* installed. On a machine that is set up, that is one row
in fifteen. It was a defensible rule when it was written — there is nothing to
decide about something already on the disk — and it is the wrong one now that a
switch exists, because a reader pressing `Podrobně` must get the same row
everywhere. **The reader decides the shape; the install state decides nothing
about it.** An installed component keeps its sentence in full view, and *what is
this 3 GB thing I already have* is a fair question to be able to answer.

**A size on every row in both views**, and `staženo` where it would have been
`0 MB` — the same answer the wizard's own listing gives, deliberately, because a
number that reads like a cost is the wrong thing to print beside something that
costs nothing.

**`Co se stáhne` over that list was a mismatch, and the heading is what
changed.** The list is mostly things already on the disk; the heading claimed
everything under it was about to download. The summary line beneath it —
`Stáhne se 0 položek, dohromady 0 MB` — is *exactly* true and is the running
total of what ticking has added up to, which is the one number that list has to
keep showing, so it is untouched. The by-hand list is headed `Modely a nástroje`
now, which is the owner's own phrase for it from two entries above, decided by
the same `manual` flag that already hides the step counter and the progress bar.

- Files: `src/SetupWizard.tsx`, `src/css/04-settings.css`,
  `src/locales/cs/wizard.ts`, `src/locales/en/wizard.ts`,
  `src/locales/sources.json`.
- Stylesheet: one rule, `.manual > .download-view`, and it is specificity and
  nothing else — `.manual > div` sets 16 px on every child of that list and the
  switch is one of them. The two listings must hold the switch at the same
  distance or a shared control stops looking shared.
- Dictionary: 1 key added, `wizard.manual.title`, with a context entry; one
  context corrected — `wizard.download.reviewTitle` no longer claims to cover
  both screens, because it no longer does.
- Verified: `npx tsc --noEmit`; `npm run build`; `npm test` 132;
  `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`;
  `cargo test` 187. No Rust changed.
- Not verified by machine: both listings in both views, and the switch moved on
  one screen then checked on the other. On a set-up machine the by-hand list
  should now show one row shape throughout and a size on every row.
- **Not done, and deliberately: the checkbox and the bin.** The owner asked for
  the green tick circle to become the checkbox and for a bin at the right-hand
  end. That is a behaviour change of a different kind — the first thing this
  application would throw away on request — and it needs, at minimum: a Rust
  command that removes exactly what a component installed, read from
  `installed.json` rather than deleting a folder wholesale; a refusal for
  whatever `settings.model` and `editor_model` name, with the reason visible
  rather than a dead button, because removing one leaves the application
  pointing at a file that is gone; and a `ConfirmationDialog` with a
  `destructive` answer naming the space it frees, since gigabytes are slow to
  fetch back. None of that belongs in a commit about row layout. **And no update
  button**: components are pinned by address and digest in `components.json` and
  move when the application is updated, not per item, so a button promising
  otherwise would do nothing.

### 2026-08-14 — The listing switch is the archive's control, icons and all

> Ten přepínač stručně/podrobně chci identicky jako na archivu, teď jen ikony.

`ListingSwitch` draws the archive's two glyphs rather than an approximation of
them: the same 16 px squares on the same 1.3 stroke, two rounded bars for the
roomy row and three lines for the dense one, in the archive's own
roomy-then-dense order. Copied by reading `ArchiveViewToggle` in `Library.tsx`
and matching it, not by drawing something similar.

**With no words the labels move rather than disappear.** Each button carries
`aria-label` and `title`, and the group keeps its `aria-label`, exactly as the
archive's do — an icon-only control that a screen reader announces as "button"
is not the same control. `wizard.download.viewCompact` and `viewFull` are
`Stručný výpis` and `Podrobný výpis` now, taking the archive's `…výpis` shape so
the two announce alike, and both context entries say the text is not drawn and
why the wording should track its counterpart.

**The 40 px is deliberately not carried over**, and the comment on the component
says so because the next person will compare the two and wonder.
`.archive-view-toggle` documents its own height as 32 px buttons matched to the
filter select beside them in that toolbar; neither listing has that neighbour,
so both take the plain segmented control — 30 px button in a 36 px track, the
default in `CLAUDE.md`. Carry the shape, not the measurement.

- Files: `src/SetupWizard.tsx`, `src/css/04-settings.css`,
  `src/locales/cs/wizard.ts`, `src/locales/en/wizard.ts`,
  `src/locales/sources.json`.
- Stylesheet: `.listing-view-toggle` makes the button square at the plain
  control's own 30 px, which is the whole of the change — the track, the
  radius, the active state and the colours are `.segmented-control`'s already.
- Dictionary: 2 values reworded and approved, 2 contexts rewritten.
- Verified: `npx tsc --noEmit`; `npm run build`; `npm test` 132;
  `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`;
  `cargo test` 187.
- Not verified by machine: the two glyphs beside the archive's, which is the
  only way to see whether *identically* is true.
- **Still not done, and now precisely specified.** The owner settled the row's
  ambiguity better than it had been put to us: *checkbox nedává smysl u
  stažených, tam to má být to zelené kolečko s fajfkou*. So **two marks chosen
  by state, not one doing double duty** — a checkbox where there is something to
  select, the green tick circle in its place where there is not, and the bin on
  that row because removing it is the only action such a row has. The column has
  to hold both marks at one width or every row shifts with its install state.
  The bin itself still needs: a refusal for whatever `settings.model` and
  `editor_model` name, with the reason visible rather than a dead button; a
  `ConfirmationDialog` with a `destructive` answer naming the space it frees;
  and a Rust command removing exactly what `installed.json` recorded for that
  component rather than a folder wholesale. Its own commit, as the first thing
  this application throws away on request. **No update button** — components are
  pinned by address and digest in `components.json` and move with the
  application, so one would promise what it cannot do.

### 2026-08-14 — The bin, and the row it needed

Asked for four times before it was built, which is a scheduling failure worth
recording: it was called "its own commit" and that put the only item in the
round with a backend behind a queue of copy edits. It should have gone first.

**The backend is the previous entry's commit** — `remove_component`, six tests,
187 to 193. This is the screen.

**Two marks, chosen by state, in one column of one width.** *Checkbox nedává
smysl u stažených, tam to má být to zelené kolečko s fajfkou.* A ticked,
disabled checkbox was a control pretending to be a fact: it looked pressable and
refused. The green circle says the one true thing about that row — this is here
— and a row with nothing to select has no checkbox at all. The column is 18 px
whichever mark is in it, because a checkbox and a 16 px circle differ by a few
pixels at their natural sizes and a list whose text left edge moved with each
row's install state would read as a rendering fault rather than as a
distinction.

**The size is on every row**, downloaded or not. It was the tick's slot; the
tick has moved to the mark column where it belongs. What a component takes up is
the question somebody has when they are looking for space to get back, and it is
exactly the row with the bin on it that they need it for.

**The bin is drawn only where deleting means something exact.** `removable`
comes from Rust, answered in `catalog()`, and is false four ways: nothing
installed, the model `settings.model` or `editor_model` is using, and the two
programs that unpack into the shared `bin` root. **No dead button anywhere** — a
row that cannot be deleted has no bin, rather than one that refuses when
pressed. The command refuses regardless, because a drawing is not a guard.

It is 26 px, `.segment-actions`' icon button, which `CLAUDE.md` documents as the
small one: this sits in a 40 px row beside 13 px type and the 32 px circle would
have been the heaviest thing on a line whose whole point is to be quiet. Grey
until the row is under the pointer, then the danger colour — the only
irreversible thing on the screen should not be the first thing the eye lands on
and must be unmistakable once reached. **Shown on `:focus-visible` too**: a
control that appears only under a pointer is one somebody on a keyboard cannot
find.

**It asks, and names the space.** `ConfirmationDialog` with `destructive`, whose
focus starts on Cancel — that component was built for exactly this shape, so a
blind Enter deletes nothing. *Uvolní se {size}. Stáhnout to jde kdykoli znovu,
ale znovu to potrvá.* The second sentence is the one that matters and its
context entry says so: deleting is not irreversible, it is **expensive**, and
for a seven-gigabyte model the undo is the whole download again. A dialog that
said *this cannot be undone* would be false; one that said only *are you sure*
would be useless.

**The list is reloaded from `catalog()` afterwards rather than edited in
place.** `removable`, `complete` and the tick all come from Rust reading the
disk, and guessing any of them here is how a screen comes to disagree with the
thing it is describing.

One thing guarded that cannot currently happen: the bin's handler stops the
click reaching the `<label>` it sits inside. A removable row's mark is the green
circle rather than an input, so nothing would toggle today — but the day this
list draws a checkbox beside a bin, the defect would be a deleted component and
a silently ticked row.

- Files: `src-tauri/src/download.rs`, `src-tauri/src/commands/downloads.rs`,
  `src-tauri/src/main.rs`, `src/api.ts`, `src/types.ts`, `src/SetupWizard.tsx`,
  `src/css/04-settings.css`, `src/locales/cs/` and `src/locales/en/` (`wizard`,
  `errors`), `src/locales/sources.json`.
- Stylesheet: `.component-mark` at one width for two marks, `.component-remove`,
  and the row's grid grows a fourth `auto` column that collapses to nothing on a
  row without a bin — which is what keeps the size column in one place down the
  whole list.
- Dictionary: 6 keys added — three for the bin and its dialog, three refusals
  from Rust — each with a context entry, and the English read against the Czech
  before approval.
- Verified: `npx tsc --noEmit`; `npm run build`; `npm test` 132;
  `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`;
  `cargo test` **193**.
- Not verified by machine, and this needs one: deleting a model and watching the
  row become a checkbox with its size; the bin's absence on the model in use,
  and its return after switching to another; Cancel leaving everything alone;
  and the keyboard path, tabbing to a bin that is invisible until it has focus.
- **Left undone.** `Model přepisu` — the owner's rename of the transcription
  card, which arrived with this and waits for the next commit. And the two
  standing items: `Nástroje` against the word `Součásti`, and a file list at
  install time, which is what ffmpeg and Deno need before their rows can have a
  bin.

### 2026-08-14 — `Model přepisu`

> Model
> Model přepisu

The transcription card's third name in a day, and each rename fixed the one
before it. `Přepis` was its tab's own word. `Model` stopped that and was right
until the language editor became a choice between models too — at which point
`Model` named two cards on one screen and told them apart nowhere. **The
qualifier is the distinction rather than decoration**, which is the same reason
`Jazykové modely` became `Jazykové modely pro přepis` in the by-hand list this
morning: both breaks were caused by the same change, and both are the same fix.

**The key stays `settings.transcription.model`**, and the rule that decided it is
the one written down when `settings.tab.tools` was retired: internal follows
visible. A key named `.model` holding `Model přepisu` is not the stale borrowing
that rule exists to catch — `settings.tab.tools` holding `Výkon a modely` was
stale because the identifier named one thing and the value another. Here they
name the same thing and the value is more precise about it. One user, one key,
no churn.

**And `Jazyková úprava` is not a mismatch beside it**, which was worth checking
rather than assuming after the by-hand list's headings turned out to be one. The
group headings were halves of one split, and halves have to be told apart on the
same axis or the split looks accidental. These are six cards on a tab — `Model
přepisu`, `Jazyk nahrávky`, `Mluvčí`, `Jazyková úprava`, `Slovník`, `Pokročilé`
— which are not halves of anything: they are a list of subjects, already mixing
things with an activity and an adjective. `Jazyková úprava` names its feature
the way `Slovník` and `Mluvčí` name theirs.

Renaming it to a thing would make it worse, not better. `Model jazykové úpravy`
would read as `Model přepisu`'s twin and make the two cards look like one pair,
which they are not: one decides how a transcript is made, the other whether a
second document exists at all. The card is about the feature; the models inside
it are how the feature is set up. **Not renamed, and the owner has not asked.**

- Files: `src/Settings.tsx`, `src/locales/cs/settings.ts`,
  `src/locales/en/settings.ts`, `src/locales/sources.json`.
- Dictionary: 1 value reworded and approved, 1 context rewritten to record that
  `přepisu` is a necessary distinction and not filler, so it is not trimmed back
  to one word by somebody who does not know what is below it.
- Verified: `npx tsc --noEmit`; `npm run build`; `npm test` 132; `cargo test`
  193. No Rust changed.

### 2026-08-14 — Both editor models described the same way, and the sentence stops where the evidence does

> Větší
> Gemma 4 12B. Více parametrů potřebuje víc paměti i času.

Used exactly, and it settles a question that had been open since morning. **The
owner's sentence names the model and then states only what follows from the
parameter count.** Nothing about the output. That is the honest form of the
distinction, and it is what four earlier attempts on this branch were circling
without landing on.

**So `Menší` is built the same way**: `Gemma 4 E2B. Méně parametrů potřebuje míň
paměti i času.` Same construction, same stopping point, and **no counterpart
claim invented to balance the pair** — the asymmetry a reader expects, that the
bigger one is better at the job, is precisely the thing nobody here has evidence
for. `docs/history/` contains no comparison of these models' output; the only
inference ever recorded was run with the 12B alone because it was the only one
installed.

Two things the rewrite quietly fixed. The old smaller-model sentence said
`nejmíň` — a superlative over a set of three, on a card that is one of two — and
`hotová`, feminine, agreeing with nothing on the screen. And the old larger one
opened with `Dělá totéž s víc parametry`, which is a claim about the output
after all: *the same work* is exactly what has not been measured.

**The context entries now carry the rule rather than the conclusion.** Both say
the sentence ends at memory and time deliberately and that *a je přesnější* must
not be helpfully appended, because the difference in memory and time is
arithmetic about a file and anything past it is not. They also say the product
name keeps its digits and capitals in every language. `i18n:check` had no
objection to `Gemma 4 12B` — the hard-coded-text scanner reads components rather
than dictionary values, so a product name inside a translated string is not
something it can mistake for untranslated copy.

`Střední` is left alone. Its sentence does a different job — it says why a model
nothing offers is on the screen at all — and rebuilding it to this pattern would
delete the only thing that explains its presence.

- Files: `src/locales/cs/catalog.ts`, `src/locales/en/catalog.ts`,
  `src/locales/sources.json`.
- Dictionary: 2 values reworded and approved against their English, 2 contexts
  rewritten.
- Verified: `npx tsc --noEmit`; `npm run build`; `npm test` 132;
  `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`;
  `cargo test` 193. No code changed at all — this is copy and the notes that
  keep it honest.

### 2026-08-14 — `Mluvčí` over its switch, `Slovník oprav`, and a sentence that is narrower than the code

Two cards on `Přepis`, both following the pattern the day converged on: **the
heading is the subject and the control under it is the action.** `Model přepisu`
over its choices, `Výkon` over its two cards, and now `Mluvčí` over `Rozlišení
mluvčích`. That it is a pattern rather than three separate decisions is the
thing worth recording.

#### The speakers card needed rebuilding to hold three lines

> Mluvčí
>
> Rozlišení mluvčích
> Spustí rozlišení mluvčích během prvního přepisu.

The card had room for two of them. `SettingsToggle`'s `heading` variant renders
the title as the card's own `<h2>` and keeps `label` for screen readers only, so
`Rozlišení mluvčích` had nowhere to be drawn — the switch was captioned `Mluvčí`
and the middle line did not exist. The card takes its own heading now and the
switch becomes the plain row, which is what makes all three appear without
inventing a fourth.

The repetition of *mluvčích* in the label and in the sentence beneath it is the
owner's and reads correctly in Czech; its context entry says so, so that nobody
merges the two lines to remove it.

#### The sentence is true of the common case and narrower than the code

Checked before shipping rather than after, and the finding is worth having in
full. **`settings.diarization` is read in exactly one place**:
`transcription/mod.rs:571`, inside `run()`, after the dictionary is applied and
before saving. There is one `run()`, reached through one `start_in_thread`,
which has **two callers** — `start_transcription` and `transcribe_in_language`
in `commands/folders.rs`.

So the switch governs *every* transcription run, and the second caller is the
`Přepsat v jiném jazyce` path from a finished recording. **A re-transcription
also runs speaker recognition, and `během prvního přepisu` does not say so.**

The sentence is kept as he wrote it, and the reason is that the distinction it
is drawing is the right one and it is drawn correctly: this switch runs
recognition *as part of transcribing*, against `Rozpoznat mluvčí` on a recording
in `Detail.tsx`, which runs it *afterwards on demand* through
`start_diarization_in_thread`. Those are two different things and a reader needs
to know which is which. For almost every recording the first transcription is
the only one, so the sentence is true as read; where it is incomplete it
promises less than happens rather than more, which is the safe direction for a
switch somebody has deliberately turned on. **The context entry carries the
exception** and points here, so a translator does not tighten *první* into
something that would make it wrong.

#### `Slovník oprav`

> Slovník
> Slovník oprav

The same move as `Model` → `Model přepisu`, and caused by the same thing: a bare
noun that was unambiguous as a whole tab, on a screen that now also holds
language models and a recording language. `Slovník` invites *dictionary of
what*.

And the description became one sentence with the examples at the end:

> Slova, která se v nových přepisech opraví pokaždé stejně: jména, místa,
> odborné výrazy.

**What shifted is which half the sentence opens with**, and it is why the
shortening works. The old one opened with the problem — *slova, která přepis
plete pokaždé stejně* — and closed with the remedy. The new one opens with the
remedy, because the heading now names it: under `Slovník oprav` the sentence no
longer has to say that corrections are what this is, and the room that frees
goes to the examples. **Without that heading the shortened sentence would not
work**, which is exactly the kind of dependency that gets lost between two
commits, so it is in the context entry.

It also answers the test the previous rounds have been applying — would a reader
who read only the heading already know what the sentence is about to tell them?
Here the heading says what the card *is* and the sentence says what *goes in
it*, and neither repeats the other.

- Files: `src/Settings.tsx`, `src/css/04-settings.css`,
  `src/locales/cs/settings.ts`, `src/locales/en/settings.ts`,
  `src/locales/sources.json`.
- Stylesheet: `.settings section > h2 + .settings-toggle:not(.section)` joins the
  selector that already places a card's first field under its heading. Same
  24 px, same relation; a switch's own weight is not what sets that gap.
- Dictionary: 4 values reworded and approved against their English, 4 contexts
  rewritten — including one that did not exist, `settings.speakers.description`,
  which is where the re-transcription exception is recorded. No key was orphaned:
  every one of the four is still drawn, and `settings.speakers.toggle` went from
  invisible to visible rather than away.
- Verified: `npx tsc --noEmit`; `npm run build`; `npm test` 132;
  `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`;
  `cargo test` 193.
- Not verified by machine: the speakers card, which is the one that changed
  shape — three lines where there were two, and the switch opposite the middle
  one rather than opposite the heading.

### 2026-08-14 — `Automatický výběr`, `Rozlišení mluvčích`, and a widened rule taken back out

Two labels, and between them a correction and a round trip that both belong in
the record rather than in a diff nobody reads.

#### The compute switch

> Automatický výběr
> Aplikace sama zvolí nevýkonnější variantu.

**The sentence ships as `nejvýkonnější`, with the `j`.** Without it the word is
*less powerful*, which is the opposite of what the switch does — it would have
told the reader the application deliberately picks the slower of the two cards
above it. Corrected rather than transcribed, and written down here so it does
not read later as an unexplained edit of his wording.

The superlative is also the better form on its own terms and replaces the
comparative he gave earlier the same day. `výkonnější` compares against nothing
named; there are two cards directly above the sentence and the application takes
the top one, so *the most powerful* is what is actually true of a set of two.

`sama` stays, and the reason survived the rename intact: the switch beside the
sentence is `Automatický výběr`, so a description saying *automaticky* would hand
the reader its own label back. Both context entries were rewritten rather than
left — one still named the old `Automaticky`, and the other still said the switch
stands **above** the cards, which stopped being true when it moved below them
two entries ago. A context entry describing a layout that has moved is worse than
none, because it is believed.

Everything else on that card stands: below the cards, the plain row rather than
the heading variant, on by default, and turning it off leaves whatever was
running selected.

#### The speakers card, back to where it started this morning

> Rozlišení mluvčích použij MÍSTO toho jednoduchého nadpisu bloku mluvčí

An hour ago this card was rebuilt to hold three lines — `Mluvčí`, then
`Rozlišení mluvčích` on the switch, then the sentence — because the `heading`
variant could only draw two of them. The longer phrase is the heading now and
the bare noun is gone, which leaves two lines again and a switch row with
nothing distinct left to call itself.

**So the heading is the switch row again**, which is what that variant is for:
`<h2>` and the control on one line, the sentence under both. The alternative —
keeping a separate `<h2>` and letting the switch carry no visible label — would
have drawn an empty caption column beside the heading and made the row taller
for nothing.

**And the selector widened for the three-line arrangement is taken back out.**
`.settings section > h2 + .settings-toggle:not(.section)` had exactly one caller
and that caller is gone; a rule left waiting for a shape nobody draws any more
is the kind of thing that is found in a year and cannot be explained. The
comment where it stood says what it was for and why it is not there.

`settings.speakers.toggle` is retired with its context and its fingerprint —
it held the middle line. One key does both jobs now: `settings.speakers.title`
is the heading **and** the switch's name for screen readers, which is honest
because it is one row.

**On the repetition, which is the fair question:** the description now says
*Spustí rozlišení mluvčích během prvního přepisu.* directly under a heading
reading `Rozlišení mluvčích`. It is on the edge and it is not the same defect
that was tidied away twice today. `Vytvoří nový dokument` and `Používá se`
repeated a **fact** — the second telling added nothing. This sentence restates
its **subject** in order to attach one that the heading cannot carry: *when* it
runs. Czech needs the subject for the verb to have one; `Spustí se během prvního
přepisu.` is the trim if he wants it, and it loses nothing but the echo. Not
rewritten, because he has not asked and the sentence is doing work.

- Files: `src/Settings.tsx`, `src/css/04-settings.css`,
  `src/locales/cs/settings.ts`, `src/locales/en/settings.ts`,
  `src/locales/sources.json`.
- Stylesheet: one selector narrowed back to what it was, with a note where the
  widening had been.
- Dictionary: 3 values reworded and approved against their English, 1 key
  retired with its context and fingerprint, 3 contexts rewritten — two of them
  because they described the compute card's previous layout rather than its
  current one.
- Verified: `npx tsc --noEmit`; `npm run build`; `npm test` 132;
  `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`;
  `cargo test` 193.
- Not verified by machine: the speakers card, which has changed shape twice
  today and should now read as one row and one sentence; and the compute
  switch's label against the sentence under it.

### 2026-08-14 — The by-hand list rebuilt on `.summary`, model names on the cards, and `staženo` as a badge

> Tohle je jako rozsypaná písmenková polévka a ikona košíku není vidět před
> hoverem.

#### The listing, rebuilt rather than patched

**`.summary`, through `.download-list`** — the row built for the wizard's own
listing two commits ago, plus the two columns this one needs: the mark it is
chosen with and the bin it is removed with. The vocabulary was already in the
file and this list was not using it. The two candidates it is not: `.check` is a
read-only panel of facts on `--subtle` and was deleted with `Technické
podrobnosti` this morning, and `.choice` frames every row — which is what this
list was doing when it was called too big an hour earlier.

Four things were wrong and all four were structural:

- **The row backgrounds fought.** Installed rows sat on `--subtle` and the rest
  on `--panel`, which gave the most contrast on the screen to the one row that
  is *not* yet chosen — the opposite of what that contrast should mean. One
  ground for every row now; installed is said by the green mark, which is the
  column that exists to say it.
- **Nothing led.** The name and its sentence sat within a point of each other,
  so fifteen two-line rows had no first thing to read. The name is 600 against a
  13 px quiet sentence, the pair a name over its detail takes everywhere else.
- **The size was not a column.** It was `auto`-width and floated. A shared
  minimum plus `tabular-nums` puts every digit under the one above it, across
  the groups as well as within them.
- **Rows were uneven.** A `min-height` gives them a floor, so a row whose name
  does not wrap is the same height as one whose does.

**And the bin is drawn always.** It appeared on hover, which is undiscoverable —
nobody sweeps a list to find what it might be hiding — and revealing it moved
the size column as the pointer travelled, which was half of why the list read as
unsettled. Quiet by default, `--text` under the pointer, the danger colour kept
for the hover where the intent is already formed. **Its 26 px is reserved on
every row**, including rows that cannot be removed, so nothing shifts.

Compact stays genuinely compact: the difference between the two views is the
sentence and nothing else, which is what the `min-height` and the fixed columns
make true rather than approximately true.

#### The model cards name their model

> U modelu přepisu dej před popis ještě název modelu. Tak jako to máme u modelu
> jazykové úpravy.

Same construction in both places now — **product name, full stop, then what
follows** — and that is the point: a reader comparing the transcription cards
with the editing cards sees one shape.

**The name is written as a reader would recognise it**, matching the Gemma
line's register: `Gemma 4 12B` drops the `-q4` quantisation, so `Whisper
large-v3-turbo` drops the `-q5_0`. `Whisper` is added because `large-v3` alone
names nothing to somebody who has not read the catalogue.

**And the sentence after it was not earned, which is the finding.** Those two
descriptions said *Nejpřesnější čeština* and *Méně přesný* — claims about
wording accuracy, which is exactly what nobody has measured and is where the
middle model's deleted *asi jedna chyba na odstavec* came from. What **is**
measured is time alignment, from 13 August: the turbo model put a block 3.34 s
from where the words are spoken while `large-v3` was 0.56 s out. The wizard's
own summaries have rested on that measurement all along, so the two screens now
say the same earned thing — clicking a word lands on it, or does not.

The five models the application no longer offers got the name too, so a machine
holding `large-v3-q5_0` does not show one card without one. Their sentences say
what they are and that nothing offers them, rather than ranking them.

**The wizard's quality cards do not get the name, deliberately.** That screen is
one question on a first run, where `Whisper large-v3-turbo` is noise — the cards
already carry the estimate and the alignment sentence, which is what somebody
choosing for the first time needs. Settings is where a reader comes back to
compare specific models, and that is where the identifier earns its place.

#### `staženo` is the badge

> Ten tag staženo u modelu přepisu a úpravy, neměl by to být badge?

Checked in all four places before changing any. The wizard's quality cards
already drew a real `.badge`; the two Settings card families put the same word
in the size slot, styled as size text. **That is drift rather than variation** —
one fact stated as a pill on one screen and as quiet right-aligned type on
another — and the fix was deleting the local styling, not adding a rule.

All three are `.badge.complete` now, the grass variant that already existed for
this exact meaning and had one user in `Detail.tsx`. The wizard's was plain
accent, sitting beside `doporučeno`, so the two read as the same kind of
statement when one is a recommendation and the other a fact about the disk.

**On whether the word is worth showing at all**: yes, and there was no
double-telling to trim. On every one of these cards `staženo` *replaces* the
size rather than standing beside it — the slot has two states and shows one.
That was already true, and it is why this is not another `Používá se`.

The size slot keeps the one state that is neither a size nor a fact about the
disk: a model on its way, which is progress.

- Files: `src/Settings.tsx`, `src/SetupWizard.tsx`, `src/css/04-settings.css`,
  `src/locales/cs/domain.ts`, `src/locales/en/domain.ts`,
  `src/locales/sources.json`.
- Dictionary: 7 values reworded and approved against their English. No key added
  or retired — `wizard.download.downloadedBadge` simply changed element.
- Verified: `npx tsc --noEmit`; `npm run build`; `npm test` 132;
  `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`;
  `cargo test` 193.
- Not verified by machine: the listing in both views, which is the thing being
  judged; the bin visible on every removable row without the pointer near it;
  and the two card families' badges against the wizard's.

### 2026-08-14 — The tab is `Modely`, and the row's marks are designed but not built

> Záložku nástroje přejmenuj na Modely a nadpis dej Modely a nástroje.

**Done, and the heading was already right.** `wizard.manual.title` has said
`Modely a nástroje` since the by-hand list was given a heading of its own two
entries ago, so only the tab changed: `Nástroje` → `Modely`.

**This is the reverse of the arrangement everywhere else on this branch and it
is deliberate.** All day the rule has been that a tab names both halves of what
is on it — `Složky a zálohy`, and `Výkon a modely` before it was split. Here the
tab is the narrower word and the page heading carries both. The reason is the
surface rather than the subject: a tab strip is seven words in a row competing
for one line, and a heading has the width to say two. The context entry records
it so the pair does not look like something that got missed.

`settings.tab.tools` keeps its name. The identifier is not renamed for one word
on screen — the rule from this morning is that internal follows visible when the
*subject* changes, and `tools` still names what that tab is about.

#### What is designed and not built

The rest of the round — three messages arriving together — is a redesign of the
row rather than a change to it, and it is set out here so it can be built in one
go rather than half-landed:

- **Both marks become circles of the bin's diameter, sharing one column.** Green
  with a tick where the component is installed; **accent with a download glyph**
  where it is not.
- **The download circle is an action, not a checkbox.** Pressing it fetches that
  one component. This is the reading to take: it matches what the model card on
  `Přepis` already does — picking a model that is missing downloads it — and it
  makes the whole screen one idea, where every row is a thing you either have or
  can fetch. A batch of tick-then-press has no reason to exist once each row can
  act for itself.
- **Hovering the green circle offers a redownload**, with a glyph that is the
  download arrow with a turn in it so the two states read as related. That
  answers a real gap: a component that is present but broken currently needs
  deleting and re-fetching in two steps.
- **The progress bar goes in the row**, at a third size beside the two
  `.progress-bar` already has, and the mark becomes a stop while it runs —
  `cancel_download` exists.

**Two guards this needs, and one of them is not in Rust yet.** Redownloading
writes through a temporary name and renames into place; doing that to the model
a transcription is reading is the failure the bin already refuses for. But
`removable` is the wrong flag to reuse: it is false for ffmpeg and Deno because
their files cannot be told apart in the shared `bin` root, and *replacing* those
is perfectly safe — `install_component` simply unpacks over them. So redownload
wants its own answer, `!component_is_in_use`, exposed the way `removable` is.
That is a small addition to `catalog()` and it should be made rather than
approximated with the flag that is already there.

**And the batch flow is what all of this dismantles.** The summary line
(`Stáhne se {count} položek, dohromady {size}`), the `Stáhnout ({size})` button
and `manualSelect` exist to gather ticks and start them together. With every row
acting for itself they describe a flow that no longer runs. They are **left
standing in this commit deliberately**, because removing them is the same piece
of work as building the per-row action and doing one without the other leaves a
screen with two ways to start the same download. When the row action lands, the
three go with it and `ManualSelection` loses its `selected`/`onToggle` props.

- Files: `src/locales/cs/settings.ts`, `src/locales/en/settings.ts`,
  `src/locales/sources.json`.
- Dictionary: 1 value reworded and approved, 1 context rewritten to record why
  this tab is narrower than its own heading.
- Verified: `npx tsc --noEmit`; `npm run build`; `npm test` 132;
  `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`;
  `cargo test` 193.

### 2026-08-14 — The two model cards in the owner's own words, and two claims that go past the evidence

> Rychlý
> Whisper 3 Turbo. Rychlejší přepis. Zvuk může získat mírný skluz.
>
> Přesný
> Whisper 3 Large. Téměř bezchybný přepis. Precizní časové značky slov.

Shipped verbatim. **This entry exists because two of those four sentences run
against what the rest of the day enforced**, and the place to catch that is here
rather than in an argument later. Both are recorded as findings, not as
objections: it is his copy and his product, and a maker describing his own work
is entitled to a register that a change record is not.

#### `Téměř bezchybný přepis` is not measured

Nothing in this repository has ever compared these models' output on wording.
The claim is the same shape as two that were deleted **today** for exactly that
reason:

- `asi jedna chyba na odstavec`, the middle transcription model's, deleted on
  13 August — and the reason that model stopped being offered at all;
- `Nejpřesnější čeština`, which stood on **this very card** and was removed two
  commits ago, in the entry above, on the grounds that wording accuracy is the
  one thing nobody here has evidence for.

So the reversal is real and it is a decision rather than a slip. What the record
has to prevent is somebody in six months reading `Téměř bezchybný` as a settled
finding and building on it — quoting it in the README, or using it to argue that
some third model is *more* accurate. It is a maker's description of his own
product. It is not a measurement, and there is none behind it.

#### `Mírný skluz` is softer than the number

The measured figure is in `CLAUDE.md` and in the 13 August entry: on the Janka
recording the turbo model put a whole block **3,34 s** from where the words were
spoken, landing in a gap where nobody was speaking, against 0,56 s for
`large-v3`. *Mírný* reads as a fraction of a second. Three and a third seconds
is the difference between clicking a word and hearing it, and clicking a word
and hearing the sentence after next.

Shipped as written, with the number recorded beside it. Anybody reopening this
should know the softer word was chosen with the harder number available.

#### The one that is earned

`Precizní časové značky slov` rests on that same measurement from the other side
and is the most accurate line of the four. It is also the one worth having said
out loud: time alignment is what actually differs between these two models, it
is what somebody clicking a word in a transcript feels, and until now no screen
said so plainly.

#### One name, after three

`Whisper 3 Turbo` and `Whisper 3 Large`. Three forms were in play within an hour
and the record should be plain about which survived and that the others were
**superseded rather than lost**:

1. `Whisper large-v3-turbo` — mine, two commits ago, the technical identifier
   with the quantisation suffix dropped to match the Gemma line;
2. `Whisper V3 Turbo` — his first form, which replaced it;
3. `Whisper 3 Turbo` — his revision, *nebo to V3 dejme jen 3 bez V*, and the one
   that ships.

**Applied to all sixteen places a model is named**, not only the two cards: the
five models nothing offers any more (`Whisper 3 Large q5_0`, `Whisper Medium`,
`Whisper Small`), the catalogue entries the by-hand listing draws, and one
context entry that spelled an older form in prose. Checked afterwards rather
than assumed — no `Whisper V3` and no `Whisper large-v3` survives anywhere in
`src/`.

#### And the catalogue was describing the same models differently

Found while applying the name, and it is the more useful half of this commit.
The two screens had drifted apart: `Přepis` said what the owner wrote, while the
by-hand listing drew `catalog.model-large.description` — **`Nejpřesnější
čeština. Vyplatí se na výkonném počítači.`** That is the very sentence removed
from the card two commits ago for being unmeasured, still being printed one tab
over, with a context entry defending it.

Both catalogue descriptions take his sentences now, word for word, so one model
is described one way wherever it appears. `catalog.model-large-q5` — the middle
model, which nothing offers — lost `Kvalita skoro jako nejvyšší` at the same
time and says what it is instead.

- Files: `src/locales/cs/domain.ts`, `src/locales/en/domain.ts`,
  `src/locales/cs/catalog.ts`, `src/locales/en/catalog.ts`,
  `src/locales/sources.json`.
- Dictionary: 10 values reworded and approved against their English. Four
  context entries written or rewritten — two added where there were none, on
  `domain.modelDescription.large-v3` and `…large-v3-turbo-q5_0`; two corrected
  in the catalogue, one of which had been defending the deleted claim. Each says
  the sentence is the owner's and was taken verbatim, names the unmeasured claim
  or the 3,34 s figure, and points at this entry — so a translator neither
  softens nor sharpens either, and anybody proposing to change them finds the
  history before the string.
- Verified: `npx tsc --noEmit`; `npm run build`; `npm test` 132;
  `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`;
  `cargo test` 193. No code changed.

### 2026-08-14 — `Jazyk nahrávky`, and what setting it actually does

> Správně nastavený jazyk výrazně urychlí přepis. U více jazyků nechte
> automatiku.

Shipped as written. Vykání in `nechte`, which `i18n:check` enforces and which is
right.

#### What the code does, checked rather than assumed

`settings.language` — or the recording's own `language_choice`, which beats it —
goes to `start_whisper` in `transcription/whisper.rs` and reaches whisper.cpp as
**`-l <code>`, unmodified**. `auto` is passed through literally; nothing in this
repository special-cases it, and nothing else in the pipeline reads the language
before the run.

So the whole difference between `-l auto` and `-l cs` is inside whisper.cpp: with
`auto` it runs the encoder once over the opening 30-second window and reads the
language token before decoding, then proceeds identically. **Decoding itself is
not faster with the language set.** One extra encoder pass on one window is a
constant of a second or two against a job measured in minutes — on the wizard's
own estimates, 1 to 35 minutes for an hour of audio.

**On the narrow reading the sentence therefore outruns the behaviour**, and
`výrazně` is the word doing it. Recorded here, beside the two claims on the model
cards in the entry above, under the same arrangement: his words ship and what is
known sits next to them.

#### And on the wider reading it is defensible, which is worth writing down too

The subject is `Správně nastavený jazyk` — a **correctly** set one. Detection
reads a single 30-second window, and it is wrong often enough that the
application has a whole path for the consequence: `transcribe_in_language` in
`commands/folders.rs`, reached from a finished recording, exists so that a
transcript produced in the wrong language can be run again. A mis-detected
opening — music, silence, an English greeting before a Czech interview — costs
the entire run a second time.

Measured against *that*, setting the language correctly does not save a second;
it saves a whole transcription. Whether a reader takes `výrazně urychlí` the
narrow way or the wide way is not something the sentence settles, and the
context entry now names both so nobody sharpens or softens it on the strength of
half the picture.

#### The second sentence is the one earning its place

`U více jazyků nechte automatiku.` tells somebody what `Rozpoznat automaticky`
is **for** — a recording that changes language partway — which the option's own
label does not and cannot. A dropdown entry can say what it does; only a sentence
beside it can say when to reach for it. That is the half of this copy that adds
something no reader could get from the control.

- Files: `src/locales/cs/settings.ts`, `src/locales/en/settings.ts`,
  `src/locales/sources.json`.
- Dictionary: 1 value reworded and approved against its English. No key
  orphaned — the card has a heading and one field, and the description slot it
  might have retired was never filled. Two contexts written: the note had none
  at all, and `settings.transcription.language`'s still said the card was a
  *field* two cards from `Jazyk aplikace`, which stopped being true when both
  became cards of their own on different tabs.
- Verified: `npx tsc --noEmit`; `npm run build`; `npm test` 132;
  `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`;
  `cargo test` 193. No code changed.

### 2026-08-14 — `Kvalitnější` replaces `Téměř bezchybný`, which corrects the entry above

> Tak téměř bezchybná nahraď slovem Kvalitnější

**`Whisper 3 Large. Kvalitnější přepis. Precizní časové značky slov.`**

**This supersedes the finding two entries above**, and that entry stays where it
is: it recorded `Téměř bezchybný přepis` as an unmeasured absolute and it was
right about the sentence that was shipping then. This is the later entry and it
wins; the earlier one still shows what was true for the hour it stood.

**And the revision is a real improvement rather than a softening.** The two
words are not the same kind of claim:

- `Téměř bezchybný` is **absolute** — *almost without error* — and an absolute
  needs a standard. There is none. It is the shape that got `asi jedna chyba na
  odstavec` deleted and the middle model dropped with it.
- `Kvalitnější` is **comparative**, and it has something to compare against
  standing directly above it on the same screen: the `Rychlý` card. It says this
  model is better than that one, which is the claim the card exists to make and
  the one nobody disputes — without asserting a standard nobody has tested it
  to.

So the objection recorded above is answered rather than overruled. A card whose
whole job is to distinguish two models is entitled to say one is better than the
other; what it may not do is say how good either is in absolute terms.

Changed in both places the sentence appears — `domain.modelDescription.large-v3`
for the card on `Přepis`, and `catalog.model-large.description` for the by-hand
listing — which is the arrangement the entry above put in place so that one
model is described one way wherever it is drawn. Both context entries now say
the comparative is deliberate and **must not be sharpened back into an
absolute**, and one of them names the words to avoid.

`Mírný skluz` on the `Rychlý` card is untouched, and its note with the measured
**3,34 s** beside it still stands.

### 2026-08-14 — `Spouští`, not `Spustí`

> Spustí rozlišení mluvčích během prvního přepisu.
> Spouští rozlišení mluvčích během prvního přepisu.

A one-letter change and a real distinction, which is why it is written into the
context entry rather than just made:

- the **perfective** `spustí` describes one future event. That is what a button
  does — press it and a thing happens once;
- the **imperfective** `spouští` describes what happens repeatedly, as a rule.
  That is what a setting is.

The sentence sits under a switch, so it is about every recording from now on
rather than about the next one. `Rozpoznat mluvčí` on a finished recording in
`Detail.tsx` is the other one — a button, one recording, one press — and the
perfective belonged to that all along.

English is unaffected: `Runs speaker recognition during the first transcription.`
was already the habitual present. The context entry says so for the languages
that have no aspect to carry the difference — *Runs*, not *Will run*.

- Files: `src/locales/cs/settings.ts`, `src/locales/cs/domain.ts`,
  `src/locales/cs/catalog.ts`, `src/locales/en/domain.ts`,
  `src/locales/en/catalog.ts`, `src/locales/sources.json`.
- Dictionary: 3 values reworded and approved against their English, 3 contexts
  extended — two recording that the comparative is deliberate, one recording the
  aspect.
- Verified: `npx tsc --noEmit`; `npm run build`; `npm test` 132;
  `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`;
  `cargo test` 193. No code changed.

### 2026-08-14 — Every row acts for itself, and the batch flow comes out with it

> Tady to pozadí u modelu je špatně a ještě máme plný kontext a neudělal jsi
> změny na výpisu modelů jak jsem chtěl minule

Built, all of it, including the part the previous entry named and deferred.
Deferring it was the wrong call: it was written up as a specification instead of
being done, and a specification in a record is not a screen anybody can use.

#### The mark is the action

One 26 px circle per row, the bin's own diameter, so the row is bracketed by two
circles of one size and **nothing shifts as a row changes state**:

- **not installed** — accent circle, downward arrow. Pressing it fetches that
  component and nothing else;
- **installed** — the green tick. Under the pointer it becomes the arrow with a
  turn in it and offers the component again;
- **running** — the ring fills as the bytes arrive and the glyph is a stop
  square. Pressing it cancels.

The tick and the turning arrow are both drawn and one is hidden by CSS rather
than swapped in JavaScript: a mark that re-renders on hover flickers, and the
two have to trade places as one control changing rather than as two controls.
Each state carries its own `aria-label` — `Stáhnout`, `Stáhnout znovu`,
`Zastavit` — because a control whose meaning changes on hover and whose
accessible name does not is a control a screen reader lies about.

**The one state with no action** is an installed component the application is
transcribing or editing with. That row keeps the tick and does not react — no
dead button, the same rule the bin follows.

#### The guard that needed a new flag

`replaceable` on `DownloadComponent`, answered in `catalog()` beside
`removable`, and it is a second flag rather than a reuse **because the two
questions have different answers**. ffmpeg and Deno cannot be deleted — nothing
recorded which files in the shared `bin` root are theirs — but replacing them is
exactly what `install_component` does anyway, so they are replaceable and not
removable. Nothing is the other way round. The one thing `replaceable` refuses
is the model in use: renaming a fresh file over one whisper has open is how a
model is lost mid-run.

#### Progress is in the row that started it

Not the bar at the top, which belongs to the guided run's set. A conic gradient
on the mark rather than a spinner, because the number is known — a spinner says
*something is happening* where the row can say how much — and the size column
gives way to the percentage while it runs, since once the bytes are moving how
big it was is no longer the question.

#### The batch flow is gone

`manualSelect`, the summary line and `Stáhnout ({size})` existed to gather ticks
and start them together. Nothing gathers ticks, so a total of what has been
gathered describes something that does not happen, and two ways to start one
download is worse than either.

**One thing they fed had to survive.** `dokonci` turns a model fetched by hand
into `settings.model` — without it the application goes on demanding a model it
does not have, which is a defect this branch already fixed once. It read the
ticked set; it reads `startedHere` now, **what this visit actually fetched**.
The same question asked of a different source. Deleting a component with the bin
un-records it, so nothing names a model that has just been removed.

`missingModule` — a document asking for its own language model — no longer ticks
a box and waits to be told. It starts the download on arrival: a screen opened
*for* one component that then asks the reader to confirm that component is
asking a question it already has the answer to.

`DownloadedMark` went with the ticking. The row draws its own glyphs by state,
so a component that could only ever draw one of the three had nothing left to
do; `.downloaded-mark` and `wizard.download.downloadedLabel` went with it.

#### The background

The group sat in a container while each row carried its own shade, so the rows
and the thing holding them competed. The container draws the border and **every
row inside it is `--panel`** — and the rule from the earlier fix holds: what is
installed is said by the green mark, not by a grey ground, so an installed row
never carries more contrast than one that is not.

#### `Plný kontext`

Checked rather than changed: the switch defaults to `compact` and stores the
answer under `download-view`, the way the archive stores its own. So a fresh
machine opens compact, and a machine where `Podrobně` was pressed once opens
detailed and keeps doing so. That is the intended behaviour and the reason the
detailed view was still showing; **the switch is working.** If the sticky
preference is itself unwanted the fix is to stop storing it, which is one line
and a different decision from the one that was made.

- Files: `src-tauri/src/download.rs`, `src/types.ts`, `src/SetupWizard.tsx`,
  `src/css/04-settings.css`, `src/locales/cs/wizard.ts`,
  `src/locales/en/wizard.ts`, `src/locales/sources.json`.
- Stylesheet: `.component-row` replaces the `<label>` grid — there is no label,
  because there is no checkbox — and `.component-mark` gains three states. The
  container takes the ground the rows used to fight over.
- Dictionary: 3 keys added with contexts (`wizard.manual.install`, `.reinstall`,
  `wizard.download.percent`), 1 retired with its fingerprint
  (`wizard.download.downloadedLabel`).
- Verified: `npx tsc --noEmit`; `npx vite build`; `npm test` 132;
  `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`;
  `cargo test` 193.
- **`npm run build` does not pass in this tree, and not because of this work.**
  Another change is in progress in the same checkout — `ai_edit.rs`,
  `commands/ai.rs`, `db.rs` and the `progress.ai.*` / `errors.ai.*`
  dictionaries, for custom AI prompts — and `i18n:check` fails on two Rust codes
  and five English strings that belong to it. Nothing in those files is touched
  here and none of them is in this commit; `vite build` is run directly to prove
  the front end compiles. Whoever finishes that change will clear it.
- Not verified by machine: the screen, which is the whole of this. What to press
  is a row's circle on something missing, and watch the ring fill and the row
  become a tick; the stop square mid-download; the green circle under the
  pointer on something installed; and the bin beside it. Then `Spravovat modely`
  on a machine with the model in use, whose row must show a tick that does not
  react and no bin.

### 2026-08-14 — Speaker recognition gets a button, and its card becomes `Vlastní prompt`

> Rozpoznání mluvčích dáme do vlastního tlačítka. A místo karty rozpoznaní
> mluvčích ve vylepšení dáme možnost pro 'vlastní prompt' a do okna s vylepšeným
> textem přidáme záložku, vlastní prompt

Three things, and the first is the one that had been wrong the longest.

#### Speaker recognition was never language editing

It sat in `AI vylepšení` as a third card beside `Věrná úprava` and
`Vylepšená úprava`, and it is not one of them: no language model is loaded,
nothing is rewritten, the transcript is divided between the voices that produced
it. The dialog's own code said as much — `startAiEdit` opened with a branch that
closed the dialog and left through a different door before any of the language
machinery ran, under a comment admitting the card shared the dialog only because
"from the reader's side both are *let the machine work on this recording*".

It is a pill in the detail header now, beside the export menu and the language
button: `Rozpoznat mluvčí`, `Rozpoznat znovu` once there are speakers,
`Rozpoznávám…` while it runs. **It says it with the sidebar's keys**, not new
ones — `detail.speakers.diarize` and its two neighbours already carried exactly
these words for the section's own action, and the same act named two ways from
two places is the drift `i18n:check` warns about. The sidebar action stays where
it is: that one is the way out of a corner, next to the speakers it would
change, and this is the way in.

The seven `detail.ai.*Speakers*` keys went with the card, in English and in
`sources.json` too. One replaces them, `detail.header.speakersMissing`, for the
tooltip when the components are not installed — the press still goes to the
module screen, which is what the card's `Stáhnout` badge did.

#### The card that took its place

`Vlastní prompt`, and choosing it reveals a field under the three cards. The
primary button becomes `Vytvořit dokument` and **cannot be pressed while the
field is empty**; Rust refuses an empty instruction a second time
(`ai.empty_prompt`). Falling back to a prepared mode was the alternative and it
is worse: a document nobody described is a document nobody can trust.

Under the field, the one line this feature is worth spending a line on:

> Pokyn i přepis zpracuje jazykový model přímo ve vašem počítači. Ven neodejde
> nic.

`SECURITY.md` and `NOTICE` both say it already. *Vlastní prompt* is the exact
phrase that makes people assume a service somewhere on the internet, so the
dialog says it where the assumption is made.

#### Where the instruction is kept, and why there

**One per installation, in `Settings.custom_prompt`, `#[serde(default)]`.** Not
per recording: what somebody wants made of a recording — minutes with tasks,
questions and answers, a letter — is usually what they want made of the next
one, and a field that empties itself between recordings makes them retype it
every time. It is written back **as it is typed**, a second after the last
keystroke, not when a run starts: somebody who writes four sentences, thinks
better of it and closes the dialog has still written four sentences. The whole
record is read back before it is written, because Settings writes the whole
record too.

**The answers are per recording and keyed by the instruction that produced
them** — `ai_custom_documents (recording_id, prompt)`. Reword the instruction
and the tab shows its empty state rather than yesterday's answer under today's
question; put the old wording back and the old answer is there. Nothing is
overwritten because a word changed.

#### Why it is not an `ai_output`

`ai_outputs` hangs off `ai_documents` by foreign key, and `save_ai_document`
deletes every row for the recording whenever a document is regenerated. Both are
right for a summary and a translation, which are made *from* the improved
transcript. A custom document is not: the instruction is given about the
recording, its source is the timed transcript, and it is asked for **from the
dialog that offers to make the improved transcript** — which is precisely the
moment when there is none. Hung off `ai_documents` it could not have been
written at all in the case it is most likely to be asked for in.

So it is its own table, referencing `recordings`, created by the same
`CREATE TABLE IF NOT EXISTS` batch every archive goes through. No schema
version: a table an older build does not know is a table it ignores, and
`SCHEMA_VERSION` is for changes that would make it read an archive wrongly.

#### The fourth tab, and the window that now opens for one document

`Vlastní prompt` sits after `Překlad` in `.ai-document-tabs`. The instruction
stands in the strip under the tabs, where the other tabs keep their length and
their language — it is what this document was asked for — and the answer, or the
empty state, below it.

The first three tabs are drawn **only when the improved transcript exists**,
because all three are it or derive from it; the fourth is always drawn. That
makes a window with one tab possible, and it has to be: a custom document made
before any improved transcript would otherwise be shown once and be unreachable
for ever. So `openAiAction` opens the window when either exists, and with no
improved transcript the heading is `Dokument podle vašeho pokynu` rather than
`Vylepšený přepis` — naming the document the reader did not ask for would be
worse than naming none.

One consequence in the stylesheet: the sparkle was selected as
`.ai-document-tabs button:first-child svg`, which stopped being true the moment
the first tab could be a different one. `DocumentViewIcon` marks it `solid` and
the rule follows the icon.

#### What the model is told, and what it is not

`custom_document_prompt` is deliberately thin: that the input is a transcript of
a recording, and that the answer is read as a finished document, so no preamble
and no Markdown fence. Nothing else. The prepared modes tell the model what to
do; here that is the reader's job, and every sentence written over theirs is one
that can contradict it — `system_prompt`'s rule against translating, which opens
and closes it, would quietly refuse `přelož to do angličtiny`. Their instruction
goes last, the end of a prompt being what a model weighs most.

There is no `is_an_edit_of` guard on the answer either. On the improved
transcript it catches a model that translated instead of correcting, because an
edit that keeps none of the wording is a defect by definition. Here keeping none
of the wording may be exactly what was asked for.

**The known limitation, written down rather than hidden:** a transcript longer
than one request is split, and the instruction is applied to each part in turn
with the answers joined — what the translation does. An instruction that asks
for one whole thing (`sestav zápis`) therefore returns one per part on a long
recording. The summary's two-stage pipeline is the cure and cannot be borrowed:
it knows what it is collecting notes for, which is the one thing this feature
deliberately does not know.

#### The transcript is still never overwritten

Nothing here writes to `segments`. The improved transcript, the summary, the
translation and now a custom document are four documents beside it, and
`Zahodit` still only ever removes the improved one. A custom document survives
that button, and survives the improved transcript being made again — it was
never made from it.

- Files: `src-tauri/src/db.rs` (settings field, table, row, two functions),
  `src-tauri/src/ai_edit.rs` (`custom_document_prompt`,
  `generate_custom_document`, the dispatch in `start_output`),
  `src-tauri/src/commands/ai.rs` (status, validation, export, file name),
  `src/Detail.tsx`, `src/detail/documents.tsx`, `src/api.ts`, `src/types.ts`,
  `src/css/04-settings.css`, `src/locales/cs/{detail,errors,progress}.ts`,
  `src/locales/en/detail.ts`, `src/locales/sources.json`.
- Stylesheet: **`.field textarea` is new** — the single `.field input…` rule
  fixes a height of 38 px and an instruction is a sentence or three. It repeats
  that rule's border, ground and padding rather than inventing any, and differs
  in the two ways a box differs from a line: the card radius, because a pill
  crops the corners of the first and last line inside it, and a height it can be
  dragged taller from. `:focus` was added to the existing selector list, not
  written again. Two adjacency rules: `.ai-edit-dialog .choices + .field` takes
  the 14 px `.ai-edit-note` takes, and `.ai-custom-bar` makes the toolbar strip
  a column that closes its own 14 px.
- Dictionary: 15 keys added with contexts, 7 retired with their English and
  their fingerprints. Five Rust codes: `ai.preparing_custom`,
  `ai.custom_chunk`, `ai.custom_ready`, `ai.empty_prompt`,
  `ai.prompt_too_long`.
- Verified: `npx tsc --noEmit`; `npm run build`; `npm test` 132;
  `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`;
  `cargo test` 193. **`npm run build` passes again** — the two Rust codes and
  the English strings the entry above could not clear were this change's, and
  they are in the dictionaries now.
- Not verified by machine: the screens. Press `Rozpoznat mluvčí` in the header
  on a recording with no speakers and again on one with them; open `Vylepšit`,
  choose `Vlastní prompt`, confirm the button is dead until something is typed;
  run one, and confirm the window opens on its tab. Then reword the instruction
  and confirm the tab goes back to its empty state, put the old wording back and
  confirm the answer returns. Then `Vylepšit` on a recording that has only a
  custom document, which must open the window on that document rather than the
  dialog.

### 2026-08-14 — One weight for every file size

> Velikost těch souborů nedávej tučně. Je to zbytečně velké.

All three size columns were at 600, which is the weight the **name** carries —
so the number competed with the thing it belongs to instead of supporting it.
They are 13 px, 500, `--text-quiet`, `tabular-nums` now, and they are one rule
rather than three that agree by accident.

**500 was chosen against its neighbours rather than picked off the scale.**
Prose beside it is 400 — `.small-text` — and the name above it is 600. A figure
somebody scans down a column belongs between the two: heavier than a sentence so
the column reads as a column, lighter than the name so it supports rather than
competes. At 600 it *was* the name's weight, which is the whole of why it looked
loud.

**No exception for the cards, and it was considered rather than skipped.** On a
`.choice` the size is genuinely part of the decision — pressing that card fetches
those gigabytes — while in a listing it is reference somebody consults. That is
a real difference and it still does not earn a heavier weight: on a card the
figure already has its own column at the right edge and is the only number
there, so weight is not what makes it findable, and at 600 beside a 15/600 title
the two read as equals. One pair everywhere.

**The two status words share that column and go with it.** `Hotovo` and `Chyba`
in the wizard's running listing were 600 on top of `--success` and `--danger`.
Colour is what sets those apart; the weight was saying the same thing a second
time, in the one column that had just been asked to stop shouting.

**On readability at the lighter weight**, which was worth answering rather than
assuming: `--text-quiet` at 13 px is what every helper sentence in this
application already uses at weight 400. At 500 the size is *heavier* than prose
that ships on every screen, so it cannot be less comfortable than text already
sitting beside it. No size bump was needed, and putting the weight back would
have been the wrong lever anyway.

Checked and left alone: `.mic-time` in the recorder is 13/600 with
`tabular-nums` and looks like a fourth member of this family. It is not a file
size — it is a clock counting a take as it is recorded, where the number *is*
the subject rather than a note beside one.

- Files: `src/css/04-settings.css`.
- Stylesheet: three rules brought to one pair, two `font-weight` declarations
  deleted from the status colours, and a comment on `.choice-size` carrying the
  reasoning for all of them — including why the card is not an exception, so the
  next person does not restore it as one.
- Verified: `npx tsc --noEmit`; `npx vite build`; `node scripts/docs-check.mjs`;
  `npm test` 132; `cargo fmt --all --check`;
  `cargo clippy --all-targets -- -D warnings`; `cargo test` 193. `npm run build`
  still fails in this checkout on the other change in flight — the
  `progress.ai.*` and `errors.ai.*` dictionaries — which is why its two halves
  are run directly here. Nothing from that change is in this commit.

### 2026-08-14 — The privacy line becomes an info note and loses half its words, and a textarea stops resizing its dialog

> Pokyn i přepis zpracuje jazykový model přímo ve vašem počítači. Ven neodejde
> nic.
>
> To dej s info ikonkou:
>
> Pokyn zpracuje jazykový model přímo ve vašem počítači.

#### What the shortening gives up, and where the claim still stands whole

`Ven neodejde nic.` was the explicit reassurance and it is gone. What carries it
now is `přímo ve vašem počítači` beside a circle-i — an info mark reads as
reassurance by its nature, and the sentence it marks says where the work
happens, which is the fact the assurance was drawn from.

**That is a change to the wording and to nothing else.** Nothing leaves this
machine, exactly as before: the model runs locally through llama.cpp, and the
claim stands in full in `SECURITY.md` and `NOTICE`, which is where it is made at
length. Nobody reading this entry later should take the shorter line as a
narrower promise.

Both places got the same note from the same key. The card in `AI vylepšení` is
where somebody meets the words *vlastní prompt* first, so the implication is
doing most of its work there — but the tab in the preview window is where the
instruction is rewritten, and a reassurance shown only the first time is one the
reader cannot go back to.

It is `InfoNote`, the component, not something styled to look like one. Not
`compact`: it belongs to the single field above it, which is the 8 px position
`.settings-info-note` already carries by default — the 14 px variant is for a
note that belongs to a whole group. `.ai-custom-bar .field > .small-text`, which
set 6 px there, came back out: the position had a rule already.

#### And the same field was resizing the dialog around it

> Když jsem roztáhl okno na prompt stalo se tohle.

A screenshot: the textarea dragged by its handle until the dialog was as tall as
the window, `Vytvořit dokument` on the bottom edge and the transcript behind it
a sliver. `resize: vertical` was mine, written without a ceiling, and a control
that can resize the shell it is inside is the defect — `CLAUDE.md` fixes the
dialog widths and the reading window's height precisely so that no dialog can
outgrow the window.

Fixed at the cause and then at the shell, in that order:

**The textarea is bounded.** 84 px to start — three lines at 14/1.5 with the
padding, which is what an instruction to a model actually is — and 160 px at the
most, six lines. The handle stays, because three more lines is a real thing to
want, and 160 is chosen so that the handle cannot do what the screenshot shows:
the tallest dialog holding one of these comes to about 610 px with the field at
its ceiling, inside the 660 px minimum window height in `tauri.conf.json`. Past
six lines the box scrolls, the way `.release-notes` does at 240 px for the same
reason.

**And the shell has a ceiling of its own**, which it did not: `.dialog` had no
`max-height` and no scroll, so any tall content — not only a dragged handle —
pushed the footer under the bottom of the screen, where it cannot be pressed at
all. It is `calc(100vh - 48px)` now, the window less the overlay's own 24 px on
each side, with `overflow-y: auto`. The reading window sets its own height above
this rule and never reaches the scroll; the preview's `Select` opens 6 px under
its trigger near the top of a 760 px box, so nothing that has to escape a dialog
is clipped by it. This is the guarantee the number in `CLAUDE.md` was already
making on paper.

- Files: `src/Detail.tsx`, `src/css/04-settings.css`,
  `src/locales/cs/detail.ts`, `src/locales/en/detail.ts`,
  `src/locales/sources.json`.
- Dictionary: 1 value shortened, its context rewritten to say that the info mark
  is carrying half the sentence, and the English written and approved —
  `The instruction is processed by the language model on your own computer.`
- Verified: `npx tsc --noEmit`; `npm run build`; `npm test` 132;
  `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`;
  `cargo test` 193. No Rust changed.
- Not verified by machine: drag the handle in both places and confirm it stops
  at six lines with the footer still on screen, on a window shrunk to its 660 px
  minimum.

### 2026-08-14 — The shortcut strip's switch comes back, and the button that replaced it goes

> Tlačítko na zobrazení rychlých tipů dej pryč. Dejme jim toggle v nastavení
> rozhraní tak, jak jsme ho měly.

**This reverses a decision made earlier on this branch, and the reversal is the
point of the entry.** The keyboard button beside the player was added on a
principle worth keeping: a way back belongs where the thing disappeared, not
three clicks away in Settings. That is why permanence was called "the trade: one
fewer control against one fewer way back" and why the button was tried at all.

**The principle was not wrong. It lost to the screen.** He has now seen both and
prefers the switch, and that is the kind of question only looking at it settles —
which is the whole reason this record exists rather than a list of rules.

So: the button is gone from the player's trailing controls, `showTips` went with
it as its only caller, and `Zobrazovat tipy nad přepisem` is a plain
`SettingsToggle` at the foot of the appearance card on **`Rozhraní`** — the tab
that card sits on, which is where he asked for it and where it would have been
had it survived the simplification.

#### The stored value was checked before anything moved

This is the part that could have cost somebody their dismissed strip. The strip
reads `localStorage["rychle-tipy"]` and treats `skryte` as hidden, anything else
— **including absent** — as shown. The button never changed that: `showTips`
removed the key and `hideTips` wrote `skryte`. The restored switch writes
`viditelne` or `skryte`, both of which the strip has always read correctly, so a
strip dismissed before this change stays dismissed after it and one dismissed
after stays dismissed on the next build. Nothing migrates because nothing needed
to.

Like the interface language, **this is not in the settings record** and the
comment beside it says not to move it there to make the card uniform. It is a
habit of one machine rather than something the archive carries, and `Detail.tsx`
reads the same key directly.

#### Placement

A plain row rather than the heading variant, because the card it joins already
has `Vzhled` at the top — the same reasoning that put the compute switch under
its cards. It sits last, after the live type sample, which is the one thing on
that card it is not about.

**No new `order` rule**, because no new card: it joined one. The tab's existing
pair still reads right — `settings-card-app-language` is 1 and
`settings-card-appearance` is 2, so the language a reader chooses first and the
appearance they adjust second, with the strip's switch at the bottom of the
second.

One selector widened: `.preview-font + .settings-toggle:not(.section)` joins the
24/22 divider that already places a plain switch after a field or a column of
cards. A framed sample is a block of content in the way that matters, and the
switch is the next thing after it.

#### Keys

`detail.tips.show` — the button's label, its only user gone — is retired with
its context and its fingerprint. `settings.tips.toggle` and
`settings.tips.description` come back with the words they had before the
simplification deleted them, so nothing was invented for a control that already
had copy. `settings.tips.title` stays retired: it was the heading of a card of
its own, and there is no card now.

And `detail.tips.hideHint` is corrected for the second time in two days. It said
*vrátit se dají v nastavení*, which the button made false; it was changed to name
the button; it names the setting again — `Skrýt. Vrátit se dají v nastavení
rozhraní.` — now with the tab named, which the original could not do because the
tab did not exist yet.

- Files: `src/Detail.tsx`, `src/Settings.tsx`, `src/css/04-settings.css`,
  `src/locales/cs/settings.ts`, `src/locales/en/settings.ts`,
  `src/locales/cs/detail.ts`, `src/locales/en/detail.ts`,
  `src/locales/sources.json`.
- Dictionary: 2 keys restored with contexts, 1 retired with its context and
  fingerprint, 1 reworded and approved.
- Verified: `npx tsc --noEmit`; `npm run build` — green again in this checkout,
  the other change having landed; `npm test` 132; `cargo fmt --all --check`;
  `cargo clippy --all-targets -- -D warnings`; `cargo test` 193.
- Not verified by machine: dismissing the strip with its ×, then finding the
  switch off on `Rozhraní` and turning it back on; and the same in reverse, with
  the application closed in between.

### 2026-08-14 — A bin on every row, a lock only where something is working, and a file list to delete by

*One sitting, and the module listing was rebuilt around one question the owner
asked about it. The sections after the first two are what he sent back while it
was being built: the lock and its tooltips, the group spacing, the download
ring, two catalogue names, the button that opens the screen, and the two numbers
the card now states.*

> Proč je u některých položek v seznamu nástrojů a modelů košík na mazání a
> jinde ne? Měl by být všude a zámek jen u těch, které se aktuálně používají.
> Například když běží převod nebo přepis.

Two separate causes behind one symptom, and neither was the other's fault.

#### One: nothing recorded which files were whose

`ffmpeg` and `deno` unpack into the **shared** `bin` root, beside `yt-dlp.exe`
and the per-build folders. `installed.json` held the address, the digest and the
date — where a component came from, which is what it was written for and is not
a list of files. So deleting the folder would have taken every other program
with it and deleting only `verification_path` would have left the libraries
behind, and the comment on `removable_path` named the honest fix rather than
attempting a clever one: *a file list written at install time.*

**It is written now.** `InstallRecord` carries `files: Option<Vec<String>>`,
paths relative to the programs or models folder in exactly the form the
catalogue writes them — `bin/ffmpeg.exe` — so `tools::component_path` reads them
back. `collect_programs` returns what it copied instead of counting it, and the
`main.exe` → `whisper-cli.exe` copy that happens *after* the unpacking is
appended, because a list that misses a file this installation wrote is a removal
that leaves something behind.

**`None` is not an empty installation.** It is an installation made before the
list existed, which is every machine in the world today, and that is why the
field is an `Option` and not a `Vec`. Those records go on loading; the component
stays undeletable and **the row now says so** rather than showing a blank
column. Fetching it again writes the list and the bin appears. Guessing at the
files would have been the wrong thing done confidently.

**What is not read from the list, deliberately.** A model is one file and a
build is a folder of its own, and both were already answered exactly by the
destination. The folder is the better answer even now that a list exists: it
takes the empty directory with it, and it cannot miss a file written after the
unpacking. So the list is the answer for the shared root and only there, which
is where nothing else can answer — and the brief's stricter reading, *no list,
no bin, for everything*, was not taken: it would have removed the bin from every
multi-gigabyte model on every existing machine in order to fix two programs of
106 and 40 MB. That is the opposite of what was asked for.

**The escape guard survived intact and grew a rule.** Every path is checked
before any of them is deleted, `..` before the prefix check — `Path::starts_with`
compares components without resolving, so `bin/../../outside.exe` starts with
`bin` as far as it is concerned — and **one bad entry refuses the whole
operation**. Half a removal is not a removal; deleting the sound files first and
then stopping is a mess with an error message.

#### Two: the lock was on the wrong question

A component was undeletable because it was **named in settings**, so the model
somebody transcribes with could never be deleted at all. The owner's sentence is
the correction: *zámek jen u těch, které se aktuálně používají.* Choosing a model
is not using it. A transcription is.

`Busy` is a snapshot of what the application is doing this moment —
`transcribing`, `editing`, `importing`, `converting` — and every field is read
from the registry that already knows, rather than from a second list kept in step
by hand: `TranscriptionTask::anything_running`, `AiEditTask::anything_running`,
`OnlineImportTask::is_running`, the waveform jobs in `main.rs`. `busy_now` in
`commands/downloads.rs` gathers them, because that is the one place all of them
are in scope; what they *mean* stays in `download.rs` beside the catalogue it
decides about.

- **A transcription locks its whole run.** ffmpeg, speech detection, the voices,
  all three whisper builds and the model in settings. It is deliberately coarse:
  one worker runs ffmpeg, whisper and sherpa in turn and writes down neither the
  stage nor the build `choose_compute` picked, so the honest answer while a job
  is alive is that any of them may have a file open. Nobody frees 17 MB of
  processor build in the middle of a transcription.
- **A conversion is ffmpeg alone**, and that is the case the owner named. The
  waveform has a registry; the seekable playback copy had none — *nothing owns
  this one*, as the comment at its caller has always said — so `tools.rs` counts
  them now, raised and lowered by a guard rather than by a line at the end, the
  same shape as `WaveformJob` and `INSTALLING` and for the same reason.
- **An import is yt-dlp, its JavaScript and ffmpeg after them.**
- **Language editing is llama.cpp and the model `resolve_editor_model` actually
  resolved** — the resolver rather than the stored name, because that is the
  function which decides what was loaded.

Windows would answer part of this by itself: a running `.exe` cannot be deleted.
But as a refusal partway through a list of files, after some have gone. That is
not a guard.

#### The configured model, and what deletion leaves behind

This is the decision the brief asked for and it needs to be written down plainly.
**Deleting the chosen model is allowed, and it never leaves the settings naming a
file that is gone.** `tools.rs` resolves `settings.model` strictly as
`ggml-{model}.bin` with no fallback, so a stale name is a transcription that
refuses to start — the same failure this branch fixed from the other direction
this morning, when a model was written into settings before it had landed.

So `settings_after_removal` **points the setting at another installed model, and
clears it when there is none.** Which model is not invented: it is the first
installed in the order the first-run answer implies — `quality_choice` `fast`
gives turbo first, anything else gives large first — and the language editor
falls through `12B, E4B, E2B`, which is the order `resolve_editor_model` already
uses, so the setting and the resolver cannot end up naming different files.
Clearing is a state the application already knows and handles: it asks for a
model. It is not a dead end.

**And the confirmation says which**, before anything is deleted rather than
after. `replaced_by` on the catalogue row is the same answer the command writes
afterwards — one question asked once — so the sentence cannot promise one thing
and the setting say another. Three sentences for three situations: a spare says
what it frees, the chosen model with a successor names it, and the chosen model
with none says the application will ask again when it needs one.

#### The lock, after the owner saw the blank column

> OK tak těm dej ikonu zámku a napiš do tooltipu na hover, co znamená.

Right, and the reason is worth keeping: **an absent control explains nothing.**
A bin on one row and blank space on the next reads as a defect rather than as a
rule, and the reader is left guessing which.

A `lock` glyph — two subpaths, the body and the shackle, the complexity of
`video` beside it in the set, and no keyhole because at 16 px it turns to grain.
It sits in the bin's own column at the bin's own 26 px, so nothing shifts as a
row changes state. Quiet colour, not danger: it states a fact, it does not warn.
It is not a button — no hover, no pointer, nothing suggesting a press does
anything — and the sentence is both `title` and `aria-label`, because a tooltip
alone is read by nobody using a screen reader.

**Two locks and two sentences**, which is the whole point of the tooltip:

- **busy** — `Právě se používá. Až práce skončí, půjde smazat.`
- **unlisted** — `Aplikace neví, které soubory k této součásti patří. Po
  opětovném stažení ji smazat půjde.`

A drawing cannot carry that difference and is not asked to. Rust decides which,
on `remove_block`, because both answers are about the disk and about what the
application is doing and neither is anything the window could work out. Where
both are true the row says **busy**: it is the answer to *why is there no bin
right now*, and the moment the work ends the row explains itself the other way
with nobody doing anything.

**Both sentences were longer and were cut**, and the rule behind the cut is
worth more than the two strings:

> Aplikace nemá zapsané... ten odstavec nedává smysl. Dokázal bys to říct
> krátce a smysluplně?

The first version explained the **mechanism** — that no record was kept of which
files were written — and a reader does not care how the application keeps its
books. What they want is why it cannot be deleted and what to do about it. Six
words for the reason, one short sentence for the way out; and the mechanism
moved into the **context entry**, where a translator needs it and a reader never
sees it. The busy one got the same treatment: it no longer names which of the
four kinds of work is running, because the reader does nothing with that either.

**The refusals in Rust say the same words.** `errors.download.component_busy`
and `errors.download.cannot_remove` are now character for character the two
tooltips. One fact said in two places — on the row, and if the work starts
between the list being drawn and the press — and saying it two ways would be
exactly the drift `i18n:check` exists to catch. Both context entries say so and
tell the translator to keep them identical.

#### And the group headings got their space

> A prosím abys těm nadpisům těch oddílů ve výpisu modelu dal více prostoru

**The space belongs above a heading, not below it.** A heading names what
follows, so the gap that separates it from the group before is what makes the
listing read as sections; a gap underneath detaches it from the rows it names.
It was 16 px between blocks against 8 under the heading — close enough that a
heading floated between two groups and belonged to neither, which is very likely
the whole of the complaint.

24 px above, which is the amount this stylesheet already separates the bands
inside a card by; 16 is what it puts between cards, and a group boundary inside
one listing is the larger of those two kinds of gap. It is more than half a
row's height in either listing view, so the boundary reads whether the rows are
one line or two. `.manual-group + .manual-group`, so **the first group takes
none** and still sits at the 10 px the view switch above it keeps — a plain rule
would have opened the list with a hole. The blocks needed a class for that
adjacency: `div + div` would have caught the switch.

The outer distance did not change. `.manual`'s own 4 px became 20, which is what
the last group's 16 plus that 4 already came to, and `.manual > .download-view`
went with the rule it existed to beat.

#### The download ring is the mini player's

> Ten progress ring je při stahování moc silný. Udělej ho tenší. Možná dva nebo
> tři pixely.
>
> Stejně, jako je v mini playeru.

The second sentence is the better answer and it is why no number was picked.
**The application already had a ring saying how far through something is** — the
one around the play button in the header's pill — and a second one at another
weight would have been two visual languages for one idea. One thickness, one
behaviour, learned once.

The two were drawn differently, which mattered: the player strokes two SVG
circles and shortens the arc with `stroke-dashoffset`; the row painted a conic
gradient across the whole 26 px circle and punched the middle out with an
`::after`, giving a 3 px band running edge to edge. **So the technique came
across with the weight**: the row draws the player's ring now, reusing
`.mini-track` and `.mini-progress` themselves rather than copying their numbers,
so the stroke cannot be changed in one place and not the other. Stroke 2, moved
from the player's 30 px box to this 26 px one at a radius that leaves the same
1.5 px to the edge.

- **Three per cent is visible**, because the track is drawn under the arc: a
  ring that only appears once a download is well under way reads as *nothing is
  happening* in the seconds when the reader is watching hardest. Against the
  track, the round cap is a mark on a circle from the first tick.
- **The stop square is untouched and was checked rather than assumed.** The old
  hole was 20 px across and the new ring's inside is 19, so the square has the
  air it always had, and it is the 10 px the tick beside it is drawn at. The
  circle stays 26 px: it holds its column against the tick, the arrow, the bin
  and the lock.

#### `Podpora online zvuku a videa`

> Online videa
> Podpora online zvuku a videa

Applied. It is more accurate as well as longer: the component pulls the **audio**
out of a web page, and podcasts and audio-only pages go the same way. The old
name said what the source usually is rather than what the thing does.

**The sibling was renamed to match**, and this was a judgement rather than an
instruction. `deno` is the other half of one feature — one fetches, the other
gets past YouTube's checks — and leaving it as `JavaScript pro online videa`
would have had the pair describing one subject two ways, one line apart in the
same group. It is `JavaScript pro online zvuk a video`. Nothing else changed;
the description is untouched, since which sites are supported is a separate
question the owner has not settled.

**The fit was checked where a catalogue name stands alone**, which is the rule
that broke `Stahuji Větší` earlier on this branch. Both rows wrap in the two
listings; the corner bubble truncates with an ellipsis, and at 28 characters the
new name is shorter than `Jazyková úprava na grafické kartě` at 33, which has
been going through that same slot all along. So no new limit — but a limit that
was already there. What is new is a smaller thing, recorded because somebody
will notice it: `Stahuji Podpora online zvuku a videa` leaves a feminine
nominative in a slot that wants the accusative. `Stahuji Detekce řeči` has read
that way since the catalogue was written; the sentence takes names as labels and
does not decline them, and fixing that is a change to the pattern rather than to
this name.

#### `Spravovat modely a nástroje`

> Modely
> Modely a nástroje
> Spravovat modely a nástroje

The first two are what the screens are already called — the card on the tab is
headed `Modely` and the by-hand screen it opens is headed `Modely a nástroje` —
so the change is the third: **the button that goes there is named for where it
goes.** It said `Spravovat modely` and landed on a screen headed `Modely a
nástroje`, which is enough to make a reader wonder whether they arrived in the
right place; and half the rows there are programs.

It is the only button that opens that list — the same button says `Doplnit`
when something required is missing, which stays — and `.button` is
`white-space: nowrap` with `flex: 0 0 auto` in that row, so the longer label
takes its width from the sentence beside it rather than wrapping or pushing the
row about.

#### And the card says what this machine has

> Chtělo by to tam nějaký dashboard, třeba 33 modelů, celkem zabraného místa
> atd. Na kartě modely v nastavení

> Dej do té tabulky ikony a ten počet součástí dej 12 z 13 nebo tak.

**Two rows**, in the `.about-panel` the `Informace` tab already uses, in its
marked variant: `Stažené součásti` and `Zabrané místo`, each with the 30 px
`.about-mark` circle that page's two fact rows wear. Two rows and not six — this
answers *what do I have and what is it costing me*, and a column of statistics is
a diagnostics panel, which is a thing this branch deleted today for good reason.
Icons are a reason to have fewer rows, not more: every row carries one or none
would, which is the rule that panel was given this morning.

**The two glyphs are the two subjects.** `download`, an arrow into a tray, for
what has arrived on this computer; `disk`, the stack of platters, for the space
it takes. Both are new in `LINE_ICONS` and both were drawn rather than borrowed
because nothing there said either thing — `folder` was the near miss and was
turned down deliberately: beside a downward arrow it reads as *where they are
kept*, which is the card directly below and a different question. A mark a
reader tries to read and cannot is worse than no mark.

**The count is a fraction and is counted in the window.** `12 z 13` — installed
out of offered. A bare total answers nothing anybody wanted; the fraction answers
*is anything missing* at a glance, which is the question the card is about and
what the sentence beside it says in words. The denominator is what the by-hand
listing draws — the catalogue less the two middle models nothing offers, plus
either of them where it is already on the disk — so the number can be checked by
counting rows rather than taken on trust. Which components are offered is the
window's own knowledge (`UNOFFERED_COMPONENTS`), and the catalogue it already
holds carries a `complete` on every row, so Rust was left out of this half
entirely: `installed_summary` became `installed_megabytes` and answers one thing.

**The size is measured off the disk, and that is the whole of the work.** Adding
up the catalogue's `megabytes` would have been one line of TypeScript and a
number that is wrong: they are hand-written constants, ffmpeg's said 85 against
an actual 106 for months (`docs/history/2026-08-13.md`), and a figure labelled
*zabrané místo* that nobody can reconcile against their own disk is worse than no
figure, because they will go looking for the difference. So `installed_megabytes`
in Rust walks the tools and models folders and sums file lengths — metadata only,
no file opened, a few dozen directory reads for several gigabytes — and it is its
own command rather than part of `catalog`, which the listing asks for on every
visit and after every download.

Two consequences worth stating plainly. The number **includes anything else in
those two folders**, which is the honest reading of *what is this costing me* and
is said in the context entry. And it is not attributed per component: only
installations that recorded a file list could be attributed at all, so a total
would have changed meaning depending on when each component was fetched, and a
total that means different things about different rows is not a total.

#### One pair of words for one set

> Programy i modely se do složky stahují samy. Cestu měňte jen v případě potřeby.
> Modely a nástroje se do složky stahují samy. Cestu měňte jen v případě potřeby.

Two words, and a vocabulary fix rather than a rewording: by the end of today that
set is named by a tab (`Modely`), a screen heading (`Modely a nástroje`), a
button (`Spravovat modely a nástroje`) and this sentence, which was calling it
*Programy i modely*. The sweep the change asks for found two more and settled
both:

- `settings.files.locationsTitle` was `Umístění nástrojů a modelů` — the same
  pair in the other order, on the card **directly under** the one that now says
  `Modely a nástroje`. It is `Umístění modelů a nástrojů`. The genitive was the
  reason to check rather than assume; it reads the same either way, so
  consistency decides;
- `settings.modules.description` said `Nástroje a jazykové modely pro lokální
  přepis` — the other order again, plus *jazykové*, which stopped separating
  anything the day the listing gained language models of two kinds. It is
  `Modely a nástroje pro lokální přepis.`

**Left alone, deliberately.** `settings.about.creditsDescription` says *licence
jazykových modelů a nástrojů* on `Informace`: that is about third-party software
and its licences, not about what this machine has downloaded, and the words are
doing a different job. The group headings in the listing — `Programy pro přepis`,
`Jazykové modely pro přepis` — stay too: they name the **halves**, which is what
a group heading is for, and the fixed pair is for the whole.

**And one inconsistency found on the way that is not this change's to fix.** The
same components are called `položky` in `settings.modules.missingRequired` and
`součásti` in the new rows and in the owner's own sentence about the lock. Both
words are already in the interface and both read correctly; picking one is a
sweep of its own, and doing it inside this commit would have meant rewording a
plural set nobody has complained about.

#### `okamžitě`

> Změna se projeví hned. Přepisy zůstanou v původním jazyce.
> Změna se projeví okamžitě. Přepisy zůstanou v původním jazyce.

The note under `Jazyk aplikace`. One word, and the English moved with it — it
said *right away*, which is the looser of the two, and says *immediately* now.

The other `hned` in the interface is `library.dropZone.automatic.hint`, *Spustit
přepis hned po přidání nahrávky*, and it is left: that is `hned po` as a
temporal preposition — *right after* — and not the same sense at all. The second
sentence of the note is untouched, and it is the half that earns its place: it
answers the question somebody actually has when changing the interface language,
which is whether it will do something to their transcripts.

- Files: `src-tauri/src/download.rs`, `src-tauri/src/commands/downloads.rs`,
  `src-tauri/src/transcription/jobs.rs`, `src-tauri/src/ai_edit.rs`,
  `src-tauri/src/online_import.rs`, `src-tauri/src/tools.rs`,
  `src-tauri/src/main.rs`, `src/SetupWizard.tsx`, `src/Settings.tsx`,
  `src/api.ts`, `src/types.ts`, `src/icons.tsx`, `src/css/04-settings.css`,
  `src/css/05-later.css`, `src/locales/cs/` and `src/locales/en/` (`wizard`,
  `errors`, `settings`, `catalog`), `src/locales/sources.json`.
- Stylesheet: `.component-lock` is new — 26 px, `--text-quiet`, no hover, the
  bin's own geometry so the two columns line up. `.component-mark.running` loses
  its conic gradient and its `::after`, and `.component-mark > svg` stacks the
  ring and the glyph in one grid cell the way `.mini-play` does.
  `.manual-group` replaces `.manual > div`'s 16 px with 24 px above every group
  but the first, `.manual` takes the outer 20, and `.manual > .download-view` is
  deleted with the rule it was beating. `.mini-track` and `.mini-progress` gain
  a comment saying they are now worn by two controls. No new amount that is not
  named here.
- Icons: three added to `LINE_ICONS` — `lock`, with the reason it carries no
  keyhole and the note that it never says *why*, there being two whys and the
  tooltip having them; `download` and `disk` for the card's two rows, each with
  the reason it is that drawing and, for `disk`, the reason it is not `folder`.
- Dictionary: 7 keys added with contexts (`wizard.manual.removeChosenText`,
  `.removeLastText`, `.lockedBusy`, `.lockedUnlisted`,
  `settings.modules.installedCount`, `.installedOf`, `.diskUsed`), one Rust code
  added (`errors.download.component_busy`) and one retired with its fingerprint
  (`errors.download.component_in_use` — the old lock's refusal, about a question
  nothing asks any more). Reworded and re-approved: `errors.download.cannot_remove`,
  `settings.modules.manage`, `.description`, `settings.files.locationsTitle`,
  `.locationsDescription`, `settings.language.description`,
  `catalog.yt-dlp.name`, `catalog.deno.name`. Fifteen English strings read
  against their Czech before approval. `settings.modules.manage` and both
  catalogue names gained context entries saying why they are longer than they
  were, so the next tidy-up does not shorten them back.
- Rust: `origin_verified` and `can_remove` are gone from the module. The first
  had one caller and `catalog()` now reads `installed.json` once for the whole
  list instead of once per row; the second moved into the tests, which were its
  only remaining caller. Comments stand where both did. `installed_megabytes` is
  new, registered in `main.rs` beside `catalog`.
- Verified: `npx tsc --noEmit`; `npm run build` (`i18n:check`, `docs:check`,
  `tsc`, `vite build`); `npm test` 132; `cargo fmt --all --check`;
  `cargo clippy --all-targets -- -D warnings`; `cargo test` **202** — nine new,
  and they are the three things this change is: the file list (recorded, read
  back, and the shared folder removed by it with its neighbours untouched), a
  record without one (which still loads, still refuses, and says `unlisted`),
  and the busy guard (idle locks nothing, including the chosen model; a
  transcription locks its whole run; each other kind of work locks only its own
  tools), plus the settings left coherent both ways, one more escape case — a
  single bad entry in a recorded list refusing the whole removal — and the
  card's size row, which is asserted to be what is on the disk rather than what
  the catalogue expected.
- Not verified by machine: the screens. What to press is the bin on a spare
  model and on the one in settings — the second must name its successor in the
  dialog, and the model card on `Přepis` must show that successor afterwards;
  the same on the last model, which must leave the setting empty and the
  application asking for one; the lock on `Podpora online zvuku a videa` and on
  `Zpracování zvuku` on a machine that has not re-downloaded them, whose tooltip
  must say so; and the lock on the model while a transcription is running, which
  must become a bin again once the run ends and the list is reopened. Then the
  ring on a download at a few per cent and at the end, the group headings in
  both listing views, and the module card's two rows — the fraction against the
  rows on the listing, and the size against what Windows says the two folders
  hold.
- **Left undone.** A transcription that started before somebody changed
  `settings.model` locks the model named *now*, not the one whisper actually
  opened. Nothing records which file a running whisper has, and the two differ
  only if the model is changed mid-run — named here rather than pretended away.
  And the lock is drawn from a catalogue read when the screen opened, so a run
  starting a second later leaves a bin on screen; pressing it is refused by
  `component_is_busy` in the command, which is why that guard is there as well
  as on the row.

### 2026-08-14 — The grey bands were the scrim, not two grounds — and the listing takes the settings column

> A tady ten divný šedý pruh při otevřeném dialogu je crap.

A screenshot of the delete confirmation over the component listing, with a grey
band down each side of the window, plainly different from the dimmed screen
between them.

#### One line, and it is not a colour

**`.dialog-overlay` was being centred as page content.** `ConfirmationDialog`
renders at the end of the wizard's `<main>`, and `.wizard.wizard > *` gave every
child of that element `max-width: 620px; margin-inline: auto`. A `max-width` on
a `position: fixed; inset: 0` box does not bounce off it: the box is laid out
against the window, then clamped to 620 px, and with both inline margins `auto`
it is centred. **So the veil covered a 620 px strip down the middle of the
window and left everything either side of it undimmed.** The bands are not a
band at all — they are the screen, at full brightness, beside a scrim that was
never covering it.

Two things this was **not**, both checked before anything was changed:

- **not two grounds under one scrim.** `.wizard` paints no background, so what
  is behind it is `body`'s `--ground` from edge to edge; the panels inside the
  column are content, and they are dimmed with everything else once the veil
  actually covers the window. Nothing needed matching, and matching it would
  have been the wrong repair in the other palette — `--scrim` is dark in both;
- **not the scroll container against the page.** `.wizard` is the scroll
  container and it is also what the overlay was a child of, which is exactly how
  the overlay came to be caught by a rule about columns.

The fix is `:not(.dialog-overlay)` on that one rule. The overlay then keeps its
own `inset: 0` and covers the window, which is all it ever wanted to do.

**The other three screens that centre their children are left alone, and that is
a decision.** `.library`, `.transcript` and `.settings` all render their dialogs
somewhere else — inside a row, beside the transcript, inside the card that
raises them — so none of them has the defect. Adding the exclusion anyway would
not be free: `:not()` carries its argument's specificity, and on the archive it
would lift that rule to what `.library.library > .archive-sticky` and
`.archive-scroll-content` already have, turning two clean specificity wins into
a matter of file order — which is the hazard this stylesheet's own header warns
about. Each of the three carries a comment instead, saying what must never
become one of its children and pointing at the rule where the whole thing is
written out. A comment cannot go stale the way a silent cascade can.

#### And the column is the settings column

> Šířka sloupce ve správě modelů a nástrojů by měla být stejná jako šířka
> sloupce v nastavení.

**720 px**, which is what `.settings.settings > *` has always been. The listing
is reached from Settings, it is a table, and a column that moved 100 px under
the reader between the two screens was a change nobody could see a reason for.

**Only that screen.** The guided run keeps 620: one question with two cards is
prose and choices, and prose does not want the width a table does. The two share
one selector, so the untangling is a variable rather than a second rule —
`--wizard-column` on `.wizard`, overridden on `.wizard.wizard-listing`, read by
the one `max-width` declaration. **Nothing new has to win a cascade fight to
change the width**, which is the trap a second `max-width` rule at the same
specificity would have been. The `var()` carries its fallback, because an
undefined one drops the declaration in silence and this one decides the shape of
every screen in the wizard.

**Same selector, two faults, one cause underneath.** They are not the same
defect: widening the column would have made the strip 720 px and left the bands
exactly where they were. What they share is the rule that centres *everything* a
screen contains, including the things that are not the column.

#### The class had to be known before the first paint

`manual` — the flag that says this is the listing rather than the guided run —
was set inside `load`, once the catalogue had arrived. It was always computed
from two props that are known at mount, and being late cost nothing until the
column's width came to depend on it: the screen would have opened at 620 and
jumped to 720 a moment later.

So it is read at the first render, and the step goes with it: opening the
listing no longer draws the one question for an instant on the way there. That
flash predates this change and is gone with it — a layout that settles after it
is drawn is a layout the reader watches move.

#### The doubled classes, since the rules were being touched

`.settings.settings` and `.wizard.wizard` both double their class to win a
cascade fight and neither said what it was beating. **The history does not say
either**: the doubling predates the English rename in `88103ec` and no entry
explains it. Both now carry a comment saying that much, and saying why they are
left doubled rather than reduced blind — a rule doubled to beat any single-class
selector landing on a direct child is a fight that can only be lost by making it
weaker, and the width change deliberately did not need to join it.

- Files: `src/SetupWizard.tsx`, `src/css/04-settings.css`,
  `src/css/02-library.css`, `src/css/03-detail.css`.
- Stylesheet: `:not(.dialog-overlay)` on `.wizard.wizard > *`;
  `--wizard-column` at 620 with `.wizard.wizard-listing` at 720; comments on the
  three centring rules that are left as they are, and on both doubled selectors.
  No colour touched, no new amount — 720 is the settings column's own number.
- Verified: `npx tsc --noEmit`; `npm run build` (`i18n:check`, `docs:check`,
  `tsc`, `vite build`); `npm test` 132; `cargo fmt --all --check`;
  `cargo clippy --all-targets -- -D warnings`; `cargo test` 202. No Rust
  changed and no dictionary key touched.
- Not verified by machine: the screens, and this one is a layout change so that
  matters more than usual. Open `Spravovat modely a nástroje` and press a bin:
  the veil must reach all four edges of the window, **in both palettes** —
  `--scrim` is dark in both, so a mismatch that hid in one would show in the
  other. Then the column itself, against Settings behind it: the two must line
  up, and the wizard's first question must still be the narrower screen.

### 2026-08-14 — The first run becomes a dialog over the archive, and the question stops pretending it has never met this computer

> Ten průvodce vypadá děsně. Musíme ho udělat znova a asi by to měl být splash
> screen po spuštění. Nebo takový větší dialog. A dvě karty. Spíš vertikálně než
> horizontálně. A tam cenné info a přínos. Třeba: Máte slabší počítač, grafickou
> kartu, pouze 16 GB RAM…

The last line is the whole of it. **The application knows this machine** — which
drivers are installed, whether there is a graphics card, how much memory — **and
was asking its one question as though it knew nothing.** Everything below follows
from repairing that, and the shell changed because a screen that tells you about
your own computer is a conversation and not a gate.

#### A dialog, of the two he offered

**`.dialog`, 520 px, over the archive.** A splash screen was the other option and
it was not taken, for two reasons worth writing down rather than deciding again:

- a splash reads as an advertisement and is clicked through unread. This screen
  asks the one question in the application that is expensive to change your mind
  about — a different transcription model means transcribing everything again —
  and it has to be read;
- **the application drawn behind it says *you have arrived*.** The wizard was a
  full screen that replaced the archive, so a fresh installation met Volocal by
  being shown something that is not Volocal. With the archive behind the dialog
  the reader can see what they have installed while they answer one question
  about it.

520 px is what `CLAUDE.md` fixes for a column of choice cards. No exception was
needed and none is taken.

**And a correction to the brief, since it is the reason that width is right.**
The brief asked for the cards *stacked vertically rather than side by side* on
the grounds that side by side crushes the text into fragments. **They were
already a column** — `.choices` is `flex-direction: column` and has been since it
was written. What was horizontal is the inside of each card: an icon, one line of
text, and a size pinned at the far edge. So the 100 px the guided run gives up is
width those cards never used; it went to the gap between the sentence and the
size. What actually changes is what a card holds, which is four things where it
held one.

**The by-hand component listing keeps its screen**, at the settings column's
720 px. That is a decision, not an omission: it is a table of everything this
machine can hold, reached from Settings or from a document asking for its model,
and every decision made about it today — the width, the row, the bin, the lock —
was made about a page.

**So `SetupWizard` returns one of two shells and the prop that decides is
`required`.** In `App.tsx` the guided run is no longer a `screen` at all: it is
`setupOpen`, rendered with the other dialogs, over whatever is behind it.
`screen === "wizard"` is now only ever the listing, which is why `required` can
be written as a literal at both call sites and `wizardRequired` is deleted — it
said the same thing a second time and the two could disagree.

**A way out is kept, including on a required first run.** The full screen had one
in the application bar, on the reasoning already written into `App.tsx`: an
errand nobody can leave is worse than one they can, as long as the way back in is
obvious — and it is, because the archive says what is missing and offers to
finish. So `Zavřít` sits in the footer and Escape does the same. **Escape is
withheld while a download is running**, which `useDialog` supports by taking no
`onClose`: the setting this wizard exists to write is written by the
`download:complete` listener, which lives in this component, so a key pressed at
the wrong moment would unmount the one thing waiting to record the answer.
`Přerušit` is the only button on screen while bytes are moving, and it is the
honest way to stop.

**And the scrim does not dismiss it.** Every other dialog in the application
closes on a click beside it; this one owns a download and a setting, and a stray
click must not be how that ends.

#### What the machine actually is, in one sentence

The question now opens with what was found here — a graphics card or not, and how
much memory:

> V tomto počítači je grafická karta a 32 GB paměti, takže přepis poběží na
> kartě.

**This is not the status panel coming back, and the distinction is the same one
written up this morning.** What was deleted was a two-panel grid — `KONFIGURACE /
Grafická karta (Vulkan)` beside `PŘEPIS / na grafické kartě` — restating the line
above it in two frames. That entry kept the sentence and said why: it explains
where the numbers below it come from. The sentence is still a sentence. It now
says more, because it turned out to be the thing the screen was missing rather
than the thing it could spare. **Do not put a panel back.**

Four of them, and not a phrase assembled from parts. The card and the memory can
each be absent, so there is a sentence for each pair, and a machine that will not
say how much memory it has gets the shorter one rather than a guess. A fifth,
`Zjišťuji, co je v tomto počítači.`, covers the fraction of a second before
`check` arrives — without it the screen would claim, for an instant on every
opening, to have found no graphics card.

**Which build is never named.** `grafická karta`, not `Grafická karta (Vulkan)`:
nothing in this application has named a backend to the reader since
`domain.compute.*` was deleted this morning, and this screen is not the place to
start again.

#### Memory had to be built, because nothing read it

`tools.rs` has read drivers since the wizard was written and has never read
memory. `total_memory_gb()` is new — `GlobalMemoryStatusEx` through the `windows`
crate already in the tree, one more feature flag and no new dependency — and
`ToolCheck` carries `memory_gb: Option<u32>`.

Three things about it are deliberate:

- **`None` is a real answer and the screen draws it.** On a platform that cannot
  be asked, or a call that fails, the sentence is shorter. A number nobody read
  is worse than no number, because a reader can check this one against their own
  machine in ten seconds;
- **rounded, not truncated.** Windows reports what the operating system may use,
  which on a 16 GB machine is about 15.8 GiB — the firmware and the graphics chip
  hold some back. `15 GB` on a box that says 16 is exactly the kind of figure
  somebody goes looking for the missing part of;
- **`GetPhysicallyInstalledSystemMemory` was the other candidate and was turned
  down.** It reads the modules out of SMBIOS and would give the number on the box
  exactly, and it fails outright on firmware whose tables it dislikes. A fact the
  screen may not get at all is worse than one it has to round.

Below half a gigabyte nothing is reported: no computer that could run this
application has that little, so such an answer is a failed reading rather than a
small machine.

**And nothing decides anything by it.** `recommendedQuality` is still
`usesGpu ? "best" : "fastest"`. Deciding by memory would need a threshold — how
much the larger model wants before it is a poor idea — and nothing in this
repository has ever measured one. A recommendation is the one place a guess costs
the reader gigabytes. Memory is on the screen because the reader recognises their
own machine in it, which is what makes the rest of the sentence credible.

#### The cards: name, model, what you get, what it costs here

Stacked, full width, and each carrying four things where it carried one — in the
order the brief asked for:

    Přesný
    Whisper 3 Large. Kvalitnější přepis. Precizní časové značky slov.
    Hodinová nahrávka asi 4 minuty, ke stažení 3,0 GB.
    Doporučeno pro tento počítač — na grafické kartě zvládne i větší model
    hodinu nahrávky bez dlouhého čekání.

**The sentence is the catalogue's own**, `domain.modelDescription.*`, the one the
model card in Settings draws. `wizard.quality.fastestSummary` and `bestSummary`
are retired with the change. Two things were wrong with a pair of sentences this
screen kept for itself: the wizard and Settings could describe one model two ways
— the defect written up earlier today, when the by-hand listing was found still
printing a claim deleted from the card two commits before — and the owner's own
wording reached every screen except the first one anybody sees.

**So the model's name is on the card after all, and that supersedes an entry from
this morning.** *The wizard's quality cards do not get the name, deliberately*
was right about the string it was refusing: `Whisper large-v3-turbo` is noise on
a first run. What ships here is `Whisper 3 Turbo` — the reader's form of the
name, in front of a sentence he wrote — and the brief asks the card to carry it.
The earlier entry stands and still explains what was true while it stood.

**The size left the right-hand edge and became part of a sentence.** A figure in
a column answers *how many megabytes*; what somebody choosing here is weighing is
minutes against megabytes, and those two belong in one line. A model already on
the disk costs no download, so the sentence stops after the time and the
`staženo` badge above it says why — the slot still has two states and still shows
one, which is the rule `staženo` was made a badge under.

**The recommendation is a reason, not a badge.** `doporučeno` in a pill asserted;
this argues, and it argues from the one fact the sentence above the cards has
just shown the reader was read off their machine.
`wizard.download.recommendedBadge` is retired. Its replacement carries no ratio —
*podstatně déle* rather than a multiple — for the reason in the next section.

#### The estimates: what was done about them, and what was refused

`estimatedMinutes` has carried 1, 8, 4 and 35 since the first commit. The comment
claimed *measured*; no entry in `docs/history/` ever took, checked or
contradicted them, and three entries today have named them as the oldest
unmeasured numbers in the interface.

**They are not measured here, and that was checked rather than assumed.**
`benchmark_compute` needs an installed model and a recording. A first run has
neither. Running it on this screen is not a thing that can be done at all.

**They were also not turned into a function of what was detected**, which the
brief offered as the other route and which is now technically possible, memory
being readable. It was refused deliberately: a formula over cores and gigabytes
would produce a different number for every machine and have evidence for none —
four honest guesses replaced by a fabricated model wearing the authority of an
arithmetic. That is worse than the problem, and on a screen that has just shown
it can read the hardware it would be worse still.

**What was done is the third thing: the copy admits it.**
`wizard.quality.changeableNote` now opens `Časy jsou hrubý odhad, ne měření.`
before saying the model can be changed later. Its context entry says the same in
as many words, so that nobody tidies it back into a claim. The reason line avoids
a ratio for the same reason: the difference between 8 and 35 minutes is as
unmeasured as either number.

**Settling them is still one command away and belongs on a machine that has been
through this screen**, not on the screen itself. `benchmark_compute` stays
registered and callerless, as three entries today have said it should.

#### A defect found on the way: the by-hand list followed the guided run to its ending

`download:complete` set `setStep(STEP_DONE)` unconditionally, and the by-hand
list shares that listener. So finishing **one row's** download in that list threw
the reader onto *Hotovo — vše je připraveno*, a conclusion to a walk they were
not on, with the list they were reading gone from under them. It was invisible
while both modes drew the same `.step` markup and is obvious once they do not.

The listener no longer moves the step when `manual`. `load()` on the line above
has already refreshed that row's own tick, which is the whole of what that mode
has to report.

#### Retired furniture

- **`.steps` and `wizard.nav.stepCounter`.** A three-segment bar and `KROK 1 ZE 3`
  drawn over an errand that is a question and a download, inside a dialog that is
  itself the thing being counted — and the download has a bar of its own that
  reports bytes. Both gone, with the `STEPS` array.
- **`.step-number`, `.step-intro`, `.step-outro` and its two children.** The
  dialog's rhythm is `.dialog h2`, `.dialog p` and `.dialog h2 + p` — 19 px, 4 px,
  18 px — and it owns every gap. Centred prose inside a dialog reads as a
  certificate. `.step` and `.step h1` stay: the listing is still a page and still
  wants a 28 px heading.
- **The `nothingNeeded` list.** `Všechno potřebné už máte.` is the dialog's own
  sentence on the download step now, where a `Stáhne se 0 položek` would have been
  arithmetic about nothing. The string is unchanged and so is the button beside it.

- Files: `src-tauri/Cargo.toml`, `src-tauri/src/tools.rs`, `src/SetupWizard.tsx`,
  `src/App.tsx`, `src/types.ts`, `src/css/04-settings.css`, `src/css/05-later.css`,
  `src/locales/cs/wizard.ts`, `src/locales/en/wizard.ts`,
  `src/locales/sources.json`.
- Stylesheet: `.setup-dialog` joins `.ai-edit-dialog, .add-recording-dialog` at
  520 px — the documented width, no new number. `.choice-cost` is
  `.choice-size`'s own type (13/500/`--text-quiet`/`tabular-nums`) without its
  `nowrap`, which is right for a figure in a column and wrong for a sentence that
  has to wrap; `.choice-reason` keeps the badge's accent at 600 rather than 700,
  because it is a sentence now. `.choice-cost, .choice-reason` take 3 px on top of
  `.choice-body`'s own 3, so the card reads as a name over its detail and then two
  statements of a different kind. `.setup-dialog > .warning` takes 18 px below
  itself and none above, which is the dialog's rhythm rather than the banner's 20.
  Six rules are deleted with the screen furniture above, with a comment where they
  stood, and the comment on `.choice.with-icon > .choice-size` records that the
  wizard is no longer one of its users.
- Dictionary: 9 keys added with context entries (the five machine sentences, the
  two cost lines, the two reasons), 6 retired with their `sources.json`
  fingerprints (`wizard.quality.introGpu`, `introCpu`, `fastestSummary`,
  `bestSummary`, `wizard.download.recommendedBadge`, `wizard.nav.stepCounter`),
  and `wizard.quality.changeableNote` reworded. Ten English strings were read
  against their Czech before approval.
- Rust: `total_memory_gb` and `memory_gigabytes` in `tools.rs`, `memory_gb` on
  `ToolCheck`, and one feature flag on the `windows` crate that was already there.
- Verified: `npx tsc --noEmit`; `npm run build` (`i18n:check`, `docs:check`,
  `tsc`, `vite build`); `npm test` 132; `cargo fmt --all --check`;
  `cargo clippy --all-targets -- -D warnings`; `cargo test` **205 passed, 2
  ignored** — three new, all about the reading: the rounding that turns 15.8 GiB
  into the 16 on the box, the refusal to report anything that cannot be true, and
  the call itself on the machine running the tests, which is the half no
  arithmetic can check. Both shells were also rendered in jsdom while this was
  being built — the dialog with its machine sentence, two cards and one reason,
  and the listing with its heading and rows — and that throwaway test was deleted
  rather than kept, since `npm test` is 132 by the brief.
- Not verified by machine: the screens, and this round is mostly screens. What to
  press is a first run on a machine with a graphics card and on one without — the
  sentence must name what is actually there, the recommended card must be the one
  the reason argues for, and the archive must be visible and dimmed behind the
  dialog at all four edges, in both palettes. Then the same dialog on a machine
  that already has one of the two models, whose card must wear `staženo` and drop
  the download size from its cost line. Then `Pokračovat`, the download, and
  `Přerušit` — Escape must do nothing while it runs and close the dialog when it
  is not — and the conclusion, with something failed and with nothing failed.
  Then `Spravovat modely a nástroje`, which must still be a 720 px page, and one
  row's download inside it, which must now leave the reader on the list.
- **Left undone.** The four estimates are still guesses; what changed is that the
  screen says so. `benchmark_compute` is still registered and callerless, and the
  measurement it could take belongs on a machine with a model and a recording on
  it. And memory is read but used only in one sentence: if a threshold is ever
  measured — how much the larger model wants before it is a poor idea — the
  recommendation is where it goes, and `recommendedQuality` is the one function to
  change.

### 2026-08-14 — The test that edited the machine it was running on

`download::tests::removing_a_component_takes_its_file_and_its_record` failed
under a plain `cargo test` and passed under `--test-threads=1`:

```
panicked at src\download.rs:1725:39: no entry found for key
```

**What was shared was `installed.json`, and not a scratch copy of it — the real
one.** Every test in that file builds a directory of its own and points a
`Settings` at it, and that is enough for `bin` and `models`, because
`component_path` resolves exactly those two names through the settings.
`installed.json` is not one of them. It falls to the `other` branch and lands at
`tools_root()`, which reads `%LOCALAPPDATA%` — so four tests that had each been
given a folder of their own were all writing their installation records into
`%LOCALAPPDATA%\cz.znackarna.volocal\installed.json`, the record of the
developer's own installation, on every run of the suite.

The intermittence follows from how that file is written.
`record_installation` reads the whole map, inserts one entry and writes the map
back. Two tests doing that side by side both read a map without the other's
entry, and the second write drops it. Whichever test then asserts on its own
entry finds it gone — `no entry found for key`, at whichever of the four the
scheduler happened to interleave. `forget_installation` and `remove_component`
rewrite the same file the same way, so a run could also delete an entry a
neighbour had just written.

**Each test gets its own root**, rather than the tests being serialised or the
assertion made forgiving. `tools::use_tools_root_for_this_test` puts a path in a
thread-local that `tools_root()` answers with, and `scratch()` — the one place a
test's own directory is established — calls it. Everything derived from the root
follows: `installed.json` for the four record tests, and `catalog()`, which
reads the same file for every row.

**A thread-local rather than an environment variable**, and that is the point
worth keeping. `LOCALAPPDATA` is process-wide and `cargo test` runs these beside
each other in one process — which is exactly why `rename_tools_root` already
takes the folder to work in as an argument, with a comment saying so.
`cargo test` gives each test a thread, and every call these tests make happens on
that thread, so a root set there belongs to one test and no other can see it.
The override is `#[cfg(test)]`; `tools_root()` in a shipped build is the function
it was.

- Files: `src-tauri/src/tools.rs`, `src-tauri/src/download.rs`.
- Verified: `cargo test` six times over under the default parallel run, 205
  passed and 2 ignored each time; `cargo fmt --all --check`; `cargo clippy
  --all-targets -- -D warnings`. And the check the failure itself asks for: the
  modification time of `%LOCALAPPDATA%\cz.znackarna.volocal\installed.json` is
  unchanged across all six runs, where before this it was rewritten by every one.
- **Worth knowing**: the record on any machine that has run this suite may have
  had entries dropped by it. Nothing is lost that cannot be rebuilt — an entry's
  absence reads as *installed before records existed*, which costs the row its
  bin until the component is fetched again, and `catalog()` reads the disk for
  what is present.
