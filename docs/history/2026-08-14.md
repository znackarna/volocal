# 2026-08-14

### 2026-08-14 — Three tabs back up to five, because three did not help

Yesterday's entry ends with Settings at three tabs — `Přepis`, `Aplikace`,
`O aplikaci`. Jakub opened that arrangement and turned it down in one sentence:

> Ty tři karty nepomáhají. Karta vzhled by měla být zvlášť. A O aplikaci bych
> dal Informace a Aktualizaci bych dal zvlášť.

That is the reason for this change, and it belongs in the record: nothing was
measured, nothing broke — the person the screen is for looked at it and said the
grouping does not answer the question he arrives with. So the grouping changed
and only the grouping. **Every control the simplification removed stays
removed**, because those were dead (`Hledání změn mluvčího`, whose setting no
Rust code reads), harmful (`Počet mluvčích`, which silently overrode `Nevím`),
or derivable from a neighbour (`Řádkování`, `Sledovat složku`). None of that was
about tabs.

**The tabs are `Přepis`, `Vzhled`, `Složky a zálohy`, `Aktualizace`,
`Informace`.**

- `Přepis` is untouched: the model, the recording's language, the speakers, the
  language editing, the dictionary, the band saying what is downloaded, and
  `Pokročilé`.
- `Vzhled` is the appearance card taken back out of `Aplikace` — interface
  language, theme, transcript type and size, and the live sample.
- `Aktualizace` is the check, `Co je nového`, `Stáhnout a nainstalovat` and the
  automatic-check switch, lifted out of the About page. It is the one thing on
  this screen that is neither a setting nor a fact: it is an errand, one press
  of which closes the application and starts an installer. It sat under the
  licence list, which is the last place a reader looking for a new version
  looks.
- `Informace` is what `O aplikaci` was, minus the updates: what the application
  is, what it can do, and what it stands on. Nothing on it can be pressed.
- `Aplikace` is `Složky a zálohy`. **This is the one decision the owner did not
  make for us.** With appearance gone, what was left under that name is the
  recordings folder, the watched folder, the archive's backups with export and
  import, and — on an ordinary installation — the portable copy. Not one of them
  is what anybody would look for behind the word `Aplikace`, and a label that
  does not describe its contents is how the seven tabs got their names in the
  first place. The new one names both halves of what is there: the two folders,
  and the copies you go to once something has already gone wrong.

**Nothing moved between cards.** Every card is the same card with the same
contents; only which tab draws it changed. The one component change is that
`About` no longer takes the four update props — the update block is rendered by
the new `settings-card-updates` section instead.

**The tab names in the code changed with the visible ones**, so `application`
became `files` and `appearance`, `updates` and `about` stand beside them. The
last open tab is remembered in `localStorage` under `settings-tab`, and the
guard that was already there is what makes a rename safe: a stored name that is
not one of `SETTINGS_TABS` opens `Přepis` rather than an empty panel. Every
machine that opened Settings yesterday has `application` in there and will land
on `Přepis` once.

**Every card on every tab has an `order` rule**, including the two tabs that
hold a single card. Alone the number changes nothing; without it, the next card
put beside them takes `order: 0` and jumps above whatever asked for 1 — which is
precisely the defect written up yesterday about the recordings folder.

**Two keys were retired and two added.** `settings.tab.application` is gone from
`cs`, `en` and `sources.json`; `settings.tab.files` and `settings.tab.updates`
are new; `settings.tab.about` keeps its name and says `Informace` / `Information`
now. The appearance tab is labelled from `settings.appearance.title` — the card
that fills it already carries that word, and a second key holding the same word
is a second thing to keep in step. The updates card does the same in the other
direction, taking its heading from `settings.tab.updates`, exactly as the
transcription card takes its own from `settings.tab.transcription`.

- Files: `src/Settings.tsx`, `src/css/04-settings.css`,
  `src/locales/cs/settings.ts`, `src/locales/en/settings.ts`,
  `src/locales/sources.json`.
- One spacing rule moved with the block: `.about-updates` carried 22 px as the
  last band of the About card, where it followed a panel. As the whole of its
  own card it follows a heading, so it takes the 24 px `h2 + .field` and
  `h2 + .about-abilities` already take, written as
  `.settings section > h2 + .about-updates`.
- Verified: `npx tsc --noEmit`, `npm run build` (which runs `i18n:check`,
  `docs:check`, `tsc` and `vite build`), `npm test` 132. `i18n:check` required
  the three reworded or new English strings to be approved against their Czech —
  *Information*, *Folders and backups*, *Updates*, each read against its source —
  and the fingerprint left behind by `settings.tab.application` was deleted from
  `sources.json`.
- Not verified by machine: the screens. What to press is all five tabs at both
  window widths, the update check on `Aktualizace` (the button, the release
  notes dialog, the automatic switch), and Settings opened on a machine whose
  `settings-tab` still says `application`.
- **Left undone**: two tabs hold exactly one card each, so `Vzhled` and
  `Aktualizace` each carry a heading repeating the tab above it. Dropping the
  heading would make them the only cards in Settings without one, and inventing
  a second sentence for each would be words for the sake of the layout. It is
  visible and it is small; if it grates, the answer is a card that earns the
  tab, not a special case in the stylesheet.
- Also left, from yesterday and still true: 13 unused `wizard.*` keys from the
  wizard rebuild, which belong to that change rather than this one.

### 2026-08-14 — A sixth tab, `Nástroje`, for what is installed rather than what is transcribed

The owner read the five-tab arrangement above and asked for one more:

> Modely bych dal jako Nástroje, zvlášt záložku

So there are six: **`Přepis` · `Vzhled` · `Nástroje` · `Složky a zálohy` ·
`Aktualizace` · `Informace`.** `Nástroje` sits third, between `Vzhled` and
`Složky a zálohy`, which is where the reading changes subject: the first two
tabs are about what the application produces and how it looks, and everything
from here on is about this machine — what is on its disk, where, which version,
and what the application is.

**Three things moved and nothing was rebuilt.**

- The status band — everything downloaded, or *N* missing, with the button that
  opens the by-hand component list. It was the `settings-card-modules` card at
  the foot of `Přepis`. Same card, same heading, same button.
- `Technické podrobnosti`, the disclosure with the found paths, `Zkopírovat
  údaje` and `Otevřít log`. It was the last band inside `Pokročilé`, which meant
  somebody whose transcription will not start had to open a block titled
  *advanced* to see what the application can see. It is the folded footer of the
  status band's card now — a `spaced` action row followed by one `card-footer`
  disclosure, which is the pair those two stylesheet rules were written for and
  exactly what the archive card does. No new CSS.
- `Složka nástrojů` and `Složka modelů`, the two read-only rows with `Vybrat…`.
  They were also inside `Pokročilé`, where a folder is not a decoding value and
  `Zpět na výchozí` two rows below them had to carry a note saying it does not
  touch them. They are a card of their own, `settings-card-locations`, under the
  band that says what has been downloaded into them.

**`Pokročilé` keeps the beam width, the five decoding thresholds and
`Akcelerace zpracování` with `Změřit rychlost`** — how a transcript is made,
which is the tab it is on. It is not only decoding, so it is not renamed; and
naming it `Dekódování` would put a word from whisper.cpp's manual on a screen
that uses one nowhere else. What did change is that the block now holds values
and only values: `Zpět na výchozí` covers everything inside it, and the
exception for the two folders is gone with the folders rather than being kept as
a rule with no visible reason.

**This is not the old `Modely` tab coming back**, and the record should say so
plainly because yesterday's entry argued against it. What was deleted there were
four read-only tiles — the model, the acceleration, the editor, the speakers —
each repeating a value chosen a card or two higher up and none of them
pressable. Those stay deleted. What the owner has asked for is the other half of
that argument: the part of the subject that is neither a repeat nor a decision
about a transcript — is it here, where is it, and what exactly did the
application find — now has one place instead of being spread across the foot of
one tab and the inside of a disclosure on it. Yesterday's entry described that
remainder exactly — *what that tab was for is one line saying whether anything
is missing and one button to the list that fixes it* — and put it at the bottom
of `Přepis`. The line is still one line. It has moved to the tab where the rest
of its subject lives, and the paths and the folders have joined it.

**The red dot moved with the band.** It marks the tab that shows what is
missing, so it is on `Nástroje` now, not `Přepis`.

**The remembered tab still survives a name it does not know.** `settings-tab` in
`localStorage` is read through `SETTINGS_TABS`, so a stored `application` — or
anything else this build has never heard of — opens `Přepis` rather than an
empty panel. Nothing was renamed this time; `tools` is simply added, and every
machine keeps whichever tab it was last on.

**Every card on the new tab has an `order` rule**: `settings-card-modules` is 1
and `settings-card-locations` is 2. `settings-card-modules` was 5 on `Přepis`
and its number went with it, so `settings-card-advanced` moved 6 → 5 and the
transcription list is 1–5 with no gap. This is the defect that keeps being
written up here: a card with no rule takes `order: 0` and jumps above everything
that asked for 1.

**Two keys added, none retired.** `settings.tab.tools` is `Nástroje` /
*Tools*, and `settings.files.locationsTitle` is `Umístění nástrojů a modelů` /
*Location of tools and models* — the new card needed a heading, and the sentence
above its two rows is the `settings.files.locations*` pair it already had, now
wearing `settings-section-description` instead of `small-text` because it has
become a card's opening sentence rather than a line inside a disclosure. Both
new keys have a context entry. Two existing contexts were corrected rather than
left lying: `settings.advanced.title` no longer claims the block holds the
folders, and `settings.advanced.reset` no longer explains an exception that does
not exist any more. Context is not fingerprinted, so neither correction asks for
a re-approval.

- Files: `src/Settings.tsx`, `src/css/04-settings.css`,
  `src/locales/cs/settings.ts`, `src/locales/en/settings.ts`,
  `src/locales/sources.json`.
- Verified: `npx tsc --noEmit`, `npm run build` (which runs `i18n:check`,
  `docs:check`, `tsc` and `vite build`), `npm test` 132. `i18n:check` reported
  three English strings with no source fingerprint: the two new ones, read
  against their Czech, and `settings.appearance.fontUi` — *Interface type* for
  `Písmo rozhraní` — left unrecorded by the previous commit on this branch when
  the control came back. All three approved.
- Not verified by machine: the screens. What to press is all six tabs at both
  window widths; the red dot on `Nástroje` on a machine with something missing;
  `Vybrat…` on both folders, which writes a settings value from a card that has
  never held one before; `Zkopírovat údaje` and `Otevřít log` in their new
  place; and `Pokročilé` with its badge and `Zpět na výchozí`, which is now the
  last thing in the block.
- **Left undone**: `Nástroje` carries a card headed `Modely`, which is the word
  the tab used to be called. It reads correctly — the card is about the models
  and the tab is about everything installed — but if the pair ever grates, the
  answer is to name the card for the state it reports rather than to rename the
  tab the owner asked for.
- Still open from the entry above: `Vzhled` and `Aktualizace` each hold one card
  whose heading repeats the tab, and 13 unused `wizard.*` keys belong to the
  wizard rebuild rather than to this change.

### 2026-08-14 — One question at the beginning, and everything follows from it

The owner read the settings screen back and answered it with a decision rather
than a correction:

> Prostě možná by člověk neměl mít na výběr. Prostě má dvě možnosti, rychle
> nebo přesně. Ta podpora pro CPU nebo GPU se stáhne podle toho co má.
> … Jazyková úprava Rychlá vs Přesná, žádné modely in the middle.
> … Prostě na začátku si vybere jestli chce rychle nebo přesné výstupy. A to
> stáhne všechno potřebné. Jazykovou úpravu bych stahoval až potom, až bude chtít.

The wizard already asks `Rychle, nebo přesně?`. What was missing is that the
answer was thrown away the moment the model was downloaded, so every later
question — which language-editing model, which acceleration — had to be asked
again, of a reader who had already said which end of the trade they are on.
**The answer is now written down and read everywhere it decides something.**

**`quality_choice` in the settings record**, `fast` or `accurate`, with
`#[serde(default)]`. Empty is the honest value for every settings record written
before today: nobody was asked. It is not guessed in Rust; `qualityChoice()` in
`types.ts` reads it against `model`, and a machine transcribing with the turbo
model is a machine that answered *rychle* when it was set up. The wizard writes
it from both paths — the two cards, and ticking a transcription model by hand in
the component list, which is the same answer given in a different place. It is
written whether or not the model landed: `model` names a file and must not name
a missing one, while this names a decision.

**The language editor follows it and is never asked about again.**
`EDITOR_TIER` maps `fast` to `editor-model-light` (3350 MB) and `accurate` to
`editor-model-best` (6980 MB). There is no card, no dropdown and no second
question anywhere: the three choice tiles left `Přepis`, the three-tier table
left the wizard, and `editor-model-balanced` is offered nowhere.

**And it is named by nothing it cannot prove.** This is the part of the change
worth reading twice, because the brief for it asked us to check before writing
and the check came back empty. `docs/history/` contains no comparison of the
three models' output — none, on any axis. Every inference ever recorded here was
run with the 12B model alone, because it was the only one installed on the
machine that did the work (`2026-08-02.md`, "Full inference was not run during
development because none of the new 3.35–6.98 GB models was already installed
locally"). The single speed number in the record — 1.2 tokens/s reasoning-on
against about 75 reasoning-off — is 12B against itself with a llama-server flag
changed, not one tier against another. And `2026-08-05.md`, the entry that gave
the three cards their counted sparkles, says it outright: they "trade nothing;
they do the same work with more of it".

So `Lépe opravuje zjevné chyby a drží souvislosti` and `Nejspolehlivější
výsledek` were exactly the middle transcription model's *asi jedna chyba na
odstavec*, deleted yesterday for being measured nowhere. They are not replaced
with a smaller claim. The catalogue now names these three by size —
`Jazyková úprava — menší / střední / větší model` — and describes them by what
follows from a file's size and nothing else: how much memory it needs. Where the
reader meets the editor there is no tier name at all, only one sentence with the
number of gigabytes in it, because at that point which model it is is not
something they can act on.

This supersedes the 5 August decision that gave the tiers one, two and three
sparkles. `EditorMark` is deleted with the cards it marked: a mark that says
"more of the same" is right only where there are three things to tell apart, and
there are none.

**It downloads once, on demand, in the background.** The first press of a
document action on a transcript asks one question — *Chcete z přepisu hotový
dokument?* — with the size in the sentence. Yes starts the download and closes
the dialog: `download` in `downloads.rs` starts a thread and returns, so the
reader carries on reading and the application's own progress bubble reports it
with a way to stop. Pressed again while that runs, the dialog says the model is
downloading instead of offering again; asked once means once. `editor_model` is
written at the press rather than when the file lands, deliberately —
`resolve_editor_model` falls back to whatever is actually on the disk, so a name
ahead of its file is not a dead end, and a listener that had to survive the
reader walking to the Archive would not have survived it. The same press exists
on `Přepis` for anyone who arrives there instead, and does the same thing;
neither walks to the wizard any more.

**`Akcelerace zpracování` and `Změřit rychlost` are gone from Settings.** The
machine picks its backend from its drivers, exactly as the wizard already picks
the programs. A reader choosing here could only choose something slower or
something this computer cannot run — and `choose_compute` then silently
substitutes, which is the defect underneath: the screen went on badging
`používá se` on a card that had never run anything. Verified in `tools.rs`
before this was written: with a stored `cuda` and no `nvcuda.dll`,
`usable_compute("cuda")` is false, the stored branch is skipped, and the next
line down that the machine can run wins.

**So `Nástroje` gained a card that says what actually ran**, `Kde přepis běží`,
built from `.about-panel` — the panel of facts the About page and the archive's
backups already share — rather than from the four choice cards it replaces. It
shows `check.compute`, which is `choose_compute`'s answer and the folder the
next `whisper-cli` comes out of, not the stored setting. Three sentences under
it, at most one at a time:

- a backend was named by hand on some earlier day and this machine can run it —
  it says so, and offers `Nechat na aplikaci`. The same shape as `Odebrat`
  beside the watched folder: when a control is removed, whatever it could set
  must keep a way back, or one press made a year ago is permanent;
- it was named by hand and cannot be run here — it says that, and offers the
  same button. This is the reported case: CUDA on a machine with an AMD card;
- nothing was named, there is a graphics card, and the transcription is not
  using it. With nothing pinned that can only mean the build is not downloaded,
  so it offers to download it. And on a machine with no graphics card at all,
  one line saying so, which is a fact and not a fault.

`compute` therefore left `ADVANCED_DEFAULTS`: a badge reading `upraveno` about a
value with no control on the screen is a badge nobody can act on. It stays in
the settings record, with a comment saying why.

**The shortcut strip has a way back.** Yesterday's entry called permanence "the
trade: one fewer control against one fewer way back", and it is not a trade
anybody needs to make. Dismissing the strip is still a × on the transcript
screen; bringing it back is a keyboard button in the player's row, drawn only
while the strip is hidden — so it costs nothing when it is up, and it is where
the thing disappeared rather than two screens away. `detail.tips.hideHint` still
said *vrátit se dají v nastavení*, which stopped being true when the switch was
deleted; it names the button now.

**Two more from the owner, about the by-hand component list:**

> V tom ručním výpisu stažení modelů a nástrojů: Rozpoznání mluvčích a detekce
> řeči bych dal do jednoho. Dal bych pryč ty vyvážené modely.

- **One heading for the two speech models.** `vad` was in `Jazykové modely` and
  `model-hlasy` had a heading to itself over one 2 MB row. Both decide something
  about the sound rather than about the words — where somebody is speaking, and
  whose voice it is — so both carry the group `speech` in `download.rs`, and the
  list draws them under `Řeč a mluvčí`. The grouping is changed at the
  catalogue, not in the list, so anything else that reads a group agrees.
- **`model-large-q5` and `editor-model-balanced` are not offered by hand
  either.** The rule is now the same in the wizard, in Settings and in the list.
  Both catalogue entries, their pins in `components.json` and their download
  paths are untouched — this is about what is offered, not what exists — and one
  that is already on the disk still appears, as the ticked, disabled row every
  installed component gets. Hiding a gigabyte that is there would make the list
  lie in the other direction, and the list is also how somebody reads what this
  machine holds.

**A contradiction found on the way, and fixed.** `catalog()` in `download.rs`
marked `model-large-q5` as `recommended` on any machine with a graphics card —
the middle model, which the wizard stopped offering yesterday. The wizard's own
card said `model-large`, so the badge and the choice disagreed; worse,
`Spravovat modely` ticks every recommended component that is not on the disk, so
opening the list to look would have queued a gigabyte of a model no screen
mentions. `model-large` is the recommendation now, which is what the wizard has
always downloaded.

**A machine set up before today keeps working, and that was checked rather than
assumed.** `settings.model` may hold `large-v3-q5_0`: `Přepis` lists
`check.found_models`, which is read off the disk, so the model is still shown,
still named — `domain.model.large-v3-q5_0` stays in the dictionary — and still
selected. `editor_model` may hold `gemma-4-e4b-q4`: `EDITOR_MODELS` in
`types.ts` keeps all three components, `resolve_editor_model` in `tools.rs`
already falls back to any installed quality, and the switch on `Jazyková úprava`
turns the feature on with whatever is actually there, preferring the implied
tier. Nobody is asked to download seven gigabytes to replace a model that works.

- Files: `src-tauri/src/db.rs`, `src-tauri/src/download.rs`,
  `src-tauri/src/tools.rs`, `src/types.ts`, `src/api.ts`, `src/SetupWizard.tsx`,
  `src/Settings.tsx`, `src/Detail.tsx`, `src/css/04-settings.css`,
  `src/css/05-later.css`, `src/locales/cs/` and `src/locales/en/`
  (`settings`, `wizard`, `detail`, `catalog`), `src/locales/sources.json`.
- Stylesheet: `settings-card-compute` takes `order: 2` on `Nástroje` and
  `settings-card-locations` moves 2 → 3, so every card on every tab still has a
  rule. `.benchmark` and `.field-prompt` are deleted with their only users —
  `.field-prompt` was a red panel saying an optional feature was not downloaded
  yet, which is not a fault and should not have been wearing the danger colour.
  No new rule was written: the compute card reuses `.about-panel`, and the
  editor's ask reuses `.settings-action-row`.
- Dictionary: 15 keys added and 44 retired, each with its `sources.json`
  fingerprint. The retired ones include the 13 unused `wizard.*` keys that two
  earlier entries today listed as left undone — the welcome screen's title and
  introduction, and the whole speaker step — which belong to this change now
  that the language-editing step's words have gone the same way. Every new Czech
  key has a context entry, and the reworded English was read against its Czech
  before approval: the six catalogue strings for the editor models, and
  `detail.tips.hideHint`.
- Verified: `npx tsc --noEmit`; `npm run build` (`i18n:check`, `docs:check`,
  `tsc`, `vite build`); `npm test` 132; `cargo fmt --all --check`;
  `cargo clippy --all-targets -- -D warnings`; `cargo test` 183.
- Not verified by machine: the screens. What to press is the wizard's two cards
  on a machine with and without a graphics card (the answer must land in
  `quality_choice`); the first document action on a transcript with no editor
  installed, twice, the second time while the download is running; the same ask
  on `Přepis`; the strip's × and the keyboard button that brings it back;
  `Nástroje` on a machine with a graphics card and no build for it, and on one
  whose `compute` still holds `cuda`; and the by-hand list, which should now
  show four headings and neither middle model.
- **Left undone, and deliberately.** `benchmark_compute` in Rust is still
  registered and now has no caller. It was kept rather than deleted because it
  is the only instrument this application has for timing a backend, and the
  estimates the wizard prints — 1, 8, 4 and 35 minutes — are still the oldest
  unmeasured numbers in the interface, which a comment in `SetupWizard.tsx` has
  been pointing at since yesterday. Deleting it would take `tools::clip` and the
  `benchmark.*` messages with it and make those numbers unsettleable. If the
  owner would rather have it gone, it goes in one commit; what should not happen
  is that it stays for a year with nobody remembering why.
- Also left: the `missingModule` branch in the wizard that adds the editor
  runtime beside an editor model is no longer reached, because neither screen
  walks to the wizard for the editor any more. It is correct if it ever is
  reached, and it is three lines; it is named here rather than quietly kept.
