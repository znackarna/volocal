# 2026-08-27

### 2026-08-27 — Correction: the four times in the wizard were measured all along

The owner, asked nothing, said it plainly: the numbers on the quality cards are
from his own computer — a Ryzen 7 9700X with a Radeon 9070. They are
measurements. Every claim made about them here since 25 August is wrong.

**How the mistake was made.** The original note beside `estimatedMinutes` named
a Radeon RX 9070. A session went looking for corroboration in `docs/history/`,
found none, and rewrote the comment to say the four numbers *have never been
measured* — reading a missing record as a missing measurement. Those are not the
same thing, and this repository, whose whole premise is that the record explains
the code, is exactly where that confusion is easiest to make.

**What it then cost.** The comment was believed. Codex's hardening brief of
25 August built item 7 on it and asked for a public claim to be withdrawn or
substantiated. This session verified item 7 by reading the comment, called it
correct, and reported to the owner that the wizard shows *unmeasured numbers as
fact* — twice, the second time as the one thing in the whole brief a user really
meets. The entry of 19 August, which had called this the only measured speed
claim the repository holds, was right, and was written off as overstatement.

An unchecked comment outlives everyone who knew better. Three readers repeated
it back as a finding and none of them asked the one person who had the answer.

The comment now records the machine, and that an AMD card means the graphics
figures are the Vulkan build. What is genuinely unknown is left standing and
named rather than filled in: which model files, and whether the run was a
release build — `benchmark_compute` would settle both, on a machine with a model
and a recording. The translator note under `wizard.quality.changeableNote` said
*časy na kartách nikdo neměřil*; it now says they are measured on one computer.

**The interface is not touched.** *Časy jsou přibližné* stays true of a real
measurement read on somebody else's machine, and it is the owner's copy.

Files: `src/SetupWizard.tsx`, `src/locales/cs/wizard.ts`.

### 2026-08-27 — The window is zoomed, not scaled, and the fix that caused it goes

He saw the toolbar pills on the live site with their arcs heavier than their
sides — the fault fixed on 19 August and written down as fixed. Then the sentence
that solved it: **only in Firefox, and only on the site; not in the preview.**

`transform: scale()` scales a picture. The subtree is rasterised and the result
resampled down, which a straight 1 px edge survives and a curve does not: the
resampling piles coverage into the arc. `will-change: transform` fixed that in
Chrome, which re-rasterises a promoted layer at the scale it is composited at.
It was measured there, at device scale 1, and the note on it is honest about
what it measured.

**Firefox does the opposite with the same rule.** A promoted layer is rasterised
once and its texture is scaled, so the hint that fixed Chrome is what caused
Firefox. The page has therefore been clean for whoever built it and heavy-armed
for everybody on Firefox since 19 August — the worst shape a defect can have.

`zoom` is not a picture. It scales used values, so the window is laid out at the
size it is shown at and every edge is drawn rather than resampled. There is no
texture, which is why `will-change` went with the transform: a layer would only
give the compositor something to scale again.

**Chosen by looking, not by reasoning.** A card with the same window three
times — transform with the hint, transform without it, and `zoom` — at the
page's own 0.92, opened in Firefox. Only `zoom` was clean; the first two were
identical, which also says the hint does nothing there but harm.

`fit()` in `preview.js` writes the factor now as `zoom`. Its measurement is
unchanged and that was checked rather than assumed: under `zoom`, `offsetWidth`
still reports the unzoomed 1180 while `getBoundingClientRect` reports the drawn
size, which is exactly the pair `fit()` and `layoutWaves()` already rely on.

The first two attempts at seeing this failed and both are worth remembering: a
test card with one pill in a 150 px stage showed nothing, because the fault
lives on long edges and large arcs; and Firefox headless would not take a
screenshot here, so the only eyes on it were his.

Files: `docs/site/index.html`, `docs/site/preview.js`.

### 2026-08-27 — The release script asks about the tree before it builds, not after

Codex's item 1, and the strongest finding in that brief. `release.ps1` ran its
checks and then built the installer, and asked whether the tree was committed
only in the second pass, twenty minutes and one human step later.

**Asking afterwards is not a guard.** The build takes what is on disk. Stash the
uncommitted part and the tree is clean and still at the same commit, so the
later check passes over an installer nobody can account for — and both things
that vouch for it, the version in the file name and the commit in
`built-from.txt`, agree with it, because both were true of a tree that no longer
exists.

It also cost twenty minutes yesterday in the ordinary direction. The build
rewrites `Cargo.lock` with the new version; that file was still uncommitted when
the second pass ran, and committing it moved `HEAD` away from the installer, so
the whole build had to run again. A check that sits that late punishes the
honest case and misses the dangerous one.

`Assert-CleanTree` is one function called by both passes — one implementation,
because two spellings of one rule is how the passes come to disagree about what
clean means. `git status --porcelain` already reports all three states that must
be refused and reports no ignored file, so what `.gitignore` covers is allowed
by construction; what it did not do was say which files, which is the difference
between a stop and a useful one.

Driven rather than reasoned about, in a throwaway repository, against the real
function lifted out of the script: a clean tree is allowed, a modified tracked
file, a staged file and an untracked source file are each stopped, and a tree
dirty only with an ignored file is allowed. The refusal was read back with all
three kinds present at once and names every path under its own heading.

`-SkipChecks` still gets past it and now says out loud what it is doing.

Files: `scripts/release.ps1`.

### 2026-08-27 — Nothing in the repository was ignoring Claude's own files

Codex's item 16, and it was right in a way the wording understates. Both files
were already ignored on this machine and **neither rule was in the repository**:
`.claude/settings.local.json` by the user's global ignore at
`~/.config/git/ignore`, and `.claude/worktrees/` by `.git/info/exclude`, which
belongs to this clone alone and is never pushed.

So the protection held here and nowhere else. A fresh clone — CI, a second
machine, anybody else — had neither rule, and the first thing `git add -A` would
have swept up is a worktree with a full `src-tauri/target/debug` inside it.

`.gitignore` now carries `.claude/`, which covers both and travels with the
repository. Verified by asking `git check-ignore` which rule answers for each
file: it is line 66 of `.gitignore` for both, where it used to be two files
outside version control.

Files: `.gitignore`.

### 2026-08-27 — And a fallback, because `zoom` is four years younger than the page's other rules

He asked the question worth asking about any fix for one browser: does it cost
anything anywhere else. One thing, and it was mine to have caught before he did.

**Firefox learned `zoom` in version 126, May 2024.** Anything older ignores the
property entirely — and ignoring it does not mean falling back to the old
behaviour, it means a 1180 px window standing at full size inside a frame that
is 1114 px wide, cropped by `overflow: hidden`. A worse fault than the one being
fixed, and a silent one.

`fit()` now asks `CSS.supports("zoom", "0.5")` and writes the transform where the
answer is no. That fallback is not the state this replaces: the resampling was
what an engine does to a **promoted** layer, and `will-change` is gone, so an old
Firefox gets a plain transform, which is what it drew correctly before anybody
promoted anything. `transform-origin: 0 0` stays on the stage for that one path
and says so.

Everything else `zoom` touches was checked rather than assumed: `offsetWidth`
reports 1180 whatever the zoom, the frame comes out 1114 × 702 as it did before,
and nothing is promoted any more.

Files: `docs/site/preview.js`, `docs/site/index.html`.

### 2026-08-27 — The brand is written the way the brand writes itself

His, from a comment on the published page: a small `z` in *značkárna*. The
footer had it with a capital.

It is not a preference, it is the rest of the repository: `LICENSE` and
`CONTRIBUTING.md` say **značkárna s.r.o.** in lower case, six times between
them, and the mark in the footer is a drawing rather than a word. The page was
the one place that capitalised it.

The English side carried the same capital and moved with it — the dictionary is
keyed by the Czech, so the entry was renamed rather than edited, which is what
keeps `docs:check` from reporting an orphan.

**Left alone on purpose:** the invented transcript says *jednám se Značkárnou*
three times, once per slide. That is a person speaking in a made-up meeting,
where a lower-case brand mid-sentence reads as a typo to anyone who does not
know the brand. It is a different decision from the footer's and it is his to
make, so it is asked rather than assumed.

Files: `docs/site/index.html`, `docs/site/en.json`.

### 2026-08-27 — The footer is one line: the mark, and whose mark it is

His, drawn on the page: an arrow from the sentence up to the mark, and *pokud
něco nesedí* struck out.

The mark and the sentence now stand on one line. They were two, and the break
between them made the reader carry the name down to the drawing it belongs to —
the sentence exists to say whose mark that is, so putting it anywhere but beside
it is the one place it does not work. `baseline` rather than `center`: the
drawing is 12 px against a 13.5 px line, and centring two things of nearly the
same height puts neither where the eye reads. The 12 px margin under the mark
went with the break, and a 3 px lift took its place, because the drawing's
shapes sit on its box's floor while the text's baseline is higher.

**And the invitation is gone**, with the address in it. Worth naming what that
costs: the page has two other ways to write — *Kontakt* in the header and
*Ozvěte se* in the panel for companies — so the address is not lost, only the
apology for the page that was standing under it. What is left is a credit, which
is what a footer is for.

Two translations went with the sentence rather than being left behind; the
dictionary reports an entry nobody uses, so removing them was the check's
instruction, not a tidy-up.

Files: `docs/site/index.html`, `docs/site/en.json`.

### 2026-08-27 — The footer's own padding had never applied

Two things from one comment, and the second is the interesting one.

**The mark and the sentence are centred on each other.** `baseline` was wrong and
he read it exactly right: a baseline puts the drawing's floor on the text's
baseline, so the letters ride above the shapes and the line looks like it is
flying upward. The drawing is 12 px in a 20 px line box, so centring the boxes
is what centres them to the eye. Measured: the two centres are the same number.

**And the page ended on the window's edge**, which he asked for as the header's
own air. The rule that should have provided it was there and had never applied:

```
.wrap   { max-width: 1180px; margin: 0 auto; padding: 0 32px; }   /* 0,1,0 */
footer  { padding: 96px 0 72px; }                                 /* 0,0,1 */
```

A class outranks a type, so `.wrap` overrode the footer's padding in full --
horizontal padding and all -- from the day the page was written. What has held
the air above the footer all this time is the closing section's own 96 below
itself, and the 72 under it was never there. The header escaped the same fate by
being written `header.top`.

It is `footer.wrap` now, with the two vertical values named separately so the
gutters stay where the wrapper puts them. Below the footer: 104 px, the same as
under the header, measured on both.

**The lesson is the one `CLAUDE.md` already has** in another form -- two rules
that differ only in file order must say so. This is its neighbour: a rule that
loses on specificity does not fail, it simply never happens, and it goes on
looking like the reason for whatever the page does.

Files: `docs/site/index.html`.

### 2026-08-27 — Correction: the footer's air is 96 above and 96 below

Two words from him — *pardon, patičkou* — correcting his own sentence eight
seconds after it: the padding he wanted matched was the footer's own, not the
header's. The entry above this one reads 104 under the footer and gives the
hero's air as the reason. That reason was mine, and the number came with it.

Measuring the whole edit rather than the half that was asked about turned up the
part that mattered more. Naming the class made **both** halves of the dead rule
live at once: the top went from 96 to 192 in the same edit that fixed the bottom
— because the closing section already puts 96 there and the footer's own 96 was
suddenly applying too. A change nobody asked for, arriving inside one somebody
did.

So the top is zero and the bottom is 96. Above the hairline: the closing
section's 96, exactly as it has been since the page was written. Below it: the
same 96. Measured on both.

Files: `docs/site/index.html`.

### 2026-08-27 — The panel offers an ecosystem, and two decisions are reversed

His, from a comment on the published page:

> Potřebujete víc? Propojte data a znalosti napříč firmou díky AI-ready
> ekosystému na míru.

It is in, and two things in it undo entries from yesterday. Both are recorded
rather than argued, because they are his to decide and the record's job is to
show that they were decisions.

**The verb goes back to the reader.** *Propojíme* became *Propojte*. Yesterday's
entry said the first person was what made the panel an offer rather than an
advertisement — Značkárna saying what it will do, instead of asking the reader
what they want. The imperative hands the doing back to the reader and keeps
Značkárna out of the sentence.

**And the vocabulary changes register.** *AI-ready ekosystém* is a compound in
English inside a Czech sentence, and *ekosystém* is a word the page has not used
once in nine screens of plain Czech — `CLAUDE.md` asks for the plain word over
the exact technical one, and this is the opposite trade. It also names what is
being sold much more concretely than *lidi, obsah a design* did, which is the
argument on the other side.

**Measured, because the panel is a row.** The Czech is 765 px against the old
633, and the paragraph's own `max-width: 66ch` is 687 — so the sentence is two
lines on any screen now, with the button beside it. That is the shape he asked
for on 26 August, so it is left alone; one line would need the measure widened
to about 74ch, which is a different decision about the whole page.

The English is *Need more? A bespoke AI-ready ecosystem, connecting data and
knowledge company-wide* — 777 px, twelve past the Czech, so the two wrap
together. Four drafts were measured; the one that reads best in English was also
the narrowest.

Files: `docs/site/index.html`, `docs/site/en.json`.

### 2026-08-27 — The cube comes back, and the brand joins the list

Both at his word, an hour apart, and both reverse yesterday.

**The mark is back before the sentence.** It went in on 26 August, came out the
same evening, and is back this morning — the group, the 18 px gap and the 44 px
height are the ones it had, recovered from the commit that carried them rather
than written again.

What changed in between is what stands beside it. The sentence is two lines on
every screen now, so 44 px of drawing sits against two lines of 18 px type
instead of one. The mark is the same size and reads as calmer, which is an
argument for leaving the height alone rather than for tuning it.

**And the offer names the brand first:**

> Potřebujete víc? Propojte značku, data a znalosti napříč firmou díky AI-ready
> ekosystému na míru.

*Značka* in front of *data* is Značkárna putting its own noun at the head of the
list — the one thing in that sentence a brand studio is expected to be asked
for, and until now the only one of the three it did not say.

English follows: *Need more? A bespoke AI-ready ecosystem, connecting brand,
data and knowledge company-wide.*

Files: `docs/site/index.html`, `docs/site/en.json`.

### 2026-08-27 — The panel for companies answers the pointer like every other card

His, from a comment: the white block has no hover, and every other block does.

It is the application's own answer, taken from the cards under *Co získáte?*
rather than invented here: the line goes from `--line` to `--line-field`, the
card takes `--shadow`, over the application's 0.22 s. Nothing lifts and nothing
moves — a translate would be this page making up a gesture the program does not
have, which is what the note on the cards says and why it is quoted rather than
rewritten.

Worth saying what the fault was: the panel is a white card among white cards,
and it was the only one that did not answer at all. A reader who has learned by
then that these blocks respond gets one that plays dead exactly where the page
asks them to write.

Files: `docs/site/index.html`.

### 2026-08-27 — The cube in the panel now presses its own shapes in and out

Tuned in the prototype with him over an afternoon, then moved into `#firmy` at
his word: *artefakt zachovej a vlož ho do bloku na webu*.

What it does: a shape in the colour of its own face — invisible against it —
sinks along that face's normal, swaps to white at the bottom of the hole and
rises back out. Then the second, then the third. Then the same thing backwards:
each white shape goes back in, takes the face colour at the bottom, and comes
out invisible, which leaves the bare cube for the next round. Dovnitř jde stěna,
ven vyleze značka.

Two things had to be got right before it read as a hole at all.

The first is that there is no perspective in this drawing. An earlier pass
scaled the shape down as it sank, which is the reflex, and he refused it in one
sentence: *analyzuj to jako perspektivu a udělej to správně*. The cube is a
parallel projection, so depth is a translation along a face normal at unchanged
size — blue straight down, red up and right, green up and left. Those three
vectors are the normals of those faces, not a choice.

The second is that the shape has to disappear behind the rim. Everything inside
the hole is clipped by the mouth, so what passes the rim is hidden by the face —
and the band left between rim and shape, drawn at 0.45 brightness, is the wall
of the hole. It costs nothing, because it is what is left over.

One layer, not two. The first working version faded a white copy in over a
sinking coloured one, and he cut it: *ten bílý tvar tam nemusíš dávat jako nový,
vždyť se může změnit ten zajetý*. The piece carries no `fill` of its own — the
colour is inherited from the group the keyframes animate, which is also why an
early attempt showed nothing: a `fill` attribute on the child outranks an
animated `fill` on its parent.

The timing is his, found on the slider and then written into the file: 8.34 s a
round, 0.5 s down or up, 0.215 s at the bottom where the colour changes. It got
there by his ear alone — 12 s, then half that speed, then too slow, then 25 s,
then 2×, then 1.5× again — which is why the numbers are milliseconds in the
source and the percentages are computed from them; six percentages rewritten by
hand each time is how a sequence quietly stops matching its description.

His export still ships whole, and shows wherever the animation cannot run — an
old browser, or a reader who asked for less motion. So the mark is never worse
than it was, and the file still contains the duplicate group exactly as it comes
out of his tool.

Verified in frozen frames — negative `animation-delay` with
`animation-play-state: paused`, since a headless browser never advances the
clock — at the surface, at the bottom of each journey, and with
`--force-prefers-reduced-motion`, which draws his export unchanged. Checked at
44 px, the size it runs at in the panel, not only at prototype size.

The prototype stays at
`https://claude.ai/code/artifact/715d8680-25e7-4d75-8095-a1b6319bdf74`, now with
variant D alone; A, B and C are gone, along with the section that existed to
choose between them.

Files: `docs/site/index.html`.

### 2026-08-27 — The mark in the panel goes to 52 px

His, in one line, once the cube started moving: *tu kostičku bych mírně
zvětšil.*

44 px was chosen against a sentence that had just become two lines, and the
entry that set it argued the smaller mark read as calmer. That held while the
drawing stood still. A moving one at the same size has to be looked for, which
is the opposite of what it is there to do.

The sentence beside it is two lines of 18 px, about 58 px of text. 52 stops just
short of that, so the mark reads as the sentence's equal without out-shouting
it. Nothing else moves: the row is still one line, the gap is still 18 px, and
the panel's height is set by the text, not by the mark.

Files: `docs/site/index.html`.

### 2026-08-27 — Half a second longer before the cube turns round

His: *dal bych mírně delší pauzu mezi tím, než se obrátí v kostce proces
vypínání a zapínání. Třeba jen o půl vteřiny.*

The stand with all three shapes white was 415 ms, which is shorter than one
shape's journey. The turn was starting while the third shape was still settling,
so the two halves ran into each other and read as one long shuffle rather than
as a thing done and then undone.

It is 915 ms now: the three returns all move half a second later and the round
grows from 8.34 s to 8.84 s. Nothing else changes — the same 0.5 s down or up,
the same 0.215 s at the bottom, and the same 655 ms of bare cube at the end,
which was never the problem.

Verified in frozen frames at 3.9 s and 4.4 s: byte-identical, so the cube really
does stand still across that window.

Files: `docs/site/index.html`.

### 2026-08-27 — The offer for companies asks about people, not the brand

His, from a comment:

> Potřebujete víc? Propojte své lidi, data a znalosti napříč firmou díky
> AI-ready ekosystému na míru.

*Značku* had been at the head of that list since this morning, on the argument
that it is the one noun a brand studio is expected to be asked for. *Své lidi*
is the better first word anyway: it is the reader's own company in the sentence,
and the two nouns after it — data, knowledge — are things people have. A brand
in that position was the studio talking about itself.

English follows: *Need more? A bespoke AI-ready ecosystem, connecting your
people, data and knowledge company-wide.*

Files: `docs/site/index.html`, `docs/site/en.json`.

### 2026-08-27 — The English offer becomes a sentence again

His, on the line that had just been written: *tohle mi nepřijde jako dobré
copy.*

It was *A bespoke AI-ready ecosystem, connecting your people, data and knowledge
company-wide* — a noun phrase, not a sentence, and it opened with the machinery.
The Czech invites: *Propojte své lidi, data a znalosti.* A reader is asked to do
something. The English described a product instead, and put the least readable
words of the four in front.

It is now *Need more? Connect your people, data and knowledge across the company,
with an AI-ready ecosystem built for you.* Same verb, same order, the mechanism
after the gain — which is the rule the release notes follow one document further
in.

The word *ecosystem* stays because it is his, and *company-wide* goes because
nobody says it out loud.

Files: `docs/site/en.json`.

### 2026-08-27 — The English offer, in his words

He wrote two of them:

> Ready for more? Unify your teams, data, and insights enterprise-wide with a
> custom-built, AI-ready ecosystem.

> Demand more. Connect teams, data, and knowledge with an AI-ready ecosystem
> built just for you.

The line on the page takes the opening of the first and the body of the second:

> Ready for more? Connect your teams, data and knowledge across the company with
> an AI-ready ecosystem built just for you.

*Ready for more?* keeps the question the Czech asks; *Demand more.* is an order,
and the panel is an invitation. *Knowledge* over *insights*, which is a word
consultants use and the Czech does not say. *Across the company* over
*enterprise-wide*, which nobody says out loud and which the page has been
avoiding all day. *Your* is back in front of *teams*, because the Czech says
*své*. The list comma goes: this page writes *punctuation, paragraphs and
errors* everywhere else.

Files: `docs/site/en.json`.

### 2026-08-27 — The English offer, settled

His, and the shortest of the day:

> Connect people, data and knowledge across your company, with bespoke
> AI-ready tools.

Half the length of what it replaces, and it moves *your* from the people to the
company, which is the smaller claim and the truer one: they are not his reader's
people, the company is his reader's company. *Tools* instead of *ecosystem* —
a thing that gets used rather than a thing that gets described.

One asymmetry is deliberate and worth recording so nobody quietly fixes it: the
Czech opens with a question, *Potřebujete víc?*, and the English does not. Czech
carries the invitation in that question; English carries it in the imperative
verb, and adding *Need more?* in front would be the same invitation twice.

Files: `docs/site/en.json`.

### 2026-08-27 — The English offer, settled again

His:

> Ready for more? Connect your people, data and knowledge with AI-ready
> ecosystem of tools.

On the page with one word added — *with an AI-ready ecosystem of tools*. English
needs the article there; without it the phrase reads as written by someone who
does not have the language, which is the one thing this half of the page cannot
afford.

The question is back at the front, so the entry above it is superseded on that
point: both halves open with an invitation again. *Ecosystem of tools* keeps
both words he has been circling all afternoon, and the noun the reader
understands is the one at the end.

Files: `docs/site/en.json`.

### 2026-08-27 — Značkárna in the footer answers the pointer too

His, from a comment: *značkárna nemá stejný hover jako ostatní odkazy v
patičce, proč ta inkonzistence?*

No reason — it was an oversight. The footer's links carry
`.foot-links a:hover`, and the name in the sentence beside the mark is not in
that row, so it inherited `a { color: inherit }` and nothing else. It was the
one link in the footer that did nothing when you reached it.

It now takes the same answer: the colour goes to `--text`.

What stays different is the resting state, and that is deliberate. A link in a
row of links is found by being in that row, so it drops its underline; a link
inside a sentence has only the underline to say it is a link, and taking that
away would hide it in the text. Same answer to the pointer, different resting
state, each for the reason it is read.

Files: `docs/site/index.html`.

### 2026-08-27 — The bold leads above the cards drop their full stops

His: *u spousty nadpisů mám za větou tečku, je to záměr? V hlavním H1 mi to
nevadí, ale co jinde?*

It was consistent and it was nobody's decision: 25 bold leads on the page, all
25 with a stop, and no entry in this folder choosing that.

They are not run-in headings. `.cards b` is `display: block`, and every one of
the 25 is followed by a `<p>`, so each stands alone on its line above its
paragraph -- and a line on its own does not take a stop. That holds even for the
ones that are whole sentences: a newspaper headline is a sentence and has no
full stop either.

Two groups made it visible. The noun phrases -- *Naprosté soukromí*, *Whisper a
Silero*, *Rust, React a Tauri*, *Procesor nebo grafika* -- promised a sentence
that never arrived. The subordinate clauses -- *Když vložíte odkaz*, *Když
kontrolujete aktualizace* -- read as cut off.

The hero keeps its two stops, which he said himself and which is right for a
different reason: *Zapněte počítač. Vypněte předplatné.* is two sentences, and
the joke is the pair. Without the stops it would be one breath.

Both languages moved together, the key in `en.json` being the Czech sentence:
25 keys and 25 English leads, checked by `translate.mjs --check` at 264.

Files: `docs/site/index.html`, `docs/site/en.json`.

