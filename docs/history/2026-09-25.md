# 2026-09-25

### 2026-09-25 — Settings stops offering itself

He noticed that the header keeps its `Nastavení` button while Settings is open.
`Nový přepis` beside it steps aside for Settings and for the wizard; the
Settings button carried no condition at all, so on its own screen it was the
one button left in the header and the one that could do nothing — pressing it
reloaded the page he was already reading.

It is hidden on the Settings screen now. The way out was already `Zpět` on the
left, which shows on every screen but the archive, so nobody is left without
one. The warning dot it carries when a component is missing loses nothing: in
Settings that component is on the page.

**Deliberately left alone: the wizard.** The same button still shows there. It
may be an escape hatch out of a first run somebody wants to abandon, and it is
his to say whether it should go too.

**Not covered by a test.** `App.tsx` is 1,960 lines and nothing renders it;
standing a harness up for one condition is a bigger change than the repair.
Checked by eye in the dev build.

Files: `src/App.tsx`.
